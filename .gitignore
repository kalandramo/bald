# 编译生成的可执行文件
bin/
bald.exe

# proto 生成产物（由 buf generate 生成）。
# - _example/bald/gen：示例项目生成代码，体积较大，不入库（本地 `task proto` 重建）。
# - pkg/conf/gen：根模块运行时必需的生成代码（约 100KB，最大文件 24KB），
#   必须入库，否则外部 `go get github.com/kalandramo/bald` 后缺少 .pb.go 而编译失败。
/_example/bald/gen/

# 示例运行时产生的本地 SQLite 数据库
_example/*.db
_example/bald/*.db

# 天枢（agent runtime）状态目录。
# 整体排除本地/派生成分，只放行跨设备同步有价值的子集：
#   - skills/   手写方法论资产（SKILL.md），跨设备复用
#   - knowledge/ 记忆库（*.jsonl），跨设备续用（对应旧 .codebuddy/memory 的定位）
# 排除：meridian.db*（会话 SQLite 状态）、backups/（编辑快照，内容在 git 历史）、
#       artifacts/ tmp/（工具缓存）、plans/（计划草稿）。
# 注：根级 .rivet-config.json（verify 命令）与 .rivet.md（项目说明）是项目级配置，
#     跨设备开发有用，**不**忽略。
.rivet/*
!.rivet/skills/
!.rivet/knowledge/
# 但 skills/_drafts 是会话自动草稿，knowledge 下的 sqlite 是索引缓存，不入库。
.rivet/skills/_drafts/
.rivet/knowledge/memory-index.sqlite

# cobramcp examples 的构建产物。
# `go build ./...` 会在每个 main 包目录内留下与目录同名的二进制；
# examples/make 的 build 目标输出到 ../../bin/（已由上面的 bin/ 规则覆盖），
# 但直接 go build ./... 仍会在包目录内落盘。逐条精确忽略，避免误伤同目录的
# makefile / 源码等同名文件。
/cobramcp/examples/task/task
/cobramcp/examples/task/mcpserver/mcpserver
/cobramcp/examples/positional-args/positional-args
/cobramcp/examples/positional-args-mcp-server/positional-args-mcp-server