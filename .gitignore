# 编译生成的可执行文件
bin/
bald.exe

# proto 生成产物（由 buf generate 生成）。
# - _example/bald/gen：示例项目生成代码，体积较大，不入库（本地 `task proto` 重建）。
# - pkg/conf/gen：根模块运行时必需的生成代码（约 100KB，最大文件 24KB），
#   必须入库，否则外部 `go get github.com/kalandramo/bald` 后缺少 .pb.go 而编译失败。
/_example/bald/gen/

# 示例运行时产生的本地 SQLite 数据库
_example/*.db
_example/bald/*.db

# 天枢（agent runtime）本地状态与配置。
# .rivet/ 含活跃 SQLite（meridian.db + WAL）、编辑备份、工具输出缓存、计划草稿，
# 体积以 MB 计且每次会话都变——入库会污染公开仓库历史且 diff 无休止。
.rivet/
.rivet.md
.rivet-config.json