# 2026-09-08

- **前端功能验证完成（go-bald-admin-web F0–F7）**：按前端功能实现计划验证 16 处问题全部修复，`vue-tsc`+`vite build`✅、`vitest` 14/14✅、`eslint`✅。
- **严重缺陷：API 模板字面量批量破坏**：8 个 API 文件（tenants/users/menus/permissions/dicts/files/audits/auth）的 `` ` 反引号被破坏为 `` ` `` 分隔符（`${1}/`tenant/`），导致 TS 全量语法错误。已逐一修复为正确的 `` `v1/<域>/${id}` ``。
- **F5 上传协议不匹配（修复）**：后端 `POST /v1/file/upload` 走 multipart form（file + directory 字段），前端用 base64 JSON。已改为 `FormData` + multipart，并添加 120s 超时 + 进度回调。下载路径修正为 `/v1/file/{id}/download`（原先 `/v1/files/` 多一层 s）。
- **`useConfirmAction` 重复弹错**：axios 拦截器已 ElMessage，confirmAction 捕获后又弹一次——改为仅 `console.error`，不再重复提示。
- **Element Plus DefaultRow TS 兼容**：6 个页面 slot `#default="{ row }"` 需加 `as` 断言；`TabPaneName` 改为 `string | number`。
- **F7 清理**：删 `SwitchUser.vue`（引用了已删除的 `userStore.changeUser`）、删 `login/apis/`（captcha 死代码）；`eslint.config.js` 加 markdown ignore（README GBK 编码 ESLint 无法解析）。
- **前端页面乱码修复**：用户截图报告登录页中文全部显示为 ``。根因：6 个 `.vue/.ts` 文件被 Windows 编辑器以 GBK 编码保存。两种模式——① 纯 GBK（`types/api.ts`/`user.ts`/`router/index.ts`）→ 转码修复；② 双重编码"锟斤拷"（`login/index.vue`/`auth/index.ts`/`axios.ts`）→ 重写修复。全量扫描 src+public 后确认无残留。
- **登录后 whoami 报 "token is malformed: invalid number of segments"（修复）**：根因在后端——`auth biz` 的 `TokenPair`/`UserInfo` 是纯 Go 结构且无 json tag，gin handler 直接 `c.JSON(pair)` 按 Go 导出名输出 `{"AccessToken":...}`，前端取 `res.access_token` 得 `undefined` 存入 localStorage，whoami 带 `Bearer undefined` 触发 JWT 段数错误。修复=两结构补 json tag（`access_token`/`expires_at`/`username`/`user_id`/`tenant_id`/`roles`/`token_type`）。**惯例（项目级）：biz 纯 Go 结构被 `c.JSON` 直接序列化必须带 json tag；proto 消息才走 writePB（protojson）**，参照 secret biz `Item`。另：login 页默认密码已改为真实种子 admin/admin123。
- **用户列表"暂无数据"（修复）**：项目无 `.env`，`axios baseURL=import.meta.env.VITE_BASE_URL` 为 undefined → 相对 URL 在嵌套路由页（/users/index）被解析为 /users/v1/user，打不中 vite proxy `/v1`，SPA fallback 返 HTML 200 → res.users undefined → 静默空列表（无报错）。修复=`baseURL: VITE_BASE_URL || "/"`（axios.ts）。次因一并修：writePB 加 `protojson.MarshalOptions{UseProtoNames: true}` 输出 snake_case（前端 types/api.ts 全按 snake_case 建模，protojson 默认 camelCase 会导致 createdAt/type_name 等系统性字段错位）；bindPB 的 Unmarshal 两种命名都接受，不受影响。
- **go-bald-admin 云端 PG 库数据实况（2026-09-08 curl 实测）**：users 表实际为 u-admin/u-alice（CreatedAt 零值=旧版本代码写入的历史行，CreatedAt 列系 T2 后 AutoMigrate 加）+ u-carol（09-07 手工测试建），**无 u-bob**（被删或未跑过含 bob 种子的版本）；当前代码重启后 seed 幂等会自动补建 bob。PowerShell 5.1 排障注意：`Invoke-RestMethod`+`ConvertTo-Json` 会把单元素数组渲染成标量（roles:["admin"] 显示为 "admin"）——**必须用 `Invoke-WebRequest -UseBasicParsing` 取原始 .Content 判断响应结构**。
- **go-bald-admin 文件存储分桶语义（源项目设计，勿误判为 bug）**：文件按 MIME 分桶（image→images、video/audio→同名、text 与 pdf/json/office→docs、其余→files）；**`file.bucket` 配置只覆盖 files 兜底桶，不是全局桶名**（`bucketFor`：仅 ContentTypeToBucketName 返回 files 时才用 defaultBucket）。云端 MinIO（10.82.69.251:30658）现有 docs（txt 等文本）/images（测试图片）/go-bald-admin（0 对象，兜底未触发过）三桶。用户若要单桶化需改 `file biz` 的 `bucketFor`。
- **vite.config.ts 乱码已修复（write_to_file 重写）**：注释按工作区 v3-admin-vite 原版模板恢复，代码保持项目现状（/v1 代理 → localhost:8080）。**教训：GBK 逆向转码不可在真实文件上实验**——双重编码乱码中有字符在 cp936 编码器无映射，GetBytes 丢字变 '?' 不可逆（本次试错写坏过一次，靠对照模板重写救回）。修复乱码的正确姿势：找到模板/上游原版直接对照重写。另：根目录配置文件（vite.config.ts 等）也要纳入编码扫描范围，别只扫 src。
- **用户名格式校验已加（前后端双重拦截）**：规则 `^[a-zA-Z0-9_]{3,32}$`（登录标识符仅英文，中文展示名留给昵称）——后端 `user biz` 加 `usernamePattern`（Create 必校验、Update 传新名才校验，berrors.BadRequest→400），前端 users 页 rules 加同规则 pattern。存量中文用户名（u-murphy/莫伟龙，用户测试所建）不受影响可登录；Login 不校验格式（只查库比对）。用户 ID 格式暂未校验（用户未要求）。
- **model 包按功能域拆分（go-bald-admin）**：`internal/apiserver/model/model.go` 拆为 user/tenant/role（Role+Permission+RolePolicy）/menu/secret/dict（DictType+DictEntry）/file/audit.go，model.go 仅留包注释 + 共享 splitCSV（RolesList/PermsList 随各自实体走）。同包拆文件不影响外部引用，go build 全绿。已提交 bald 根仓库 `835dba4 refactor(examples): go-bald-admin model 包按功能域拆分`（未推送）。
- **联调修复提交（2026-09-08，未推送）**：bald 根仓库 `10e4115`（后端 3 文件：auth json tag、writePB UseProtoNames、用户名校验）；go-bald-admin-web 独立仓库 `1af1feb`（36 文件，T2-T5 全部前端页面+联调修复首次入库）。**git 结构事实：go-bald-admin-web 是独立 git 仓库**（根仓库 .gitignore:19 忽略），提交需进该目录单独做；`.agents/` 已加其 .gitignore，`.vscode/` 按 v3-admin-vite 惯例仅 extensions.json 入库。**教训：PowerShell 里 `git commit -m` 带长中文多段信息可能静默不执行（exit 0 无 commit 产出）——改用 `[IO.File]::WriteAllText` 写 UTF-8 无 BOM 的 `.git/COMMIT_MSG_TMP` + `git commit -F`（项目既有惯例，用后即删）。**
