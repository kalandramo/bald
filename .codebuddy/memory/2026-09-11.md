# 2026-09-11（macOS）

## berrors 包名修正：errors→berrors（b537495，49 文件 +68/-69）
- **动因**：模块 `github.com/kalandramo/bald/berrors` 声明 `package errors`（沿袭 Kratos api/errors 遮蔽标准库惯例），迫使全仓 40 处消费文件写 `berrors "..."` 别名；决策⑧落地后消费面进一步扩大，别名负担显性化。
- **改动**：①包声明 3 文件（errors.go/code.go/errors_test.go）`package errors`→`berrors`；②grpcerr 无别名引用处 `errors.`→`berrors.` 限定符（含注释 `*errors.Error`）；③40 个消费文件 sed 去别名；④`transport/web/binder.go`/`handler.go` 原本无别名引用（用 `errors.` 限定符），同步改 `berrors.`；⑤去别名后 gofmt 重排 import（3 文件）。
- **验证**：全 module（root/transport-web/examples/bootstrap/contrib/_example）构建通过；berrors 三包单测 + pkg/middleware 全量 + examples task verify（含 e2e）全绿；gofmt 零残留。
- **文档同步**：框架契约总览.md 两处包名记载；错误模型设计.md 代码示例（§设计 package errors→berrors）+ §兼容性第 2 条 + FAQ「为什么包名遮蔽标准库」标注被 09-11 改名取代（保留删除线原文，历史可溯）。
- **教训两个**：
  1. **并行 replace_in_file 同一文件有竞态**——两个编辑同时下，后写覆盖先写，第一个「成功」静默丢失。同一文件多处改动必须串行（本次 213 行标题第一次替换被并行竞态吞掉，grep 复查才发现）。
  2. 批量 sed 后 gofmt 必跑——去 import 别名改变分组排序，3 文件需要重排。
- **连带发现（未改）**：`broker/rocketmq/option`→`rocketmqOption`、`broker/rocketmq/v2`→`rocketmqClientGo` 驼峰包名不合规（go-kratos 迁移残留），属 broker 闲置模块，按需再治理。
