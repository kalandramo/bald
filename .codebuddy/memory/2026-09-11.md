# 2026-09-11（macOS）

## berrors 包名修正：errors→berrors（b537495，49 文件 +68/-69）
- **动因**：模块 `github.com/kalandramo/bald/berrors` 声明 `package errors`（沿袭 Kratos api/errors 遮蔽标准库惯例），迫使全仓 40 处消费文件写 `berrors "..."` 别名；决策⑧落地后消费面进一步扩大，别名负担显性化。
- **改动**：①包声明 3 文件（errors.go/code.go/errors_test.go）`package errors`→`berrors`；②grpcerr 无别名引用处 `errors.`→`berrors.` 限定符（含注释 `*errors.Error`）；③40 个消费文件 sed 去别名；④`transport/web/binder.go`/`handler.go` 原本无别名引用（用 `errors.` 限定符），同步改 `berrors.`；⑤去别名后 gofmt 重排 import（3 文件）。
- **验证**：全 module（root/transport-web/examples/bootstrap/contrib/_example）构建通过；berrors 三包单测 + pkg/middleware 全量 + examples task verify（含 e2e）全绿；gofmt 零残留。
- **文档同步**：框架契约总览.md 两处包名记载；错误模型设计.md 代码示例（§设计 package errors→berrors）+ §兼容性第 2 条 + FAQ「为什么包名遮蔽标准库」标注被 09-11 改名取代（保留删除线原文，历史可溯）。
- **教训两个**：
  1. **并行 replace_in_file 同一文件有竞态**——两个编辑同时下，后写覆盖先写，第一个「成功」静默丢失。同一文件多处改动必须串行（本次 213 行标题第一次替换被并行竞态吞掉，grep 复查才发现）。
  2. 批量 sed 后 gofmt 必跑——去 import 别名改变分组排序，3 文件需要重排。
- **连带发现→同日已治理（c77b712）**：`broker/rocketmq/option`→`rocketmqOption`、`broker/rocketmq/v2`→`rocketmqClientGo` 驼峰包名（go-kratos 迁移残留）。改名 `option`/`rocketmq`，消费方（v2 内部、contract、transport/rocketmq）别名清零。**关键先例**：transport/kafka import broker/kafka 裸同名导入是仓内既有惯例，transport/rocketmq 与自身 package rocketmq 同名裸导入合法；TestServer 集成测试基线即红（需真实 broker），验证以单测为准。全仓 mixedCaps 包名违例清零。

## Windows 侧：决策⑧前端收尾 + reason 风格统一专项

- **前端收尾 `2f2c561`（go-bald-admin-web，独立仓库 1af1feb..2f2c561）**：axios.ts 删 errorData?.error 死 key、backendMessage = message、error.reason = details?.[0]?.reason 挂程序化出口（导出 ApiError 类型）；types/api.ts 补 ApiErrorBody/ApiErrorDetail。vue-tsc/vitest 14/14/eslint 全过。工具坑：execute_command 对含 vitest 字样命令误判 watch 模式吞输出——用 Start-Process node.exe 直调 vitest.mjs + 读日志文件绕开；PS5.1 Get-Content 读 UTF-8 显乱码是 ANSI 显示层问题，验证用 [IO.File]::ReadAllText。
- **reason 统一 `8d7a73e`（bald，901d72c..8d7a73e，19 文件 +53/-43）**：reason 全仓收敛「域/稳定标识」（小写 snake_case，动态内容进 WithMessage，NotFound 三面 message 同形 "xxx not found"）。biz 8 处句子风格拆双字段（user/invalid_username、tenant/platform_protected、dict/type_missing_required_fields 等）；grpc notFoundOr 双参化 + 直写域级化（user/not_found）；gin 哨兵域级化（NOT_FOUND×8→各域值、auth/bad_credential、admin/component_*），writeBizErr 泛型兜底→裸值 not_found/already_exists。e2e 零改动（只断言状态码）。shuffle 8 包全绿。**重大教训：PS 嵌套数组 @(@(...)) 单元素被展平，$pair[0] 取首字符，批量替换损坏 5 文件（git checkout 恢复，-split 字符串 job + ,@() 防展平重做）——批量脚本必须先打计数日志验证 pattern 完整性**。gofmt -l 全列是 CRLF 环境噪音（对照组实验确认，09-10 记录再验证）。
- 至此决策⑧三面 + 前端消费端全闭环；reason 契约统一在消费方诞生前完成（零破坏窗口期）。
