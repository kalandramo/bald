# 2026-09-07

- **pkg/{audit,authn,authz} 是否移根目录评估**：结论不移动。bald 布局契约=顶层目录（有独立 go.mod）=可独立发布的桥接/插件模块，`pkg/`=根模块核心包（判别标准：目录下有无 go.mod）。三包零第三方依赖、被 middleware/appkit/crudbridge 内部引用（authn→pkg/contextx），contrib/authn-jwt 经根模块依赖它们，v0.1.0 已发布路径为对外契约。

- **go-wind-admin 业务移植计划已创建**：`bald/docs/devel/zh-CN/go-wind-admin 业务移植计划.md`（README 索引已登记）。范围=精选子集 9 项（租户/用户/角色权限/菜单/字典/审计日志/文件/认证扩展+Nacos），其余 30 服务列后续迭代。里程碑 T0–T8。计划状态：待用户提供云端依赖（PG/Redis/MinIO/Nacos/可选 OTLP）后开始实施。

- **go-bald-admin 装配关键事实（子代理核实）**：
  - 配置：appkit 四源（flag>env>file>remote），BootstrapConfig proto（bconf/proto/bootstrap/v1/bootstrap.proto）已有 database.sql/cache.redis/storage.minio/registry.nacos 段；业务配置经 main.go BeforeStart 的 baldconfig.Unmarshal 注入 internal/bootstrap 结构体。
  - openDB()：env BALD_ADMIN_DB_DSN，dsnScheme 分流 postgres/mysql/sqlite（非空 DSN 不静默降级）；多租户 store.RegisterTenant("tenant_id", store.DefaultTenantFunc)。
  - 分层范式：apiserver/{biz/v1,handler/gin,grpc,model/model.go（统一实体文件）,gen}；新增模块需 wire.go BizSet+casbin csv+e2e。
  - 审计：StoreAuditor→audit_records 表；StreamAuditor→Redis XAdd "audit.events"；audit.backends 期望态 Reconcile 热切换。
  - e2e 范式：grpc/secret_e2e_test.go 真实 gRPC 连接 + Signer.IssueToken。

- **移植前需处置的三个已核实缺口**：① contrib/cache-redis 仅 New(addr)，无 password/db/TTL 配置；② bconf storage.minio 契约无 bucket 字段（业务自持 file.bucket 段）；③ go-bald-admin 未接真实 registry（inmemory），Nacos 接线路径=RegistrarRegistry+nacoscontract.Provider（_example/bald/register_nacos.go 先例，build tag 仅 _example 用）。

- **go-wind-admin 源事实**：38 service；Ent schema 为唯一真源（sql/ 无 DDL，仅 demo 种子且部分过期如 sys_dict_entries.entry_label 已迁 i18n）；种子在 pkg/constants/default_data.go（admin/Abcd@1234、platform:admin、PlatformTenantID=0）；casbin 策略由 authorizer_provider.go 从 DB 装载；审计=HTTP 中间件 AOP 写入（log_hash SHA256+ECDSA 签名）；Redis 需 8.0+（HExpire，bald 侧无此硬依赖，7.x 可用）。

- **T0/T1 已完成（2026-09-07）**：云端依赖接通（PG 10.82.138.249:31068/db=go-bald-admin、Redis :30967 db8、MinIO 10.82.69.251:30658）；cache-redis 加 WithPassword/WithDB/WithTTL Option；bootstrap 加 Configure(cfg, fileBucket) + openDB 按 database.sql driver 分流（env BALD_ADMIN_DB_DSN 仍优先）+ MinIO 构造；冒烟全通过（登录/PG CRUD/跨租户 404/RBAC 403/Cache-Aside）。
- **修复的存量时序 bug（重要约定）**：①wire provideSigner 构造期快照 nil Signer → bootstrap.LazySigner()；②secret.New 构造期快照 nil SecretStore → 请求期 b.store() 解析。**约定：biz 层引用 bootstrap 包级桥接一律请求期读取，禁止构造期快照**（T2+ 新模块必须遵守）。
- **环境事实**：本机无 gcc → SQLite driver 换纯 Go github.com/glebarez/sqlite（三处 import 已换）；metrics 与 gRPC 默认同抢 :9090，运行须 BALD_ADMIN_METRICS_ADDR=:9091（T8 根治）；grpc_auth_e2e_test issueToken 修了 shuffle 顺序耦合；PowerShell 下 go mod edit 的 -replace 参数需引号包裹；Start-Process 冒烟后 go build -o 必须重编 exe（go build ./... 不更新它）；curl.exe 冒烟 POST body 含空格会被 PowerShell 拆参（报 proto unexpected EOF），测试覆盖以 httptest e2e 为准。

- **T2 已完成（2026-09-07，租户+用户模块）**：proto 全部收敛 api/（用户指令：proto+生成物都放 api/；buf 根=api/，源 api/{secret,tenant,user}/v1/，生成物 api/gen/<域>/v1/，旧 proto/ gen/ 已删）；Tenant 实体（无 TenantID 字段=平台管理面，P8 天然不作用）+ User 加 CreatedAt/UpdatedAt；biz/handler(gin+grpc)/wire/gateway 全链路；seed 加 platform/t-default/t-other 租户。冒烟：租户列表/403/用户 CRUD/网关转码全通过。
- **T2 关键决策（T3+ 必守）**：① REST 路径单数（/v1/tenant、/v1/user）与 gRPC 归一同源（P9 策略单写）；② gin handler 必须用 bindPB/writePB（protojson）而非 c.ShouldBindJSON/c.JSON——否则枚举输出数字、Timestamp 输出内部字段，与 gateway 转码行为不一致；③ 列表 total 用 uint32（proto3 JSON int64 编码为字符串）；④ casbin 策略行 tenant 仅 admin、user admin 全权 viewer 只读；⑤ ListUsers 已从 SecretService 迁 UserService（避免 gateway 重复注册 panic）。

- **T3 已完成（2026-09-07，角色/权限/菜单，全量测试绿）**：
  - **D3 策略数据化**：静态 rbac_policy.csv 已删除；p 行落 RolePolicy 表、g 行由 User.Roles 装载；bootstrap.loadPolicyCSV 读库拼 csv 注入 contrib casbin（改库重启生效，无运行期热重载——列后续迭代）。
  - **RolePolicy 主键=string 业务键 `role:object:action`**：放弃 uint 自增（store.Eq 仅收 string 值；且与仓库「全实体 string 主键」范式一致）——Create 冲突天然防重，删除语义清晰。
  - **策略行动作六元组**：REST 归一化=HTTP 动词小写（post/put/delete），gRPC=get/list/write——双协议全放行需 get/post/put/delete/list/write 六行（menu/permission 管理域因此修复过一轮 403）。
  - **Menu 树**：ParentID 空串=根（源 uint32 parent_id=0 的零值歧义精简）；Children `gorm:"-"` 内存嵌套；BuildTree 两遍 O(n)（孤儿跳过、各层 Order 稳定排序——源项目前端排序，bald 服务端排定）；删除级联子树（BFS 收集）。
  - **顺手修 contrib/store-gorm 缺陷**：toMapExcludeKey 反射不解析 gorm tag，`gorm:"-"` 字段被当列写进 Updates（SQLite 报 no such column）——已修+回归锁；测试 driver 切 glebarez + t.Cleanup 显式 Close（Windows TempDir 文件锁同款坑）。
  - **顺手修 contrib/authz-casbin**：NewWithModel 容错空策略文本（stringadapter 对空串报 invalid line）——空策略=无策略 enforcer=全拒绝（fail-closed 合法初始态，双处单测锁定）。
  - 新文件：api/{menu,permission}/v1/*.proto、biz/v1/{menu,permission}/、handler/gin/{menu,permission}.go、grpc/{menu,permission}.go、t3_e2e_test.go、security/casbin/casbin_test.go。
  - **测试解码注意**：e2e 断言 writePB 输出必须用 protojson 解（encoding/json 的 snake_case tag 对不上 protojson 的 lowerCamel 输出，如 menuIds）。

- **流程事实**：用户规则「计划文档实现后需要复制一份到 docs 目录」实指复制到示例项目 `go-bald-admin/docs/`（T3 完成时已执行；bald/docs/devel/zh-CN/ 仍为唯一真源）。

- **T4 已完成（2026-09-07，字典管理，全量测试绿）**：
  - **实体**：DictType（ID=type_code）/ DictEntry（ID=`type_code:entry_value` 业务键），i18n 表精简掉（label 内联 zh）；字典=租户级实体（TenantID 字段保留，P8 隔离），种子 3 组 8 条归 t-default。
  - **Cache-Aside**：键 `dict:entries:<tenant>:<typeCode>` 按类型聚合条目 JSON；写穿透失效；空 type_code 全量直连不缓存。
  - **contrib/cache-redis 降级语义（T4 验收触发）**：Get 对 Redis 非 Nil 错误降级直连 loader（此前报错会把缓存故障放大为业务 5xx）；回填 Set 失败静默；miniredis Close 单测锁定。注意 Get 命中路径别写成两次 rdb.Get（初版犯过）。
  - **store API 事实**：`Store.Update(ctx, obj)` 两参数（主键定位 + injectWriteTenant）；`Store.List` 返回 total 为 int64（biz 转 int）。
  - **e2e 形态**：startDictREST 以 miniredis 真实例注入 dict biz，断言缓存键真实存在/失效；跨租户 P8 空列表；viewer 只读写 403。miniredis 升 v2.39.0（go get @latest 拉的新版，与 cache-redis module 的用法兼容）。
  - **接线清单（新模块全量）**：model 实体 → proto+buf → biz → handler/gin+grpc → wire.go+wire_gen.go（手工同步）→ server.go RegisterRoutes → main.go（import/registerGRPC/registerGateway）→ bootstrap（AutoMigrate/Store/种子/策略行）→ e2e + 存量 e2e 签名适配。RegisterRoutes 每加一个 biz 参数，admin_e2e/auth_middleware_e2e/tenant_e2e 三处调用点都要同步。

- **T0–T4 代码已提交（2026-09-07，用户明确要求）**：bald 根仓库三个 commit——7cef49c fix(authz-casbin,cache-redis,store-gorm)、72072b8 feat(examples) T2–T4 全量、e907e3a docs 计划文档。结构性处置：.gitignore 删 `examples/**/go-bald-admin` 忽略规则（示例项目纳入根仓库，用户选定）；真实凭证 go-bald-admin.yaml 不入库（保持 M 挂起），占位模板 go-bald-admin.yaml.example 入库。未推送（规则：不自行推送）。**BOM 教训**：PowerShell 5.1 `Set-Content -Encoding UTF8` 带 BOM，commit 1 标题开头有 U+FEFF（git log 显示 ﻿）；写提交信息应改用 `[IO.File]::WriteAllText(path, text, [Text.UTF8Encoding]::new($false))`（commit 2/3 已用，干净）。
