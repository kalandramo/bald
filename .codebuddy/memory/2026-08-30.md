# 2026-08-30

## 按架构演进路线文档逐条落地 P0-P4（bald）

用户要求"@架构演进路线.md 按路线一一实现"。澄清范围：全量 P0-P4，但 **P2.3 多引擎子模块（mongo/ent/redis）暂不新建**（gorm 已落作范本，避免空工程）。

### 落地清单（全绿验证）
- **P0 生命周期分阶段停机**：`pkg/appkit/appkit.go` 的 `stopAll` 重构为三阶段（beforeStop→server.Stop→afterStop）各自 `context.WithTimeout(parent, 阶段超时)` + 新增 `runHook`（recover 隔离钩子 panic）；struct 加 `beforeStopTimeout/afterStopTimeout` 字段 + `BeforeStopTimeout/AfterStopTimeout` Option（默认 5s）。补 `TestP0_StopPhasesOrderAndTimeout`/`TestP0_HookPanicIsolated`。
- **P1 全局 builder 注册表**：新增 `pkg/appkit/registry.go` —— 泛型 `Registry[T]`（Register/MustRegister/Get/List，并发安全）+ 预置 `ServerRegistry/ProviderRegistry/MiddlewareRegistry`；`RegisterStoreProvider(name, factory)` 便捷函数供桥接子模块 init 自注册。补 `TestP1_Registry`/`TestP1_StoreProviderRegistry`。
- **P2.1 分页三策略内聚**：新增 `pkg/store/paging.go` —— `Paginator` 接口 + `pagePaginator/offsetPaginator/tokenPaginator/noPaginator` 三/四实现 + `detectStrategy` + `encodeToken`（base64）；`store.go.translate` 改为调策略算 offset/limit 并按策略填 meta。补 `paging_test.go`（策略单测 + translate 元数据）。
  - 坑：`PagingRequest.NextToken` 原生 *string、`PageSize/Page` 原生 *uint32、`Total` 等 wrapperspb（与历史一致）；移除 store.go 冗余 `strconv` import。
- **P2.2 可选 Mapper**：新增 `pkg/store/mapper.go` —— `Mapper[DTO,Entity]` 接口 + `CopierMapper`（反射同名字段复制，含 nil 指针自动 New）；补 `mapper_test.go`。仓促修复：`copyFields` 对 nil dst 指针需 `dst.Set(reflect.New(...))`（指针实体 `Mapper[DTO,*Entity]` 用例暴露）。
- **P3 横切对齐**：`pkg/log`（全局 nop 回退，已具备，已有测试）、`pkg/berrors`（**改 `New` 构造即 captureStack**，对齐 go-lulu WindError；原有仅 With* 时捕获）、`pkg/middleware`（可观测依赖倒置 + otel no-op，已具备）。补 `TestStackTraceCaptured`（栈字符串含调用方帧）。
  - 坑：`StackTrace()` 返回 `string` 非 `[]runtime.Frame`，测试初写错类型报错后修正。
- **P4 工程化**：`_example/bald/main.go` 改 cobra root 挂 `codegen` 子命令 + **kubectl 风格 PATH 插件发现**（`bald-<name>` 转发，兼容旧 cobra 无 `CommandNotFound` 字段，用 `isKnownCommand` 预判）；新增 `_example/bald/codegen/gen.go`（`gen proto`/`gen store` 子命令，embed 模板式，复用 osbuilder 范式）。
  - 坑：`_example/bald/gen/` 目录已有 `greet.pb.go`(pkg baldv1) 与我新 gen.go(package gen) 包冲突 → 改放 `codegen/` 目录。

### 验证结果（全绿）
- `go build ./...` + `go test ./pkg/...` 通过
- `bald-store-gorm` `go test ./...` 通过
- `_example` `go run ./bald user`（内存）与 `go run -tags gorm ./bald user`（GORM）一致
- `task verify` + `task example-e2e`（grpc-gateway e2e，main.go 加子命令未破坏 serve 默认行为）PASS
- `go run ./bald gen store product --out ...` / `gen proto product` 生成骨架正常

### 关键经验
- cobra `CommandNotFound` 字段是 v1.8+ 才有；跨版本兼容用「预判 args[0] 是否已知命令 + exec.LookPath」更稳（避免依赖具体版本字段）。
- 同目录 package 冲突：新增命令包时先 `ls` 目标目录确认无同名/已占用 package（gen vs 已有 baldv1）。
- berrors 栈：原设计仅 With* 捕获，对齐"构造即捕获"更符合预期且 P3 目标；改动后既有 Is/Unwrap 测试无回归。
- 文档：`架构演进路线.md` 顶部加「落地状态总表」标记 P0-P4 已落地、P2.3 暂不新建。

## 代码审查后全量修复（P0-P4 复查发现的 6 项问题）

用户 "@command://cr 审查代码变更" 后要求"全修"。修复全部落地并验证全绿：

1. **🔴 flatten 丢弃 OR 语义**：`pkg/store/store.go` 原 `flatten` 静默把 `FilterExpr.Groups` 展平为 AND（丢失 `FilterExpr.Type` AND/OR）。改为报错（后端仅支持 AND 扁平）；proto 实际结构是 `FilterExpr.Type`(AND/OR)+`Groups []*FilterExpr`（非 `Logic` 字段，修正了初版误用 `GetLogic`/`FilterLogic_OR` 的编译错误）。补 `TestFlatten_RejectsOR`。
2. **🔴 NextToken 末页误发**：原 `translate` 无条件 `NextToken=encodeToken(off+ps)`，导致末页也返回 token、客户端空翻页。改为 `translate` 不预填，`fillTotal` 在 `total > offset+limit` 时才填 NextToken。补 `fillTotal` 末页判定测试。
3. **🟡 元数据字段类型统一**：`PageSize/NextToken/CurrentSize` 是原生指针，`Total/TotalPages/CurrentPage/CurrentOffset` 是 wrapperspb——保留 `uint32p`（原生）仅用于 PageSize，删误用。
4. **🟡 GORM ILIKE**：注释从不实"跨库可用 lower()"改为"仅字符串列安全 + 优先原生不区分大小写匹配 + lower() 兜底 SQLite"。
5. **🟡 codegen go_package**：`gen proto` 模板路径由 `.../codegen/{Name}/v1` 改为 `.../api/gen/go/bald/{Name}/v1`，与示例模块 + buf 生成目录对齐。
6. **🟡 gorm Update 零值/主键**：`Updates(obj)` 跳过零值且可能误改主键。改为 `Updates(toMapExcludeKey(obj))`（反射导出字段转 map、剔除主键列 `id`），写入零值且不动主键。
- **附带的 toColumn 真实 bug**：`"ID"`→`"i_d"` 导致 Update 报 `no such column: i_d`。修正 snake_case 算法（处理缩写：`ID`→`id`、`UserID`→`user_id`、`HTTPServer`→`http_server`）。此 bug 之前因 `Updates(obj)` 走模型自身列映射而未被暴露，改 map 更新后触发。

## P7/P8/P9 认证/授权/多租户/数据权限落地（架构演进路线补全实现）

用户要求"继续实现"P7/P8/P9。设计对齐文档 §0.5（依赖倒置、核心零耦合、proto-first）：

- **P8 多租户**（`pkg/store` + `pkg/contextx`）：`RegisterTenant(key, func(ctx)(string,bool))` 全局注册 + `mergeTenant` 在 `Store.translate` 自动注入租户等值条件（NoPaging 也注入，防跨租户泄漏）；业务已手写同名条件则不重复。`pkg/contextx` 加 `WithTenantID/TenantIDFromContext`。`Where.T(ctx)` 显式标记（可选）。默认内置 `tenant_id` 维度，从 `contextx.TenantID` 取值。
- **P7 核心** `pkg/authn`：`Authenticator` 接口（`Authenticate(ctx)`/`AuthenticateToken(string)`）+ 强类型 `AuthClaims`（Subject/TenantID/Name/Scopes/Roles/ExpiresAt/Issuer）+ `ContextWithAuthClaims`（同步写 contextx 的 user/tenant 键）+ `TokenExtractor`/`ContextWithToken`（传输层抽 token 存 ctx，核心不 import gin/grpc/jwt）。`pkg/authz`：`Authorizer` 接口 + `AllowAll`/`DenyAll`/`Func`（不 import casbin）。
- **P7 拦截器**：`pkg/middleware/{gin,grpc}` 的 `AuthnMiddleware`/`AuthzMiddleware`/`AuthnInterceptor`/`AuthzInterceptor` 改用 `authn.Authenticator`/`authz.Authorizer`（依赖倒置）。gin 从 header、grpc 从 metadata 抽 Bearer，经 `authn.ContextWithToken` 传入，拦截器判 401/403。替换了旧 `TokenExtractor`+`UserRetriever` 弱接口。
- **P7 桥接子模块** `bald-authn-jwt`（独立 go.mod，`replace => ../bald`）：实现 `authn.Authenticator`，用 `golang-jwt/jwt/v5`，强制 `HS256` 白名单（防 alg=none），无硬编码默认密钥（空 secret panic），`IssueToken` 供 login/测试。不 import gin/grpc（token 纯 string 解析）。测试通过。
- **P9 数据权限** `pkg/store.RegisterDataScope(fn(ctx, *authn.AuthClaims) []*FilterCondition)`：注册策略条件在 `translate` 的 `mergeDataScope` 注入（租户之后进一步收窄），即 go-crud Viewer 五级范围的依赖倒置实现。审计 Auditor 仍待业务需求。
- **架构文档** `架构演进路线.md` 更新 P7/P8 ✅已落、P9 🟡部分落；§3 树补 bald-authn-jwt[已落]。
- **提交**：`bald` 仓库 commit `5e81917`（pkg/authn+authz+store/scope+tenant+contextx+middleware+doc）。`bald-authn-jwt` 为 bald 仓库外的独立 module（与 bald-store-gorm 同况，非 git 仓库），仅 built+tested 通过，未 git 提交（需用户 `git init` 或纳入工作区仓库时再提交）。
- **注意**：`pkg/middleware` 的 Authn/Authz 签名已变（旧 `TokenExtractor`/`UserRetriever` → `authn.Authenticator`/`authz.Authorizer`），example 未直接使用故未受影响；若业务用了旧中间件需改调。

## 代码审查修复：P7/P8/P9 后续 6 项（2026-08-30）

CR 最近一次提交（`5e81917` P7/P8/P9 代码落地）发现 6 个 🟡 项，已全部修复：
1. **tenant.go**：不再 `init()` 隐式注册 `tenant_id`（改为业务显式 `RegisterTenant("tenant_id", DefaultTenantFunc)`）；`defaultTenantFunc` 重命名为导出 `DefaultTenantFunc`。
2. **tenant.go** `Where.T`：改为深拷贝（独立 Filters 切片 + 显式字段复制），避免共享底层数组串扰。
3. **tenant.go** `mergeTenant`：去重改为"业务已写该列任意 Op 即跳过注入"，避免 EQ+NEQ 叠加恒假。
4. **scope.go** `mergeDataScope`：过滤 nil / 空字段条件，防无效过滤进翻译期。
5. **gin/authn.go**：认证失败补 `log.GetLogger().Error`；移除 `claims.Expired()` 重复判断。
6. **grpc/authn.go**：移除 `claims.Expired()` 重复判断（过期校验下沉 `Authenticator` 契约）。
- `authn.Authenticator` 接口注释补充"实现必须校验 ExpiresAt 过期"。
- `where.go` 新增 `Ne` 辅助函数（测试用）。
- `tenant_test.go` 改为显式注册维度 + `t.Cleanup` 还原；新增 NEQ 不重复注入用例。
验证：`go build/test ./pkg/...` 全绿；`bald-authn-jwt`、`_example/bald` 独立模块构建+测试通过。

## 桥接子模块搬入 bald 仓库（2026-08-30）

将 `bald-authn-jwt`、`bald-store-gorm` 从 `konglingfei/` 根目录（bald 仓库外、未跟踪）移至 `bald/contrib/`（A1 方案：保持独立 go.mod + module 名不变，仅改 replace 相对路径）。
- 决策依据：路线文档 P5「核心零耦合、桥接子模块独立 go.mod」，故不合并进主 module。
- `contrib/authn-jwt/go.mod`、`contrib/store-gorm/go.mod` 的 `replace github.com/kalandramo/bald => ../..`（注意 authn-jwt 在 bald/contrib/authn-jwt/，父父级即 bald/，不是 ../../bald）。
- `_example/go.mod` 的 `replace github.com/kalandramo/bald-store-gorm => ../contrib/store-gorm`（_example 在 bald/_example/）。
- 验证：两 contrib 子模块 build+test 通过；_example build 通过。
- contrib/ 当前未 git add（待提交）。提交建议：本次仅 add contrib/ 与 _example/go.mod 改动，与前面 CR 修复分开或合并均可。

## go-bald-admin 重构计划（2026-08-30）

计划将 `go-wind-admin/backend`（gow/go-wind-toolkit 后台管理系统，553.go + 97.proto）用 bald 重构为官方范例，验证 P0–P9。
- 决策：放 `bald/examples/go-bald-admin/`（独立 go.mod，非 `_example`，普通目录名可纳入构建；module 名 `github.com/kalandramo/bald/examples/go-bald-admin`）。
- 只搬 backend（frontend 留原仓库）。
- 依赖选型（设计文档已定）：认证→bald-authn-jwt；授权→bald pkg/authz + 业务 RBAC；存储→bald-store-gorm(GORM 替代 ent)；配置→pkg/conf；DI→appkit 注册表(放弃 wire)；多租户/数据权限→store.RegisterTenant/RegisterDataScope。
- 明确不引入：ent/kratos/wire/源项目 pkg/jwt/pkg/authorizer/casbin/etcd-consul(jwt 期)。任务队列 asynq→MVP 本地抽象；redis_cache→MVP 本地缓存降级。
- 文档：`bald/examples/go-bald-admin/docs/需求文档.md` + `设计文档.md`（含选型映射表）。尚未 git 提交。
