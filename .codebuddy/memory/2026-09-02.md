# 2026-09-02

## R1-2 期望态 diff-apply 协调落地（提交待 push）

- **核心**：`pkg/appkit/reconcile.go` 新 `Reconcile(name, fn)` Option + `ReconcileCtx`（`Viper`/`Name`/`app` + `Mounted/Has/Mount/Unmount` 实际态原语，基于 A1）+ `runReconcilers(ctx, v)` 调用点（启动收敛期 afterStart 后 + keywatch 配置变更后）+ `DiffStrings(want,have)(add,remove)`（导出）+ `parseStringList` + 重组审计 `AuditEvent{Object:"reconciler"}`。`reconItems map[string][]reconItem` 由 `reconMu` 保护。
- **语义**：对齐 K8s controller——期望态写在配置，框架 diff 后只更新变动部分；部分失败留中间态，下次协调补全（自愈）。8 个契约测试 race-clean。
- **go-bald-admin 端到端范例**：移除 `main.go` 中 `BeforeStart` 静态 `audit.SetAuditor(MultiAuditor)`，改由 `reconcileAudit` 协调器以配置键 `audit.backends`（log|store|stream 逗号/空格分隔）为期望态；读 `InitBridges` 后的 DB/Redis 重建 `MultiAuditor`，与 `currentAuditBackends`（加锁）diff 仅变化时重建，旁路语义绝不阻断。configs 新增 `audit.backends: "store,stream"`；运行期改键经 WatchConfigFile→OnConfigChange→runReconcilers 自动热切审计后端。范例测试 `reconcile_audit_test.go`（解析/收敛/幂等）。
- 文档同步：架构优化路线.md（R1 状态✅ + R1-2 落地记录 + go-bald-admin 集成说明）、框架契约总览.md（Reconcile/DiffStrings/ReconcileCtx 契约）。
- **坑**：reconcile 函数签名是 `(ctx context.Context, rctx *ReconcileCtx) error`（两个入参，非单参）；ReconcileCtx 无 `ContextForLog()`，日志用第一参 ctx。
- 提交待 push（git 仍会警告 Large files：gen pb.go，未处理）。

## R1-2 升级：逐组件粒度 diff-apply（提交 d062f8d，已推送）

- 初版 `reconcileAudit` 是「整体重建 MultiAuditor」——未真正用 ReconcileCtx 原语。升级为**逐后端 Mount/Unmount**：协调器以 `rctx.Mounted()`（reconItems 维护的实际态）vs `audit.backends`（期望态）做 `DiffStrings`，`add`→`rctx.Mount(ctx, name, &auditBackendComponent{name, build})`、`remove`→`rctx.Unmount(ctx, name)`；每个 `auditBackendComponent.Start` 把自身 `audit.Auditor` 注册进共享 `reconAuditors` 表并刷新全局 `MultiAuditor`、`Dispose` 注销。完整兑现「只更新变动部分」。
- **新增测试原语**：`AppKit.SetRunningForTest()`（标记 running，供 MountComponent 不报错）、`appkit.NewReconcileCtx(app, name, v)`（构造器，因 `app` 字段未导出）。
- **踩坑（真 bug）**：初版组件 `Name()="audit:"+name` 导致 `want`(log) 与 `Mounted()`(audit:log) diff 不对称 → 重复挂载 + Unmount 名不匹配。修正：`Name()` 直接用后端名 `name`，与 `want` 对称；`Unmount` 传 `name` 非 `"audit:"+name`。测试用 `reconAuditors.set` 判空验证 nop 退回（capturedAuditor 自增会误判，已弃）。
- 范例测试 4 个：解析/挂载收敛/幂等/置空卸载退回 nop，race-clean。

## P12 第二步：AppSpec proto 方言驱动 gen app --spec（提交 f50d9b0，已推送）

- **proto 方言**：参考 go-wind-admin 的 `service/v1`+`config` 分层，新增 `api/proto/bald/appspec/v1/appspec.proto`：`AppSpec{meta,server,components[],capability,audit_backends[],bundle_normalized}`。把「应用装配」升格为单一 proto 真相源（取代模板+手填配置键双真相源）。经 `task proto-config`（= `api/proto` 下 `buf generate`）生成 `pkg/conf/gen/go/bald/appspec/v1`（**生成代码提交进仓库**，config/store 同此约定；非 gitignore）。
- **CLI**：`gen app <name> --spec <AppSpec.json>` 整合进既有 `genAppCmd`（加 `--spec` 分支，调用 `runGenAppSpec`→`renderAppSpec`）。读取 AppSpec（protojson）渲染 `appspecTmpl`，依 ServerSpec/ComponentSpec/CapabilitySpec/audit_backends 真实接线 appkit 全原语（S1 Provides/Requires、C1 MountComponent、A1 热插拔、R1-2 Reconcile("audit.backends")、P10 bundle.Normalized）。`capOpts` 收口所有 Option（含 `appkit.Servers(servers...)`）；`net/http` 仅 HTTP 启用时引入。
- **模板真 bug 修正**：① `capOpts..., appkit.Reconcile(...)` 非法——variadic 展开必须在末位，改为把 Reconcile 并入 capOpts 再 `appkit.New(capOpts...)`；② `MountComponent(ctx,name,comp)` 实际签名 `(ctx, comp)`；③ `Servers` 是 Option 非方法 `app.Servers`；④ `setLogger` 为 example 私有，改为 `baldlog.SetLogger(baldlog.NewSlogLogger(baldlog.NewOptions()))`。
- **端到端验证**：两种规格（http+grpc+组件+normalized / grpc-only+空组件）生成物均在独立 module（`replace` 指向本地 bald）内 `go build` 真实编译通过；新增测试 `TestGenAppSpec_TemplateFormats`/`TestGenAppSpec_GeneratedCompiles`（httpgrpc+grpconly 两分支）pass。
- **example 模块治理**：`_example/bald/go.mod` 模块路径须为 `github.com/kalandramo/bald/example/bald`（与代码内 import 前缀一致），`replace github.com/kalandramo/bald => ../..`。编译用 `GOFLAGS=-mod=mod go build`（example 不在核心 `go build ./...` 范围，Taskfile 有 `example-build` 任务）。

## 关键工具链约定（补充到长期记忆）

- **proto 生成**：必须用 `task proto-config`（核心契约）/`task proto-example`（示例 gRPC），别手敲 `buf generate --path`（部分子包静默不生成）。Taskfile 见 `bald/Taskfile.yml`。
- **example 模块**：`_example` 下划线目录被 Go 工具链忽略，需单独 `cd _example && GOFLAGS=-mod=mod go build/test`。
- **生成代码提交策略**：`pkg/conf/gen/go/**` 进仓库（非 gitignore），config/store/appspec 同此；改 proto 后务必 `task proto-config` 重新生成再提交。

## 设计评审：找出不合理设计（产出文档）
- 新建 `docs/devel/zh-CN/不合理设计评审.md`，逐条附 [严重度]+[文件:行]。已源码走读实测核验，非虚构。
- **P0 安全/正确性**：① `pkg/conf/bindflags.go:348-356` `MustTLSConfig` 失败降级 `InsecureSkipVerify:true`（安全反模式）；② `pkg/log/filter.go` `WithAttrs` 不过滤 → 脱敏被 `slog.With/WithAttrs` 绕过；③ gin `Authn` 失败 `Abort` 不调用 `c.Next`，`Audit` 注册在其后 → 401/403 永不审计（与 `pkg/audit/audit.go` 文档及范例注释自相矛盾）。
- **P1**：④ 审计后端三路径分裂（`bundle` 仅用注入否则 Nop、`appkit/mount.go:176` 与裸中间件走全局 `audit.GetAuditor`），`appkit` 无 Auditor Option；⑤ gRPC 链无 recovery 且 `grpc/audit.go:116` `metrics.Record` 在 `recordSafely` 外，panic 逃逸（违反「旁路不阻断」）；⑥ P9 归一化 `bundle.Normalized()` 默认 false，非默认生效；⑦ 中间件构造期快照 `log.GetLogger()`（observability.go:135/139），范例 `setLogger` 重建后请求日志仍用旧实例（main.go:63/128/262 时间线）。
- **P2**：`AuditMiddleware(_, opts)` 首参死参（gin:46/grpc:55）；HTTP 错误体形状不统一（`web.ErrorResponse` 的 {error:{code,message}} vs authn 的 {reason,message}）；`ResolveTLS` 提供 CA 即 `RequireAndVerifyClientCert`（误触发 mTLS）。
- **P3**：`pkg/config/config.go:260` `Marshal` 无调用方（死代码）；`pkg/conf` 耦合 `pkg/log` 而 `pkg/store` 去耦，风格不一。
- 注：记录为「设计问题清单」，尚未修改代码；用户未要求修复。

## 可精简代码评审（追加到 不合理设计评审.md 第五章）
- 在同文档追加「五、可精简代码（冗余/死代码）」E1-E11，均经全仓 `search_content` 调用点核验。
- **直接可删（高/中危，零调用纯转发）**：E1 gin/grpc 共 15 个 `ObservabilityWithXxx` 便捷函数 + 其独占 setter（`pkg/middleware/gin|grpc/observability.go`，仅定义+文档表，零调用）；E2 `pkg/conf` 的 `ValidateAddress`/`MustTLSConfig`/`cfgToLogOptions`（纯转发死支）；E3 `pkg/config.Marshal`(`config.go:260`)+整个 `encoder.go`（零调用）；E4 `bindflags.go` 死变量 `_ = d` 与重复 doc；E6 `gin/logging.go` `RequestID()` 别名去重。
- **可合并**：E5 gin/grpc 可观测性平行实现（`TraceInjectionMode`/常量/`Option`/`matchWildcard`/`injectTrace` 逐行重复）→ 抽 `pkg/middleware/shared`；E11 appkit 内部重复（`auditComponent`↔`auditReconcile`、组件 start 错误包裹串、`store/tenant.go`↔`scope.go` 注册范式）。
- **需决策（预留脚手架/可选层，删减负担但留扩展性）**：E7 `appkit/registry.go` 的 `ServerRegistry`/`MiddlewareRegistry`(0 引用)/`ProviderRegistry`(仅测试)/`RegisterStoreProvider`(仅注释)/`NewRegistry[T]`(仅测试) 纯未接线脚手架；E8 appkit 7 个零调用 Option（`ID`/`KratosRegistrar`/`Env`(仅被注释)/`BeforeStopTimeout`/`AfterStopTimeout`/`EffectTimeout`/`ComponentTimeout`）；E9 `pkg/store/mapper.go` 整包（DTO/Entity 可选映射层零采用，仅测试）；E10 store 零散零调用（`WithPageSize/WithMaxPageSize/WithLogger`/`Provider()`/`FromLogger`/`logAdapter`、`contextx.UsernameFromContext` 写入端用但读取端无调用）。
- 用户仅要求「记录到文档」，尚未改动代码。

## 评审文档正确性二次核验 + 执行计划（追加第六/七章）
- **二次核验结论**：D1-D12 / E1-E11 共 23 项全部证实，无虚构。修正 1 处笔误（D3 原文把 gRPC 链序误标为 "gin 链序"，已改为双链各自正确）；精确化 2 处边界：① D2 绕过仅限 `With`/`WithAttrs` 固化路径（`slog.go:189` slogLogger.With→slog.Logger.With→handler.WithAttrs，filterHandler.Handle 只过滤 record 自带 attrs），日志直接参数仍被过滤；② D3 缺口精确为「受保护路由 authn abort（401）+ gRPC Unauthenticated」，无 Authn 路由（/v1/login）的业务 401 仍被全局链 Audit 记录。
- **D6 加重证据**：`grpc/audit_test.go:150-164` `TestAuditInterceptor_PanickingAuditorSafe` 把 panicAuditor 传给被丢弃的首参 → 实际执行全局 nop → 测试对 recover 零覆盖而通过（假通过实证）。
- **D5 证实**：pkg 下仅 gin/recovery.go 有 Recovery，gRPC 全无。
- **执行计划（文档第七章）**：阶段 0 基线 → 阶段 1 P0（D1 删 MustTLSConfig / D2 WithAttrs 先过滤 / D3 authn 失败显式发审计事件，不采用 Audit 外移方案因 gRPC ctx 不外传）→ 阶段 2 P1（D4 appkit.Auditor Option+resolveAuditor / D5 safeEmit+自写 20 行 gRPC recovery / D7 normalized 默认 true+Raw() opt-out / D8 Logger 请求期惰性）→ 阶段 3 P2 破坏性集中（D6 签名去首参+修假通过测试 / D9 统一 ErrorBody / D10 proto 拆 root_ca/client_ca，task proto-config 重新生成）→ 阶段 4 纯删（E1-E4/E6+契约总览.md:270 清理）→ 阶段 5 决策项（E5/E7-E11 独立 PR 不阻塞）→ 阶段 6 全量验证+文档同步+打 v0.2.0。破坏性变更清单与迁移路径已列。

## 执行落地（2026-09-02，阶段 0-4+6 完成，阶段 5 决策项未动）
- **P0**：删 `MustTLSConfig`；`filterHandler.WithAttrs` 先过滤再下沉（+2 测试）；gin/grpc `Authn` 失败发 `ResultDeny` 审计事件（新 `AuthnWithAuditor` option，bundle 注入 `b.auditor`，事件 `{Object:authn,Action:authenticate}`，Meta 带 reason/path/full_method）。
- **P1**：`appkit.Auditor` Option + `resolveAuditor()`（注入→全局→nop；mount/reconcile 审计同源并补 TenantID）；新 `pkg/middleware/grpc/recovery.go` `RecoveryInterceptor`（panic→`berrors.Internal("PANIC")`，bundle 链 Error→**Recovery**→...；空链长度断言 3→4）；`emitMetricsSafely`（gin/grpc metrics 纳入 recover）；`bundle.New` 默认 `normalized:true` + 新 `Raw()` opt-out；observability 各加 `resolveLogger()` 惰性解析。
- **P2**：`AuditMiddleware/AuditInterceptor` 签名去死参（bundle+测试+示例同步；假通过测试改 `AuditWithAuditor(bad)` 真实覆盖 recover）；authn 401 响应改 `web.ErrorBody{error:{code,message}}`；proto `Tls` 增 `root_ca=6`/`client_ca=7`（`ca=3` 弃用别名），`ResolveTLS`：root_ca/ca→RootCAs 不再触发 mTLS，client_ca→ClientCAs+RequireAndVerifyClientCert。
- **P3**：删 15 个便捷函数+独占 setter（**保留 WithLogger=显式注入入口、gin.WithSkipMetrics=Logging() 依赖**）；删 `config.Marshal`+`encoder.go`；bindflags 死变量+重复 doc 合并；删 `RequestID()` 别名（统一 `RequestIDMiddleware`，bundle/_example 调用点同步）。
- **关键坑（长期有效）**：① `examples/**/go-bald-admin` 在 bald/.gitignore 中，ripgrep/search_content 尊重 gitignore → **对该目录搜索盲区**，影响面排查必须 `grep -r` 兜底（D6 签名变更漏掉 `grpc_audit_e2e_test.go:49` 旧签名 nil 变首参 → nil deref panic，go-bald-admin race test 抓出后修复）；② 删"独占 setter"前先确认无语义依赖（WithLogger 是 D8 显式注入唯一入口）。
- 验证：核心 build+vet+race 全绿；_example/bald、examples/go-bald-admin、5 个 contrib module 全绿；改动文件 gofmt 过；`框架契约总览.md` 已同步 D3/D4/D5/D6/D7/D8 契约（grpc 链序 Error→Recovery→...、bundle 默认归一化、appkit.Auditor 行、authn 审计行、删便捷变体行）。未提交（等用户指令）。
