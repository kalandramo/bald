# 2026-09-02

## R1-2 期望态 diff-apply 协调落地（提交待 push）

- **核心**：`pkg/appkit/reconcile.go` 新 `Reconcile(name, fn)` Option + `ReconcileCtx`（`Viper`/`Name`/`app` + `Mounted/Has/Mount/Unmount` 实际态原语，基于 A1）+ `runReconcilers(ctx, v)` 调用点（启动收敛期 afterStart 后 + keywatch 配置变更后）+ `DiffStrings(want,have)(add,remove)`（导出）+ `parseStringList` + 重组审计 `AuditEvent{Object:"reconciler"}`。`reconItems map[string][]reconItem` 由 `reconMu` 保护。
- **语义**：对齐 K8s controller——期望态写在配置，框架 diff 后只更新变动部分；部分失败留中间态，下次协调补全（自愈）。8 个契约测试 race-clean。
- **go-bald-admin 端到端范例**：移除 `main.go` 中 `BeforeStart` 静态 `audit.SetAuditor(MultiAuditor)`，改由 `reconcileAudit` 协调器以配置键 `audit.backends`（log|store|stream 逗号/空格分隔）为期望态；读 `InitBridges` 后的 DB/Redis 重建 `MultiAuditor`，与 `currentAuditBackends`（加锁）diff 仅变化时重建，旁路语义绝不阻断。configs 新增 `audit.backends: "store,stream"`；运行期改键经 WatchConfigFile→OnConfigChange→runReconcilers 自动热切审计后端。范例测试 `reconcile_audit_test.go`（解析/收敛/幂等）。
- 文档同步：架构优化路线.md（R1 状态✅ + R1-2 落地记录 + go-bald-admin 集成说明）、框架契约总览.md（Reconcile/DiffStrings/ReconcileCtx 契约）。
- **坑**：reconcile 函数签名是 `(ctx context.Context, rctx *ReconcileCtx) error`（两个入参，非单参）；ReconcileCtx 无 `ContextForLog()`，日志用第一参 ctx。
- 提交待 push（git 仍会警告 Large files：gen pb.go，未处理）。

## R1-2 升级：逐组件粒度 diff-apply（提交 d062f8d，已推送）

- 初版 `reconcileAudit` 是「整体重建 MultiAuditor」——未真正用 ReconcileCtx 原语。升级为**逐后端 Mount/Unmount**：协调器以 `rctx.Mounted()`（reconItems 维护的实际态）vs `audit.backends`（期望态）做 `DiffStrings`，`add`→`rctx.Mount(ctx, name, &auditBackendComponent{name, build})`、`remove`→`rctx.Unmount(ctx, name)`；每个 `auditBackendComponent.Start` 把自身 `audit.Auditor` 注册进共享 `reconAuditors` 表并刷新全局 `MultiAuditor`、`Dispose` 注销。完整兑现「只更新变动部分」。
- **新增测试原语**：`AppKit.SetRunningForTest()`（标记 running，供 MountComponent 不报错）、`appkit.NewReconcileCtx(app, name, v)`（构造器，因 `app` 字段未导出）。
- **踩坑（真 bug）**：初版组件 `Name()="audit:"+name` 导致 `want`(log) 与 `Mounted()`(audit:log) diff 不对称 → 重复挂载 + Unmount 名不匹配。修正：`Name()` 直接用后端名 `name`，与 `want` 对称；`Unmount` 传 `name` 非 `"audit:"+name`。测试用 `reconAuditors.set` 判空验证 nop 退回（capturedAuditor 自增会误判，已弃）。
- 范例测试 4 个：解析/挂载收敛/幂等/置空卸载退回 nop，race-clean。

## P12 第二步：AppSpec proto 方言驱动 gen app --spec（提交 f50d9b0，已推送）

- **proto 方言**：参考 go-wind-admin 的 `service/v1`+`config` 分层，新增 `api/proto/bald/appspec/v1/appspec.proto`：`AppSpec{meta,server,components[],capability,audit_backends[],bundle_normalized}`。把「应用装配」升格为单一 proto 真相源（取代模板+手填配置键双真相源）。经 `task proto-config`（= `api/proto` 下 `buf generate`）生成 `pkg/conf/gen/go/bald/appspec/v1`（**生成代码提交进仓库**，config/store 同此约定；非 gitignore）。
- **CLI**：`gen app <name> --spec <AppSpec.json>` 整合进既有 `genAppCmd`（加 `--spec` 分支，调用 `runGenAppSpec`→`renderAppSpec`）。读取 AppSpec（protojson）渲染 `appspecTmpl`，依 ServerSpec/ComponentSpec/CapabilitySpec/audit_backends 真实接线 appkit 全原语（S1 Provides/Requires、C1 MountComponent、A1 热插拔、R1-2 Reconcile("audit.backends")、P10 bundle.Normalized）。`capOpts` 收口所有 Option（含 `appkit.Servers(servers...)`）；`net/http` 仅 HTTP 启用时引入。
- **模板真 bug 修正**：① `capOpts..., appkit.Reconcile(...)` 非法——variadic 展开必须在末位，改为把 Reconcile 并入 capOpts 再 `appkit.New(capOpts...)`；② `MountComponent(ctx,name,comp)` 实际签名 `(ctx, comp)`；③ `Servers` 是 Option 非方法 `app.Servers`；④ `setLogger` 为 example 私有，改为 `baldlog.SetLogger(baldlog.NewSlogLogger(baldlog.NewOptions()))`。
- **端到端验证**：两种规格（http+grpc+组件+normalized / grpc-only+空组件）生成物均在独立 module（`replace` 指向本地 bald）内 `go build` 真实编译通过；新增测试 `TestGenAppSpec_TemplateFormats`/`TestGenAppSpec_GeneratedCompiles`（httpgrpc+grpconly 两分支）pass。
- **example 模块治理**：`_example/bald/go.mod` 模块路径须为 `github.com/kalandramo/bald/example/bald`（与代码内 import 前缀一致），`replace github.com/kalandramo/bald => ../..`。编译用 `GOFLAGS=-mod=mod go build`（example 不在核心 `go build ./...` 范围，Taskfile 有 `example-build` 任务）。

## 关键工具链约定（补充到长期记忆）

- **proto 生成**：必须用 `task proto-config`（核心契约）/`task proto-example`（示例 gRPC），别手敲 `buf generate --path`（部分子包静默不生成）。Taskfile 见 `bald/Taskfile.yml`。
- **example 模块**：`_example` 下划线目录被 Go 工具链忽略，需单独 `cd _example && GOFLAGS=-mod=mod go build/test`。
- **生成代码提交策略**：`pkg/conf/gen/go/**` 进仓库（非 gitignore），config/store/appspec 同此；改 proto 后务必 `task proto-config` 重新生成再提交。
