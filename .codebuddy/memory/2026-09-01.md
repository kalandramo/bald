# 2026-09-01

## bald 文档整理（提交 4dbc7a0，已推送）
逐文件审查 `docs/` 全部 14 个 devel 文档 + guide 目录，修过期/错误/废弃内容：

**已修（6 文件 +114/-102）**：
- `guide/quickstart`：`example/bald`→`_example/bald`（路径错）；删除已废弃 `pkg/options` 的 `Unmarshal(&opts)` 示例，改 proto 契约 `Unmarshal+Validate`。
- `guide/introduction`：补五支柱可观测/可组合性/contrib 三项核心特性；架构概览补 Component/Effect/Capability。
- `应用框架设计`：状态「设计中」→「已落地」；停机三阶段→五阶段（补效应回放+组件Dispose）；Options 表补 6 个第二轮 Option；迭代清单改写为闭环记录。
- `架构演进路线`：§2 P0-P4 改已落地状态；§3 总览补 bundle/audit/metrics/testkit + 3 个 P11 contrib；P7 删未实现的 `CreateIdentity` 虚假目标、修正首范本 `bald-authn-oidc`→`bald-authn-jwt`。
- `配置中心设计`：§12 删废弃 `pkg/options` 描述。
- `README 索引`：描述对齐最新内容。

**审查后保留（仍有效）**：proto 配置契约设计、错误模型设计、服务端设计、日志设计、服务注册设计、路由注册与绑定设计、数据存储设计、grpc-gateway 配置与 transcoding、框架契约总览、架构优化路线（前次已同步）。

**发现并修正的典型问题类型**：① 路径引用错（example→_example）；② 已废弃 API 仍在示例中（pkg/options Unmarshal）；③ 状态标「设计中」但实际已落地（应用框架设计）；④ 迭代清单把已落地项标为待办；⑤ 架构总览缺 P10 后新包；⑥ 未实现的 API 写进目标（CreateIdentity）。

## slog 能力迁移复核（提交 8f6d24b，已推送）
复核 `docs/devel/zh-CN/slog 日志能力接入路线.md` 对照 `pkg/log` 实际实现：
- **阶段 1（文件轮转）已完整落地**：`slog.go` `openWriter` 接 lumberjack、`RotateOptions` 完整（MaxSize/MaxAge/MaxBackups/Compress/Enabled）、log.proto 有 `RotateOptions`（在 `api/proto/bald/config/v1/log.proto`，非 pkg/conf 目录）、flags `--log.rotate.*` 齐、rotate_test.go 测试齐、lumberjack 已转 direct 依赖。
- **阶段 2（P3 交付）已落地**：slog 门面/全局/Context/FilterKey 脱敏/otel hook/Options 全部在。
- **阶段 3 全部未做（文档本就标可选，非欠账）**。
- **修正 4 处过度声明**：① 文档能力表+阶段 1 声称「按大小/时间」轮转——**lumberjack 只支持按大小切割**（MaxAge/MaxBackups 只管备份清理时限/份数），gookit rotatefile 的 RotateTime（按天/小时）能力未迁移，确需时引 `github.com/gookit/rotatefile` 或系统 logrotate；② 阶段 3「分级 Handler 由 OutputPaths 多目标已能实现」是错的——OutputPaths 多目标是**复制分流**（multiWriter 每目标收全量），非级别分流，按级别落文件需多个不同 Level handler 各挂独立 writer（WithHandler 已支持）或新增 LevelWriters 配置；③ log.proto/options.go 同步修正注释；④ **本地 protoc-gen-go v1.36.10→v1.36.12**（对齐既有生成代码，避免 buf generate 版本号倒退——教训：全量重新生成前先 diff 检查生成器版本，版本不齐会污染所有 pb.go）。
- 注：go-kit/slog 目录在 workspace 根（`konglingfei/slog/`，gookit/slog fork），RotateTime 能力在那边。

## M10 运行时可重组落地（提交 2dd3040，已推送）
go-bald-admin 接入 P10/A1 实际使用 + 修复真实 bug：
- **M10.1 bundle 等价替换**：gin 全局层（Recovery→RequestID→Logging→Audit）+ gRPC 全链（Error→RequestID→Observability→Authn→Audit→Authz）切 `pkg/middleware/bundle`；gin Authn/Authz 保持路由级分组保护（/v1/login 公开）——全局链与分组保护是两种合法模式；增强：/v1/login 也进审计。
- **M10.2 管理面端点**：`/admin/components` GET/POST/DELETE（authn+authz admin 资源保护），工厂目录业务定义、`demo.heartbeat` 演示 goroutine 组件生命周期，挂载/卸载自动产生 A1 重组审计。
- **修复真实时序 bug**：`RegisterRoutes` 传构造期 nil `bootstrappkg.Authenticator`，gin AuthnMiddleware 对 nil 退化 no-op——**main 进程 HTTP 认证一直未生效**（e2e 不走 main 装配路径未暴露；gRPC 靠延迟闭包幸免）。修复：`bootstrappkg.LazyAuthenticator/LazyAuthorizer`（请求期读最新值）。
- e2e 4 用例（真 AppKit Run 存活期验证挂载/卸载全链）。**cleanup 死锁坑**：`cancel` 与 `<-app.Done()` 分开注册 t.Cleanup 会因 LIFO 变「先等 Done 后 cancel」互等（卡满 timeout 600s），必须合并单个 cleanup 先 cancel 再等。
- **R1 期望态 diff-apply 复绪条件达成**（管理面实践里程碑）。剩余：R1-2 diff-apply、P12 gen app（bundle 接入形状已为模板提供素材）。
- git 推送警告 Large files detected（gen/secretv1 pb.go 类）——后续可考虑 .gitignore gen/ 或 git-lfs，未处理。
