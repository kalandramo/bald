version: v2
# 框架级配置契约。无外部依赖：仅使用 protobuf 官方 well-known types（WKT），
# 由 buf 内置提供，因此 buf generate 无需联网拉取依赖。
modules:
  - path: .
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH
breaking:
  use:
    - FILE
