# Bald 启动器配置系统

提供强大的 YAML 配置系统，支持多文件、环境变量、热更新和动态配置中心。

## 1. 配置结构

### 1.1. 完整配置模版

## 2. 环境变量

### 2.1. ${VAR}语法

### 2.2. 默认值

### 2.3. 从 `.env` 加载

Bald 自动加载 .env 文件：

```bash
# .env
BALD_APP_NAME=bald-demo
BALD_APP_VERSION=v1.0.0
```

## 3. 多环境配置

### 3.1. 配置继承

### 3.2. 环境选择

## 4. 配置层级合并

## 5. 动态配置

### 5.1. 监听配置变更

### 5.2. 配置中心集成

## 6. 配置校验

## 7. 敏感信息加密

|方式|安全级别|说明|
|---|---|---|
|.env| 文件|	中|	加入 .gitignore|
|环境变量|	高|	K8s Secret / Docker Secrets|
|Vault|	最高|	HashiCorp Vault 动态注入|
|配置中心加密|	高|	Nacos/Apollo 加密配置|

## 8. 程序内访问配置