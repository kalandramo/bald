# 框架契约总览（API Surface）

> 本文档是 `bald` 框架**所有公开契约**的速查表：导出接口、类型、函数、常量及其签名。
> 目的不是替代各设计文档，而是让使用者一眼看清「能用什么、签名是什么、怎么接入」。
> 所有包路径前缀为 `github.com/kalandramo/bald/pkg/`。
>
> 设计主线（详见各设计文档）：**核心零后端耦合**，注册中心 / 配置中心 / 校验 / 追踪等
> 可选能力均通过「最小接口 + 桥接适配器」注入，核心不绑定任何具体实现。

---

## 0. 模块与依赖层级

```
appkit        ← 编排层（组合 server/registry/config/log + 能力解析/组件/效应账本/挂载，最上层）
 ├─ server      服务端抽象（HTTP/gRPC/Gateway）
 ├─ registry    服务注册抽象 + FromKratos 桥接
 ├─ config      配置加载器 + RemoteSource 抽象 + FromKratosSource 桥接
 ├─ conf        框架配置 Protobuf 契约（默认值/校验/flag 绑定）
 ├─ web         gin HTTP 处理流水线（Handler/Validator/响应）
 ├─ middleware  gin / grpc 中间件（依赖倒置接口 + 实现）
 │    └─ bundle   横切关注点门面（双传输链序固化，P10）
 ├─ berrors     传输中立错误模型（核心包零依赖）
 │    ├─ grpcerr  gRPC 边界转换（可选）
 │    └─ httperr  HTTP 边界转换（可选）
 ├─ log         结构化日志（Logger/Options/kvs/Context）
 ├─ authn       认证抽象（Authenticator/AuthClaims/ctx 注取）
 ├─ authz       授权抽象（Authorizer + P9 传输中立归一化）
 ├─ audit       审计抽象（Auditor/AuditEvent/全局注入点）
 ├─ metrics     指标抽象（Recorder/Event，otel API 封装）
 ├─ validation  命令式校验分发器（依赖倒置）
 ├─ store       泛型数据访问层（零引擎依赖 + 多租户）
 ├─ testkit     测试工具（FreeAddr 等）
 └─ contextx    请求上下文键（trace_id / user / vars / tenant_id）

contrib/       独立 module 桥接（单向依赖核心，按需引入）：
  bald-authn-jwt / bald-store-gorm / bald-cache-redis /
  bald-authz-casbin / bald-observability-otlp
```

---

## 1. `pkg/appkit` — 应用编排

| 符号 | 签名 | 说明 |
|---|---|---|
| `AppKit` | `type AppKit struct`（非导出字段） | 应用编排器；提供 `Run(ctx)` / `Viper()` / `Config()` / `Logger()` / `Endpoints()` / `Scheme()` |
| `New` | `func(bootstrap *confv1.Bootstrap, opts ...Option) (*AppKit, error)` | 构造；`bootstrap` 来自 `conf.NewBootstrap()`；`opts` 见下 |
| `(*AppKit).Run` | `func(ctx context.Context) error` | 启动所有 server + 注册 + 优雅停机；内部有 CAS 防重入 |
| `(*AppKit).Viper` | `func() *viper.Viper` | 返回合并后的 viper 实例（BeforeStart 内 `Unmarshal` 用） |
| `(*AppKit).Config` | `func() *confv1.Bootstrap` | 返回已加载的配置 |
| `(*AppKit).Logger` | `func() *log.Logger` | 返回日志句柄 |
| `(*AppKit).Endpoints` | `func() []server.Endpoint` | 返回各 server 的监听端点（供注册中心用） |
| `(*AppKit).WaitReady` | `func(ctx, timeout) error` | 等待所有 server 就绪（探针/readyz 用） |
| `(*AppKit).Resolve` | `func() error` | S1 能力声明校验（Run 启动早期自动调用；缺失聚合报错） |
| `(*AppKit).UndoEffects` | `func(parent context.Context)` | T1 效应账本立即逆序回放并清空（幂等；e2e `t.Cleanup` 隔离用；Run 停机自动调用） |
| `(*AppKit).MountComponent` | `func(ctx, Component) error` | A1 运行期挂载：Start + 纳入停机序列 + mount 审计；`ErrNotRunning`/已销毁拒绝 |
| `(*AppKit).UnmountComponent` | `func(ctx, name string) error` | A1 运行期卸载：移除 + 立即 Dispose（LIFO）+ unmount 审计 |
| `(*AppKit).ListComponents` | `func() []string` | 当前停机序列中的组件名（启动/挂载序，观测用） |

**停机五阶段（stopAll）**：0 效应账本逆序回放 → 1 BeforeStop → 2 Server.Stop 并发 → 3 AfterStop → 4 组件逆序 Dispose（装配期 + 运行期挂载统一处理；`disposed` 闭环拒绝迟挂载）。

**函数式选项 `Option = func(*AppKit)`：**

| 选项 | 签名 | 说明 |
|---|---|---|
| `ID` / `Name` / `Version` | `func(string) Option` | 应用元信息 |
| `Registrar` | `func(r registry.Registrar) Option` | 注入 bald 注册中心 |
| `KratosRegistrar` | `func(r kratosregistry.Registrar) Option` | 桥接 kratos 注册中心（= `Registrar(registry.FromKratos(r))`） |
| `RemoteConfig` | `func(src config.RemoteSource) Option` | 注入远程配置源 |
| `ConfigFile` | `func(f string) Option` | 指定 `--config` 路径 |
| `Env` | `func(env string) Option` | 运行环境（影响默认文件名 / 远程 path） |
| `WatchConfigFile` | `func(watch bool) Option` | 本地文件热更新 |
| `OnConfigChange` | `func(fn func(*viper.Viper)) Option` | 配置变更回调 |
| `Servers` | `func(servers ...server.Server) Option` | 注入 server 列表 |
| `StopTimeout` | `func(d time.Duration) Option` | 优雅停机超时 |
| `BeforeStart` / `AfterStart` / `BeforeStop` / `AfterStop` | `func(fn func(context.Context) error) Option` | 生命周期钩子 |
| `Bind` | `func(prefix string, opt any) Option` | 注册业务配置 flag；`opt` 为 `proto.Message`（conf.BindFlags）或 `PlainBinder`（AddFlags，prefix 必须为空） |
| `PlainBinder` | `interface { AddFlags(fs *pflag.FlagSet) }` | 无前缀 flag 注册器（如 `log.Options` 内部注册 `--log.*`） |
| `Effect` | `func(name string, undo func(context.Context) error) Option` | T1 效应账本：登记全局写入的逆操作（停机阶段 0 逆序回放；panic 隔离、独立超时） |
| `EffectTimeout` | `func(d time.Duration) Option` | 单条 undo 的独立超时（默认同钩子超时） |
| `Provides` | `func(caps ...string) Option` | S1 声明进程提供的能力（重复声明报错） |
| `Requires` | `func(component string, caps ...string) Option` | S1 声明组件依赖的能力（缺失 fail-fast；重复幂等） |
| `Components` | `func(comps ...Component) Option` | C1 注册进程内组件（顺序 Start、停机逆序 Dispose） |
| `ComponentTimeout` | `func(d time.Duration) Option` | 组件 Start/Dispose 独立超时 |
| `OnKeyChange` | `func(key string, fn func(old, new string)) Option` | R1 key 级配置订阅：仅当该 key 值确实变化才触发；与 `OnConfigChange` 共存（先全量后分发）；基线=首次加载值 |

**`Component` 接口（C1）**：`interface { Name() string; Start(ctx) error; Dispose(ctx) error }`——进程内基础设施（trace provider/metrics exporter/审计后端连接等）。`ComponentFunc(name, dispose)` 适配纯清理函数。`server.Server` = Component + Endpoint。

**`Registry[T]` 泛型注册表**：`NewRegistry[T]()` / `Register(name, impl)`（装配期，不可逆，冲突报错）/ `Get(name) (T, bool)` / `Mount(name, impl) (undo func(), err error)`（A1 运行期可逆挂载，undo 幂等）/ `Unmount(name) (T, bool)`。

> 关键约束：`Bind("http", &bootstrap.Http)` 的 prefix 与 viper 键（`http.addr`）、
> 环境变量（`NAME_HTTP_ADDR`）、文件键四者一致，flag 才能压过文件/远程。
> 构造后不要再自行 `AddFlags(pflag.CommandLine)`，否则重复注册。

---

## 2. `pkg/server` — 服务端抽象

| 符号 | 签名 | 说明 |
|---|---|---|
| `Server` | `interface { Run(ctx) error; Stop(ctx) error; Endpoint() (Endpoint, error); Scheme() string }` | server 统一接口 |
| `Endpoint` | `type Endpoint struct { Address string; Scheme string }` | 监听端点（注册中心消费） |
| `EndpointFunc` | `type EndpointFunc func() (Endpoint, error)` | 端点惰性获取（避免构造期锁死地址） |
| `ServiceEndpoint` | `type ServiceEndpoint struct { Key string; Value Endpoint }` | 注册中心用的 key/value |
| `NewFromProto` | `func(bs *confv1.Bootstrap, opts ...ServerOption) (*ServerX, error)` | 从 proto 配置批量构造 HTTP/gRPC/Gateway server 集合 |
| `ServerOption` | `type ServerOption func(*ServerOptions)` | server 构造选项 |
| `NewGRPCServerWithRegister` | `func(cfg *confv1.Grpc, register GRPCRegister, opts ...ServerOption) (*GRPCServer, error)` | gRPC server；`register` 注入 proto 服务 |
| `NewHTTPServer` | `func(cfg *confv1.Http, opts ...ServerOption) (*HTTPServer, error)` | HTTP(server) server |
| `NewGatewayServer` | `func(http *confv1.Http, grpcBackend *confv1.Grpc, register GatewayRegister, opts ...ServerOption) (*GatewayServer, error)` | grpc-gateway 反向代理；延迟解析后端地址 |
| `(*GRPCServer).Endpoint` | 实时读 `s.cfg.GetAddr()` | **不缓存**构造期快照（env 覆盖后才写回） |
| `(*GatewayServer).Start` | 延迟建 `conn` + `attachHandler`，持锁完成即释放再 `Serve` | 防与 `Stop` 死锁 |

> `GRPCRegister` / `GatewayRegister` 由调用方注入（依赖倒置）；核心不依赖具体的 proto 服务生成代码。

---

## 3. `pkg/registry` — 服务注册

| 符号 | 签名 | 说明 |
|---|---|---|
| `Registrar` | `interface { Register(ctx, *ServiceInstance) error; Deregister(ctx, *ServiceInstance) error }` | 注册中心抽象 |
| `Discovery` | `interface { GetService(ctx, name string) ([]*ServiceInstance, error) }` | 发现抽象（可选，kratos nacos 实现） |
| `ServiceInstance` | `struct { Name, Host, Port string; Metadata map[string]string }` | 注册实例 |
| `FromKratos` | `func(r kratosregistry.Registrar) Registrar` | kratos → bald 桥接适配器 |
| `inmemory.New` | `func() Registrar` | 内存实现（e2e 演示用，零依赖） |

> 注册中心后端（etcd/consul/nacos）均经 `FromKratos` 接入，核心不绑定。

---

## 4. `pkg/config` — 配置加载器

| 符号 | 签名 | 说明 |
|---|---|---|
| `RemoteSource` | `interface { Load() ([]*config.KV, error); Watch() (Watcher, error) }` | 远程配置源抽象（`KV{Key, Value, Format}`） |
| `Watcher` | `interface { Next() ([]*config.KV, error); Stop() error }` | 变更监听 |
| `FromKratosSource` | `func(src kratosconfig.Source) RemoteSource` | kratos config.Source → bald 桥接 |
| `Load` | `func(opts *Options) (*viper.Viper, error)` | 四源合并：flag > env > 文件 > 远程 |
| `NewConfig` | `func(opts *Options) (*viper.Viper, error)` | 同 `Load`（别名） |
| `NewBootstrap` | `func() *confv1.Bootstrap` | 默认值（委托 `conf.NewBootstrap`） |
| `Unmarshal` | `func(v *viper.Viper, msg proto.Message) error` | viper → proto（桥接点） |
| `MarshalYAML` | `func(msg proto.Message) ([]byte, error)` | proto → YAML（生成远端基准配置用） |
| `MapToMessage` | `func(m map[string]any, msg proto.Message) error` | map → proto |
| `NewInMemory` / `NewFile` / `NewEtcd` / `NewNacos` | 源构造辅助 | 本地/远端源 |

> `Load` 的 `BindPFlags` 仅对 `flags.Changed == true` 的 flag 生效，避免零值 flag 压过 env/文件。

---

## 5. `pkg/conf` — 框架配置契约（Protobuf）

| 符号 | 签名 | 说明 |
|---|---|---|
| `NewBootstrap` | `func() *confv1.Bootstrap` | 默认值注入（proto 注解 + post-process） |
| `Validate` | `func(cfg *confv1.Bootstrap) error` | 一次性返回所有校验问题 |
| `BindFlags` | `func(fs *pflag.FlagSet, msg proto.Message, prefix string) error` | 遍历 proto 描述符注册带前缀 flag |
| `ResolveTLS` | `func(cfg *confv1.Tls) (*tls.Config, error)` | proto Tls → `tls.Config` |
| `Scheme` | `func(cfg *confv1.Tls) string` | 返回 `"https"` 或 `"http"` |
| `LogOptions` | `func(l *confv1.Logger) *log.Options` | proto Logger → `log.Options` |
| `ValidateAddress` | `func(addr string) error` | 地址格式校验（导出） |

> 契约类型 `confv1.Bootstrap / App / Http / Grpc / Logger / Tls` 由
> `api/proto/bald/config/v1/*.proto` 生成到 `pkg/conf/gen/go/`，server 层直接消费。

---

## 6. `pkg/web` — gin HTTP 处理流水线

| 符号 | 签名 | 说明 |
|---|---|---|
| `Handler[T, R]` | `type func(ctx context.Context, req *T) (R, error)` | 业务处理函数 |
| `Validator[T]` | `type func(ctx context.Context, req *T) error` | 请求校验器（返回 `*berrors.Error`） |
| `Defaulter` | `interface { Default() }` | 绑定后填充默认值（可选实现） |
| `HandleJSONRequest` | `func(c, Handler[T,R], ...Validator[T])` | 仅 JSON body 绑定 |
| `HandleQueryRequest` | `func(c, Handler[T,R], ...Validator[T])` | 仅 query 参数绑定 |
| `HandleUriRequest` | `func(c, Handler[T,R], ...Validator[T])` | 仅 path 参数绑定 |
| `HandleAllRequest` | `func(c, Handler[T,R], ...Validator[T])` | URI→Query→JSON 依次绑定（JSON 覆盖，最后 Default） |
| `ShouldBindAll` | `func(c *gin.Context, obj any) error` | 多源绑定 + Defaulter 钩子 |
| `WriteResponse[T]` | `func(c *gin.Context, data T, err error)` | 统一成功/错误响应（data 为 nil 仅 200 无 body） |
| `ErrorResponse` | `func(c *gin.Context, err error)` | 写 `{error:{code,message}}`，code 取 `berrors.Error.Reason` |
| `ErrorBody` / `ErrorDetail` | `struct { Error ErrorDetail }` / `struct { Code, Message string }` | 错误响应体 |

> 校验错误用 `errors.BadRequest(...)` 等，被 `ErrorResponse` 自然映射为 HTTP 400。

---

## 7. `pkg/berrors` — 传输中立错误模型（核心零依赖）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Error` | `struct { Code uint32; Reason string; Message string; Details map[string]string; cause error; stack []uintptr }` | 业务错误；字段级不可变 |
| `New` | `func(code uint32, reason string) *Error` | 构造 |
| `(*Error).Error` | 实现 `error` 接口 | 格式 `code: N, reason: X; cause: ...` |
| `(*Error).Is` | 按 `Reason` 匹配（忽略 Code/Message/cause） | 标准库 `errors.Is` 兼容 |
| `(*Error).WithMessage` | `func(format string, args ...any) *Error` | 注入用户文案 |
| `(*Error).WithDetails` | `func(map[string]string) *Error` | 注入 i18n 变量 |
| `(*Error).WithCause` | `func(error) *Error` | 包装底层错误 |
| `(*Error).WithCode` | `func(uint32) *Error` | 覆盖传输类别（逃生舱） |
| `(*Error).StackTrace` | `func() string` | 调用栈格式化 |
| `Is` / `As` / `Unwrap` / `Join` | 桥接标准库 | 重导出（注意 `New` 因签名冲突不重导出） |
| `FromError` | `func(err error) (*Error, bool)` | 从 error 链提取 `*Error` |
| 错误码常量 | `CodeOK=0 … CodeUnauthenticated=16`（与 gRPC codes 1:1） | 不要改数值 |
| 便捷工厂 | `BadRequest/NotFound/AlreadyExists/PermissionDenied/ResourceExhausted/FailedPrecondition/Unauthenticated/Internal/Unimplemented/Unavailable/DeadlineExceeded(reason) *Error` | 一行构造 |

### 7.1 `pkg/berrors/grpcerr`（gRPC 边界，可选 import）

| 符号 | 签名 | 说明 |
|---|---|---|
| `ToStatus` | `func(err error) *status.Status` | `*Error` → gRPC status + `errdetails.ErrorInfo`；未命中退回 Unknown |
| `FromStatus` | `func(st *status.Status) error` | status → `*Error`（还原 Reason/Details） |

### 7.2 `pkg/berrors/httperr`（HTTP 边界，可选 import，仅标准库）

| 符号 | 签名 | 说明 |
|---|---|---|
| `CodeToHTTP` | `func(code uint32) int` | Code → HTTP 状态（未知兜底 500） |
| `HTTPToCode` | `func(httpStatus int) uint32` | 反向映射（多对一取最贴切，未知兜底 Unknown） |
| `StatusCode` | `func(e *berrors.Error) int` | `= CodeToHTTP(e.Code)`（替代原核心包 `Error.StatusCode()` 以免循环依赖） |

---

## 8. `pkg/log` — 结构化日志

| 符号 | 签名 | 说明 |
|---|---|---|
| `Logger` | `type Logger struct`（封装 `*slog.Logger`） | 日志句柄 |
| `Options` | `struct { Level, Format, OutputPaths []string, ... }` | 日志配置（`NewOptions()` 取默认） |
| `NewOptions` | `func() *Options` | 默认配置 |
| `Option` | `type func(*Logger)` | 构造选项 |
| `With` | `func(opts ...Option) *Logger` | 从默认构造 |
| `NewSlogLogger` | `func(opts ...Option) *Logger` | 构造 logger |
| `NewSlogFromOptions` | `func(o *Options) (*Logger, error)` | 从 `Options` 构造 |
| `WithHandler` / `WithOTelHandler` | `func(h slog.Handler) Option` | 注入自定义 slog handler（OTel backend 用 `WithOTelHandler`） |
| `WithLevel` / `WithFormat` / `WithOutputPaths` | `func(...) Option` | 基础配置 |
| `Context` | `func(ctx, msg, keysAndVals ...) error` | 写 info 级，支持 ctx 属性流 |
| `Error` / `Warn` / `Debug` / `Info` | `func(ctx, msg, ...) error` | 各级别（Context 变体） |
| `ContextWithAttrs` | `func(ctx context.Context, attrs ...slog.Attr) context.Context` | 向 ctx 注入属性流（可被 slog backend 消费，如 inject `trace_id`） |
| `ContextAttrs` | `func(ctx) []slog.Attr` | 取出 ctx 属性流 |
| 级别常量 | `LevelDebug / LevelInfo / LevelWarn / LevelError`（`slog.Level` 别名） | |
| 属性构造 | `kvs.String / Int / Bool / Duration(...)` 等 | `= slog` 同名函数重导出 |

> 全局函数（`log.Context` / `log.Error` / ...）消费 ctx 中 `ContextWithAttrs` 注入的属性，实现 trace_id 贯穿。

---

## 9. `pkg/middleware` — 中间件（依赖倒置）

### 9.1 gin 中间件

| 符号 | 签名 | 注入接口 |
|---|---|---|
| `authz.NewAuthorizer` | `func(matcher Matcher, denyHandler Handler) gin.HandlerFunc` | `Matcher func(*gin.Context) (allow bool, reason string)`、`Handler` |
| `authn.NewAuthenticator` | `func(extractor TokenExtractor, resolver UserResolver) gin.HandlerFunc` | `TokenExtractor func(*gin.Context) (token string, err error)`、`UserResolver func(ctx, token) (*User, error)` |
| `observability.New` | `func(opts ...Option) gin.HandlerFunc` | 内部 `otel.Tracer` 起 span + `ContextWithAttrs(trace_id/span_id)` |

### 9.2 grpc 中间件（均返回 `grpc.UnaryServerInterceptor` / `StreamServerInterceptor`）

| 符号 | 返回 | 注入接口 |
|---|---|---|
| `RequestIDInterceptor` | `UnaryServerInterceptor` | 无（内置） |
| `ErrorInterceptor` | `UnaryServerInterceptor` | 收口 `*berrors.Error` → gRPC status；**必须最外层** |
| `ValidatorInterceptor` | `UnaryServerInterceptor` | `MessageValidator func(ctx, rq any) error` |
| `DefaulterInterceptor` | `UnaryServerInterceptor` | 对实现 `Defaulter` 的请求调 `Default()` |
| `AuthnInterceptor` | `UnaryServerInterceptor` | `authn.Authenticator`；认证后注入 `AuthClaims` + `contextx.WithTenantID` |
| `AuthzInterceptor` | `UnaryServerInterceptor` | `authz.Authorizer` + `AuthzOption`（`WithObjectResolver/WithActionResolver/WithSubjectResolver`，P9 归一化可选） |
| `AuditInterceptor` | `UnaryServerInterceptor` | `AuditOption`（`AuditWithAuditor/Metrics/ObjectResolver/ActionResolver/SubjectResolver`）；handler 返回后记录，deny/error 结果捕获 |
| `UnaryObservability` / `StreamObservability` | 拦截器 | 内部 OTel span + `trace_id`/`span_id` 注入 ctx |
| `UnaryObservabilityWithW3CTraceContext` / `WithTraceID` / `WithCustomHeader` / `SkipMetrics` / `WithSkipMethods` | 便捷变体 | 同上 |

> 拦截器链原则（bundle 已固化）：`ErrorInterceptor` 最外层 → RequestID → Observability → Authn → **Audit** → Authz（Audit 在 Authn 内侧读 subject/tenant、Authz 外侧捕 deny/error）。

### 9.3 `pkg/middleware/bundle` — 横切关注点门面（P10）

| 符号 | 签名 | 说明 |
|---|---|---|
| `New` | `func(opts ...Option) *Bundle` | 一次构造双传输产出 |
| `(*Bundle).Gin` | `func() []gin.HandlerFunc` | Recovery→RequestID→Logging→CORS→Secure→Authn→Audit→Authz（未注入层省略） |
| `(*Bundle).GRPCInterceptors` | `func() []grpc.UnaryServerInterceptor` | Error→RequestID→Observability→Authn→Audit→Authz |
| `(*Bundle).GRPCChain` | `func() []grpc.ServerOption` | 直接传 `grpc.NewServer` / `NewGRPCServerWithRegister` |
| Option | `Authn/Authz/Audit/Metrics/Normalized/CORS/Secure/NoLogging` | 依赖构造器注入；`Normalized()` 一键启用 P9 归一化；auditor 未注入显式接 Nop（不吃全局兜底） |

---

## 10. `pkg/validation` — 命令式校验分发器

| 符号 | 签名 | 说明 |
|---|---|---|
| `Validator` | `interface { ValidateXxxRequest(ctx, *Req) error }` | 业务自行实现，方法名对应请求类型 |
| `Validation` | `type Validation struct` | 分发器（按请求类型名找方法） |
| `NewValidation` | `func() *Validation` | 构造 |
| `Register` | `func(v any) error` | 注册校验器；方法名拼错返回 error（不静默失效） |
| `Validate` | `func(ctx, req any) error` | 按类型名分发到对应 `ValidateXxxRequest` |

> 与 miniblog 的 `Rules map[string]ValidatorFunc`（字段名字符串匹配）不同：方法名拼错即报错，改 proto 字段名不会静默 panic。

---

## 11. `pkg/contextx` — 请求上下文

| 符号 | 签名 | 说明 |
|---|---|---|
| `WithUser` / `User` | `func(ctx, *User) ctx` / `func(ctx) (*User, bool)` | 认证用户 |
| `WithTraceID` / `TraceID` | 注入/读取 trace_id | |
| `WithVars` / `Vars` | 通用键值袋 | |
| `User` | `struct { ID, Name string; Roles []string }` | 上下文用户 |

---

## 11.5 `pkg/store` — 数据访问层（泛型，零引擎依赖）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Store[T]` | 泛型 CRUD 门面 | `Create/Update/Delete/Get/List/Count` + `ListWithPaging(ctx, *storev1.PagingRequest)` |
| `DBProvider[T]` | `interface { DB(ctx) (Queryable[T], error); Close() error }` | 引擎句柄注入点（依赖倒置） |
| `Queryable[T]` | `interface { Create/Update/Delete/Get/List/Count/Migrate }` | 后端实现契约（GORM/Mongo 各自实现） |
| `Where` | `struct { Offset, Limit int; Filters []*FilterCondition; Sorting []*Sorting }` | 引擎无关条件 |
| `Eq/Gt/Gte/Lt/Lte/In/Nin/Like/Contains` | `func(field, value string) *FilterCondition` | 条件便捷构造 |
| `Sort/SortDesc` | `func(field string) *Sorting` | 排序便捷构造 |
| `Logger` / `NopLogger` / `FromLogger` | 日志接口 / 空转 / 适配器 | 最小子集，避免反向依赖 `pkg/log` |
| 哨兵错误 | `ErrNotFound` / `ErrConflict` / `ErrInvalidToken` | |
| `inmemory.NewProvider[T]` | `func(keyOf func(*T) string) *Provider[T]` | 内存实现（零依赖，e2e/单测用） |

> 分页/过滤/排序契约由 `bald/store/v1`（proto 生成）定义，与 gRPC/HTTP 共享类型。
>
> 多租户（P8）：`RegisterTenant(key, valueFunc)` 注册隔离维度（重复注册覆盖）；
> `UnregisterTenant(key)` 逆操作（幂等，T1 对偶成员）；`DefaultTenantFunc` 从
> `contextx.TenantID` 取值；写路径自动注入 `tenant_id`。测试隔离用
> `t.Cleanup(func() { store.UnregisterTenant("tenant_id") })`。

## 11.6 `pkg/authn` — 认证抽象

| 符号 | 签名 | 说明 |
|---|---|---|
| `Authenticator` | `interface { Authenticate(ctx) (*AuthClaims, error); AuthenticateToken(token string) (*AuthClaims, error) }` | 认证器抽象（中间件消费） |
| `AuthClaims` | `struct { Subject, TenantID, ... string }` | 认证后的身份声明 |
| `ContextWithAuthClaims` / `AuthClaimsFromContext` | ctx 注入/读取 | 中间件注入，审计/授权/多租户消费 |

## 11.7 `pkg/authz` — 授权抽象（P7/P9）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Authorizer` | `interface { Authorize(ctx, subject, object, action string) (bool, error) }` | 授权器抽象（casbin 桥接实现） |
| `DefaultGRPCObject/Action` | `func(fullMethod string) string` | P9 归一化：FullMethod → 资源名/动作（`/svc.Svc/Get` → `svc`/`get`） |
| `DefaultHTTPObject/Action` | `func(path/method string) string` | P9 归一化：path/method → 资源名/动作 |

> 桥接只做纯 Enforce，归一化在核心拦截器层完成（勿在桥接重复，双命名空间根源）。

## 11.8 `pkg/audit` — 审计抽象（M7）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Auditor` | `interface { Record(ctx, AuditEvent) }` | 审计后端抽象（旁路：panic/错误降级不阻断） |
| `AuditEvent` | `struct { Subject, TenantID, Object, Action, Result, Error, Meta }` | 审计事件 |
| `ResultAllow/Deny/Error` | 结果常量 | 由错误/状态推导 |
| `SetAuditor` / `GetAuditor` / `NopAuditor` | 全局注入点 / 兜底 | 默认 nop；bundle 走构造器注入 |

## 11.9 `pkg/metrics` — 指标抽象（M8）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Recorder` | `interface { Record(ctx, Event, Transport, duration float64) }` | 指标记录器抽象（otel API 封装，默认 no-op） |
| `Event` | `struct { Object, Action, Result string; Error error }` | 指标维度 |
| `Transport` / `TransportGRPC` / `TransportHTTP` | 传输维度常量 | |
| `New` | `func(scope string) Recorder` | 基于全局 MeterProvider（`bald_requests_total` / `bald_request_duration_seconds`） |

## 11.10 `pkg/testkit` — 测试工具（P13）

| 符号 | 签名 | 说明 |
|---|---|---|
| `FreeAddr` | `func(tb testing.TB) string` | 分配空闲 TCP 地址 `127.0.0.1:port`（e2e「先拿地址再监听」；勿用 `:0`） |

## 12. contrib 独立 module 桥接（`bald/contrib/`，独立 go.mod 按需引入）

| module | 包名 | 实现的核心抽象 | 说明 |
|---|---|---|---|
| `bald-authn-jwt` | `authnjwt` | `authn.Authenticator` | JWT（HMAC/RS256/ES256 Option 化），M6.5 |
| `bald-store-gorm` | `baldborm` | `store.DBProvider[T]` | GORM 全引擎（DSN scheme 路由 + 多租户），P8/M6 |
| `bald-cache-redis` | `rediscache` | —（旁路缓存组件） | Cache-Aside `Get/Delete/Key`（键须含租户），P11 晋升 |
| `bald-authz-casbin` | `authzcasbin` | `authz.Authorizer` | 内嵌通用 RBAC 模型，策略由调用方注入（`New(policyCSV)`），纯 Enforce，P11 晋升 |
| `bald-observability-otlp` | `metrics`/`trace` | `metrics.Recorder` + 全局 TracerProvider | Prometheus + OTLP 双通道（`WithOTLPAddr`），env 读取在调用方，P11 晋升 |

## 13. 契约接入速记

| 能力 | 核心抽象 | 桥接/实现 |
|---|---|---|
| 服务注册 | `registry.Registrar` | `registry.FromKratos(...)` / `inmemory.New()` |
| 配置中心 | `config.RemoteSource` | `config.FromKratosSource(...)` |
| 请求校验 | `web.Validator[T]` / `validation.Validator` | 业务实现 |
| 错误模型 | `berrors.Error` | 边界 `grpcerr` / `httperr` |
| 可观察性 | OTel no-op tracer | 注入 TracerProvider + `log.WithOTelHandler`；指标走 `metrics.New` + `bald-observability-otlp` 接线 |
| 数据访问 | `store.Store[T]` + `DBProvider[T]` | `store/inmemory` 内存实现；`bald-store-gorm` 等独立子模块经 `DBProvider` 桥接 |
| 配置契约 | `confv1.*`（proto） | `conf.NewBootstrap` / `Validate` / `BindFlags` |
| 认证 | `authn.Authenticator` | `bald-authn-jwt`；经 `AuthnInterceptor`/`AuthnMiddleware` 接入 |
| 授权 | `authz.Authorizer` | `bald-authz-casbin`（纯 Enforce）；归一化在拦截器层（P9） |
| 审计 | `audit.Auditor` | 自定义后端（落库/日志/消息总线）；`AuditInterceptor/Middleware` 旁路记录 |
| 多租户 | `store.RegisterTenant/UnregisterTenant` | `bald-store-gorm` 自动注入；Authn 后须 `contextx.WithTenantID` |
| 横切组装 | `middleware/bundle` | `bundle.New(...)` 双传输链序固化（P10） |
| 装配校验 | `appkit.Provides/Requires` | `Resolve()` 启动期 fail-fast（S1） |
| 拆卸正确性 | `appkit.Effect` / `Component` / `Registry.Mount` | 停机逆序回放/Dispose；e2e 隔离用 `UndoEffects`（T1/C1/A1） |
| 配置细粒度热更新 | `appkit.OnKeyChange` | key 级 diff 触发，与 `OnConfigChange` 共存（R1） |

> 全部契约遵循「核心定最小接口，实现由调用方注入」；新增后端（如 nacos）只需提供
> 实现并接 `FromKratos` / `FromKratosSource`，不改核心代码。
