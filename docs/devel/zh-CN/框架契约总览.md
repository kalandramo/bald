# 框架契约总览（API Surface）

> 本文档是 `bald` 框架**所有公开契约**的速查表：导出接口、类型、函数、常量及其签名。
> 目的不是替代各设计文档，而是让使用者一眼看清「能用什么、签名是什么、怎么接入」。
> 主模块包前缀为 `github.com/kalandramo/bald/pkg/`；`bconf` / `bconfig` / `bootstrap` /
> `transport` / `log` / `berrors` 均为独立 Go module（依赖全景见《模块依赖关系.md》）。
>
> 设计主线（详见各设计文档）：**核心零后端耦合**，注册中心 / 配置中心 / 校验 / 追踪等
> 可选能力均通过「最小接口 + 桥接适配器」注入，核心不绑定任何具体实现。

---

## 0. 模块与依赖层级

> 顶层模块（独立 Go module：bconf/bconfig/bootstrap/transport/log/berrors/registry）之间的
> 依赖方向、启动数据流与设计原则见《模块依赖关系.md》；下图为 API 视角的包层级。

```
appkit        ← 编排层（组合 transport/registry/bootstrap/config/log + 能力解析/组件/效应账本/挂载，最上层）
 ├─ transport   独立 module：Server 契约（HTTP/gRPC/Gateway）+ 子包 grpcserver/httpserver/gateway/web
 ├─ registry    独立 module：服务注册/发现契约（Registrar/Discovery/Watcher + inmemory；后端见 registry/<backend>）
 ├─ bootstrap/config  独立 module bootstrap 内：配置装载器 Store（Layer/RemoteSource/FromKratosSource 桥接）
 ├─ bconf       独立 module：框架配置 Protobuf 契约 bootstrapv1（默认值/校验/flag 绑定）
 ├─ transport/web     gin HTTP 处理流水线（Handler/Validator/响应）
 ├─ middleware  gin / grpc 中间件（依赖倒置接口 + 实现）
 │    └─ bundle   横切关注点门面（双传输链序固化，P10）
 ├─ berrors     独立 module：传输中立错误模型（包名 berrors，同名免别名）
 │    ├─ grpcerr  gRPC 边界转换（可选）
 │    └─ httperr  HTTP 边界转换（可选）
 ├─ log         独立 module：结构化日志（Logger/Options/kvs/Context；实现子包 log/slog）
 ├─ authn       认证抽象（Authenticator/AuthClaims/ctx 注取）
 ├─ authz       授权抽象（Authorizer + P9 传输中立归一化）
 ├─ audit       审计抽象（Auditor/AuditEvent/全局注入点）
 ├─ metrics     指标抽象（Recorder/Event，otel API 封装）
 ├─ validation  命令式校验分发器（依赖倒置）
 ├─ store       泛型数据访问层（零引擎依赖 + 多租户）
 ├─ testkit     测试工具（FreeAddr 等）
 └─ contextx    请求上下文键（user_id / username / trace_id / request_id / tenant_id）

contrib/       独立 module 桥接（module 前缀 github.com/kalandramo/，单向依赖核心，按需引入）：
  bald-authn-jwt / bald-store-gorm / bald-cache-redis /
  bald-authz-casbin / bald-observability-otlp
```

---

## 1. `pkg/appkit` — 应用编排

| 符号 | 签名 | 说明 |
|---|---|---|
| `AppKit` | `type AppKit struct`（字段非导出） | 应用编排器：`New(opts...)` 构造、`Run(ctx)` 启动；对外方法见下 |
| `New` | `func(opts ...Option) *AppKit` | 构造（不返回 error；配置经 Run 的 loadConfig 加载） |
| `(*AppKit).Run` | `func(ctx context.Context) error` | 启动所有 server + 注册 + 优雅停机；CAS 防重入（重复调用返回 `ErrAlreadyRunning`） |
| `(*AppKit).Settings` | `func() map[string]any` | 合并后的配置快照（深拷贝；BeforeStart 内 `Unmarshal` 用；Run 前/未加载为 nil） |
| `(*AppKit).Config` | `func() *config.Store` | 配置仓库（`GetString/GetStringSlice/Unmarshal` 实时点路径读取；Run 前/未加载为 nil） |
| `(*AppKit).Done` | `func() <-chan struct{}` | Run 结束（含停机完成）后关闭；供测试/嵌入等待 |
| `(*AppKit).Err` | `func() error` | Run 的退出错误（Run 返回后有效） |
| `(*AppKit).Resolve` | `func() error` | S1 能力声明校验（Run 启动早期自动调用；缺失聚合报错） |
| `(*AppKit).UndoEffects` | `func(parent context.Context)` | T1 效应账本立即逆序回放并清空（幂等；e2e `t.Cleanup` 隔离用；Run 停机自动调用） |
| `(*AppKit).MountComponent` | `func(ctx, Component) error` | A1 运行期挂载：Start + 纳入停机序列 + mount 审计；`ErrNotRunning`/已销毁拒绝 |
| `(*AppKit).UnmountComponent` | `func(ctx, name string) error` | A1 运行期卸载：移除 + 立即 Dispose（LIFO）+ unmount 审计 |
| `(*AppKit).ListComponents` | `func() []string` | 当前停机序列中的组件名（启动/挂载序，观测用） |
| `(*AppKit).ReconcileNow` | `func(ctx context.Context)` | 立即执行一次全量协调（按注册序；测试/手动触发） |
| `ErrAlreadyRunning` | `var error` | 重复调用 `Run` 的哨兵错误 |

**停机五阶段（stopAll）**：0 效应账本逆序回放 → 1 BeforeStop → 2 Server.Stop 并发 → 3 AfterStop → 4 组件逆序 Dispose（装配期 + 运行期挂载统一处理；`disposed` 闭环拒绝迟挂载）。

**函数式选项 `Option = func(*AppKit)`：**

| 选项 | 签名 | 说明 |
|---|---|---|
| `ID` / `Name` / `Version` | `func(string) Option` | 应用元信息 |
| `Registrar` | `func(r registry.Registrar) Option` | 注入 bald 注册中心（显式实例，优先于契约装配） |
| `WithRegistrarRegistry` | `func(rr *bootstrap.RegistrarRegistry) BootstrapOption` | FromBootstrap 契约装配注册表（显式 MustRegister provider，阶段 B 按 `registry.type` 单选构造） |
| `RemoteConfig` | `func(src config.RemoteSource) Option` | 注入远程配置源（bootstrap/config 的 kratos 桥） |
| `ConfigLayers` | `func(ls ...config.Layer) Option` | 契约层直通 Store（`Registry.Build` 产出，注册序=优先级；2026-09-05 归一接入） |
| `ConfigFile` | `func(f string) Option` | 指定 `--config` 路径 |
| `Env` | `func(env string) Option` | 运行环境（影响默认文件名 / 远程 path） |
| `WatchConfigFile` | `func(watch bool) Option` | 本地文件热更新 |
| `OnConfigChange` | `func(fn func(map[string]any)) Option` | 配置变更回调（形参为重合并后的新快照） |
| `Servers` | `func(servers ...transport.Server) Option` | 注入 server 列表 |
| `StopTimeout` | `func(d time.Duration) Option` | 优雅停机超时（钩子阶段固定 defaultHookTimeout，安全网保留不可配置） |
| `BeforeStart` / `AfterStart` / `BeforeStop` / `AfterStop` | `func(fn func(context.Context) error) Option` | 生命周期钩子 |
| `Bind` | `func(prefix string, opt any) Option` | 注册业务配置 flag；`opt` 为 `proto.Message`（bconf.BindFlags）或 `PlainBinder`（AddFlags，prefix 必须为空） |
| `PlainBinder` | `interface { AddFlags(fs *pflag.FlagSet) }` | 无前缀 flag 注册器（如 `log.Options` 内部注册 `--log.*`） |
| `Effect` | `func(name string, undo func(context.Context) error) Option` | T1 效应账本：登记全局写入的逆操作（停机阶段 0 逆序回放；panic 隔离、独立超时） |
| `EffectTimeout` | `func(d time.Duration) Option` | 单条 undo 的独立超时（默认同钩子超时） |
| `Provides` | `func(caps ...string) Option` | S1 声明进程提供的能力（重复声明报错） |
| `Requires` | `func(component string, caps ...string) Option` | S1 声明组件依赖的能力（缺失 fail-fast；重复幂等） |
| `Components` | `func(comps ...Component) Option` | C1 注册进程内组件（顺序 Start、停机逆序 Dispose） |
| `ComponentTimeout` | `func(d time.Duration) Option` | 组件 Start/Dispose 独立超时 |
| `OnKeyChange` | `func(key string, fn func(old, new string)) Option` | R1 key 级配置订阅：仅当该 key 值确实变化才触发；与 `OnConfigChange` 共存（先全量后分发）；基线=首次加载值 |
| `Auditor` | `func(a audit.Auditor) Option` | D4：注入 appkit 自身审计后端（A1 重组审计、R1-2 协调审计）——与业务 `bundle.Audit` 注入同源，消除 split-brain sink；未注入回退全局 `audit.GetAuditor()`（`resolveAuditor` 单一解析序：注入→全局→nop 默认） |
| `Reconcile` | `func(name string, fn func(ctx context.Context, rctx *ReconcileCtx) error) Option` | R1-2 期望态协调：声明以 `name`（配置键）为标识的协调器；框架在**启动收敛期**（`loadConfig` 基线后、AfterStart 前）与**每次配置变更**（keywatch 分发后）自动调用 `fn`；语义对齐 K8s controller 的 diff-apply（期望态写在配置，框架只更新有变动部分） |
| `DiffStrings` | `func(want, have []string) (add, remove []string)` | R1-2 导出工具：期望/实际集合 diff，返回需新增/移除项；供协调函数做幂等 diff-apply（期望==实际直接早退，不抖动） |


**`Component` 接口（C1）**：`interface { Name() string; Start(ctx) error; Dispose(ctx) error }`——进程内基础设施（trace provider/metrics exporter/审计后端连接等）。`ComponentFunc(name, dispose)` 适配纯清理函数。`transport.Server` = Component + Endpoint。

**`Registry[T]` 泛型注册表**：`NewRegistry[T]()` / `Register(name, impl) error`（装配期，重名报错不覆盖）/ `MustRegister(name, impl)`（重名 panic，装配期显式注册用）/ `Get(name) (T, bool)` / `Mount(name, impl) (undo func(), err error)`（A1 运行期可逆挂载，undo 幂等）/ `Unmount(name) (T, bool)` / `List() []string`（升序）。注册表实例由调用方显式构造——无全局预置实例、无 init() 自注册（曾预置的 `ServerRegistry`/`MiddlewareRegistry`/`ProviderRegistry` 与 `RegisterStoreProvider` 已删，零消费且与显式装配哲学矛盾）；bootstrap 三 Registry 与 appkit 各业务 Registry 均以此为基座。

**`ReconcileCtx`（R1-2）**：协调函数入参，携带 `Settings map[string]any`（协调开始时的配置快照，只读）+ 辅助读取 `String(key) string` / `StringSlice(key) []string`（点路径）、`Name string`（协调器名）、`app *AppKit`，并暴露实际态查询/操作原语：`Mounted() []string`（已挂载组件名列表）/ `Has(name string) bool` / `Mount(ctx, name, c Component) error` / `Unmount(ctx, name string) error`（基于 A1 的 `Registry.Mount/Unmount` 与 `AppKit.MountComponent/UnmountComponent`）。方法是并发安全的（`reconMu` 保护 `reconItems`）。协调器内部应自行用 `DiffStrings` 比对期望态（`rctx.StringSlice(key)` 等）与这些方法返回的实际态，再调用 `Mount/Unmount` 收敛；部分失败留中间态，下一次协调补全（controller 自愈）。

**R1-2 协调上下文与测试装置**：`NewReconcileCtx(app *AppKit, name string, settings map[string]any) *ReconcileCtx` 构造协调上下文（供测试或「手动触发协调」；正常路径由 `runReconcilers` 内部构造）。`NewHarness() *AppKit` 构造测试装置——跳过配置加载与 server 启动、直接进入运行态（`MountComponent`/`ReconcileCtx.Mount` 可用），供跨 module 集成测试驱动协调逻辑（examples/*、tests/* 是独立 Go module，无法用 `export_test.go` 暴露未导出成员的惯用法）；生产代码用 `New` + `Run`，不使用装置。

> 关键约束：`Bind("http", &bootstrapv1.Server_Http{...})`（契约实例）的 prefix 与配置键（`http.addr`）、
> 环境变量（`NAME_HTTP_ADDR`）、文件键四者一致，flag 才能压过文件/远程。
> 构造后不要再自行 `AddFlags(pflag.CommandLine)`，否则重复注册。

---

## 2. `transport` — 服务端抽象（module `github.com/kalandramo/bald/transport`）

> 顶层契约在包 `transport`；构造函数分布在子包：
> `transport/http`（包名 `httpserver`）、`transport/grpc`（包名 `grpcserver`）、
> `transport/gateway`（包名 `gateway`）。下表符号归属：除标注外，顶层契约在 `transport`，
> `NewHTTPServer`/探针常量在 `httpserver`，`NewGRPCServer*` 在 `grpcserver`，`NewGatewayServer` 在 `gateway`。

| 符号 | 签名 | 说明 |
|---|---|---|
| `Server` | `interface { Start(ctx context.Context) error; Stop(ctx context.Context) error; Endpoint() string }` | 统一生命周期契约（与 kratos transport.Server 兼容的超集）；Endpoint 支持 `:0` 动态端口解析 |
| `ReadinessFunc` | `func(ctx context.Context) error` | 就绪探针回调；nil 视为恒就绪 |
| `Ready` | `func(fns ...ReadinessFunc) ReadinessFunc` | 多依赖就绪聚合（`errors.Join` 保留全部失败） |
| `DefaultHealthPath` / `DefaultReadyPath` | `const "/healthz"` / `const "/readyz"`（`httpserver`） | HTTP 探针默认路径 |
| `NewHTTPServer` | `func(cfg *bootstrapv1.Server_Http, handler http.Handler, readiness ReadinessFunc, opts ...HTTPServerOption) *HTTPServer` | HTTP(S) server；业务 handler 挂 "/"，框架探针自动注册（业务同名路径优先） |
| `HTTPServerOption` / `WithProbePaths(health, ready)` | `type HTTPServerOption func(*HTTPServer)` | 覆盖探针路径（默认 `/healthz` `/readyz`） |
| `NewGRPCServer` | `func(cfg *bootstrapv1.Server_Grpc, srv *grpc.Server, readiness ReadinessFunc, opts ...GRPCServerOption) *GRPCServer` | 包装已构建 `*grpc.Server`（不含 health/reflection，业务自注册） |
| `NewGRPCServerWithRegister` | `func(cfg *bootstrapv1.Server_Grpc, unary []grpc.ServerOption, register func(s *grpc.Server), readiness ReadinessFunc, opts ...GRPCServerOption) *GRPCServer` | gRPC server + health/reflection 自动注册 + 业务 service |
| `GRPCServerOption` / `WithReadinessPollInterval(d)` | `type GRPCServerOption func(*GRPCServer)` | 覆盖 readiness 后台轮询间隔（默认 2s，`defaultReadinessPollInterval`） |
| `NewGatewayServer` | `func(httpCfg *bootstrapv1.Server_Http, backend *bootstrapv1.Server_Grpc, register func(ctx context.Context, conn *grpc.ClientConn) (http.Handler, error), readiness ReadinessFunc) (*GatewayServer, error)` | grpc-gateway 反向代理；延迟建 conn + 持锁时序防 Stop 死锁 |
| `(*GRPCServer).Endpoint` / `(*HTTPServer).Endpoint` | 实时读 cfg 地址 + listener | 支持 `:0` 动态端口、可达 IP 解析 |

> 就绪语义：HTTP `/readyz` 与 gRPC health（`SetServingStatus("")`，整体服务）共用同一 `ReadinessFunc`，两端对称（P1）。业务 handler 由调用方注入（`http.Handler` / `register` 回调），核心不依赖具体 proto 生成代码。

---

## 3. `registry` — 服务注册（2026-09-17 自 `pkg/registry` 下放为独立 module）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Registrar` | `interface { Register(ctx context.Context, ins *ServiceInstance) error; Deregister(ctx context.Context, ins *ServiceInstance) error }` | 注册中心最小契约 |
| `Discovery` | `interface { GetService(ctx, name) ([]*ServiceInstance, error); Watch(ctx, name) (Watcher, error) }` | 服务发现契约（对齐 go-wind；provider 已实现，bald 消费方出现即可用） |
| `Watcher` | `interface { Next(ctx) ([]*ServiceInstance, error); Stop() error }` | 实例变更监听 |
| `ServiceInstance` | `struct { ID, Name, Version string; Metadata map[string]string; Endpoints []string; Kind string }` | 注册实例：ID=实例唯一标识（appkit 默认 hostname-随机后缀，同机多实例不冲突）；Endpoints=各 `Server.Endpoint()` 解析地址（支持 `:0`）；Kind=single/multi |
| `inmemory.New` | `func() *inmemory.Registrar` | 内存实现（开发/测试；`Register` 按 ID 覆盖） |
| `(*inmemory.Registrar).List` | `func() []*ServiceInstance` | 已注册实例快照（测试断言用） |

> 后端（etcd/nacos/consul/kubernetes）为 registry/<backend> 下**直连 SDK 独立 module**
> （移植自 go-wind），经 `bootstrap.RegistrarRegistry` 显式注册 + 契约 `registry.type`
> 单选装配（2026-09-06 起，kratos 桥接已删）。核心不绑定任何后端。

---

## 4. `bootstrap/config` — 配置装载器（`github.com/kalandramo/bald/bootstrap/config`，2026-09-05 收编自独立 module `bald/config`；同日 viper 退役、Registry 归一为层模型）

> 统一层模型内核：`Layer`（命名源层：远程桥/契约源/本地文件都是层）+ env/flag 枚举 +
> map 深合并（copy-on-write），类型规范化下沉 §5 `bconf.UnmarshalMap`。
> 优先级（高 → 低）flag > env > 本地文件 > 契约源层（`Layers` 列表首最高）> 远程桥（基准）；
> 任一层热更新均触发**全量重合并**，各层缓存保留（本地覆盖不污染远程基准、远程更新不冲掉本地覆盖）。
> 2026-09-05 起 viper 已退役（其全部能力由内核替代，且 AllSettings 不含纯 env key），
> 格式收敛为 yaml/json（`Layer.Format` 为空回退 yaml，yaml.v3 兼容 JSON）；
> `bootstrap.Registry.Build` 产出 `[]Layer`（注册序=层优先级），装配主链路不再经过
> `bconfig.FallbackReader`（退守同 key 多候选回退域，见《Bald 配置源层设计.md》消费者章节）。

| 符号 | 签名 | 说明 |
|---|---|---|
| `Layer` | `struct{Name string; Reader bconfig.Reader; Format string; Watch bool}` | 命名配置源层（整文档源，key 固定传空串；`Watch=true` 且 reader 实现 `bconfig.ValueWatcher` 才参与热更新） |
| `Options` / `Load` / `RemoteSource` / `FromKratosSource` | 同前 | 层模型合并 + 热更新 + kratos 桥接（`Remote` 内部适配为最低基准层） |
| `Store` | `Load` 返回 | 配置仓库：`Settings() map[string]any`（深拷贝快照）、`Get/GetString/GetStringSlice`（点路径）、`Unmarshal(msg)`、`Close()`（停层 watch 与自建源；reader 资源归 Registry cleanup） |
| `Unmarshal` | `func(m map[string]any, msg proto.Message) error` | 配置快照 → proto（委托 `bconf.UnmarshalMap(m, msg)`） |

---

## 5. `bconf` — 契约层工具（`github.com/kalandramo/bald/bconf`，2026-09-05 自 pkg/conf 迁入并切 bootstrapv1）

| 符号 | 签名 | 说明 |
|---|---|---|
| `NewBootstrap` | `func() *bootstrapv1.BootstrapConfig` | 手写默认值（App.Id=主机名/Http :8080/Grpc :9090/Logger slog） |
| `Validate` | `func(cfg *bootstrapv1.BootstrapConfig) error` | app/server.addr/logger 段校验，启动前 fail-fast |
| `BindFlags` | `func(fs *pflag.FlagSet, msg proto.Message, prefix string)` | 遍历 proto 描述符注册带前缀 flag；**nil 子消息跳过**（防实例化破坏「非 nil 即启用」语义，如 Server_TLS） |
| `UnmarshalMap` | `func(m map[string]any, msg proto.Message) error` | 配置树 → proto（类型规范化 + 合并式语义 + repeated 替换）；加载器无关 |

> 契约类型 `bootstrapv1.BootstrapConfig / Server_Http / Server_Grpc / Logger` 等由
> `bconf/proto/` buf managed mode 生成到 `bconf/gen/go/`，transport/bootstrap 全部直连。
> legacy `confv1` 契约与 `pkg/conf`、`api/proto`（含 protoc-gen-defaults stub）已于 2026-09-05 删除；
> 服务器地址语义为「指针直通」：flag/Unmarshal 合并与 server Start 读值同一对象，无桥接快照。
> `LogOptions`（契约 Logger → bslog.Options）归装配层 bootstrap（见 §11.6）。

---

## 6. `transport/web` — gin HTTP 处理流水线（`github.com/kalandramo/bald/transport/web`，包名 `web`，2026-09-05 自 pkg/web 迁入）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Handler[T, R]` | `type func(ctx context.Context, req *T) (R, error)` | 业务处理函数 |
| `Validator[T]` | `type func(ctx context.Context, req *T) error` | 请求校验器（返回 `*berrors.Error`） |
| `Defaulter` | `interface { Default() }` | 绑定后填充默认值（可选实现） |
| `HandleJSONRequest` | `func(c, Handler[T,R], ...Validator[T])` | 仅 JSON body 绑定 |
| `HandleQueryRequest` | `func(c, Handler[T,R], ...Validator[T])` | 仅 query 参数绑定 |
| `HandleUriRequest` | `func(c, Handler[T,R], ...Validator[T])` | 仅 path 参数绑定 |
| `HandleAllRequest` | `func(c, Handler[T,R], ...Validator[T])` | URI→Query→JSON 依次绑定（JSON 覆盖，最后 Default） |
| `ShouldBindAll` | `func(c *gin.Context, obj any) error` | 多源绑定 + Defaulter 钩子 |
| `WriteResponse[T]` | `func(c *gin.Context, data T, err error)` | 统一成功/错误响应（data 为 nil 仅 200 无 body） |
| `ErrorResponse` | `func(c *gin.Context, err error)` | 决策⑧错误出口：写 google.rpc.Status JSON 形状 |
| `StatusOf` | `func(err error) StatusBody` | ErrorResponse 的单源构造器（中间件 AbortWithStatusJSON 等自定义出口复用，保证全部出口同形） |
| `StatusBody` / `ErrorInfoBody` | `struct { Code uint32; Message string; Details []ErrorInfoBody }` / `struct { @type, Reason, Domain, Metadata }` | 决策⑧错误体：`{"code","message","details":[{"@type","reason","domain","metadata"}]}`，与 grpc-gateway 转码输出结构同形（空值语义对齐 protojson：`""`/`{}`/`[]`） |

> 校验错误用 `berrors.BadRequest(...)` 等，被 `ErrorResponse` 自然映射为 HTTP 400。
> 错误体三面（gin/gRPC/gateway）一份契约（2026-09-10 决策⑧落地）：展示读 `message`，程序化读 `details[0].reason`；`Error` 日志串不再外发。

---

## 7. `berrors` — 传输中立错误模型（module `github.com/kalandramo/bald/berrors`，2026-09-05 自 pkg/berrors 迁出；**包名 `berrors`**，2026-09-11 自 `errors` 改名——import 无需别名）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Error` | `struct { Code uint32; Reason string; Message string; Details map[string]string; cause error; stack []uintptr }` | 业务错误；字段级不可变 |
| `New` | `func(code uint32, reason string) *Error` | 构造 |
| `(*Error).Error` | 实现 `error` 接口 | 格式 `code: N, reason: X; cause: ...` |
| `(*Error).Is` | 按 `Reason` 匹配（忽略 Code/Message/cause） | 标准库 `errors.Is` 兼容 |
| `(*Error).WithMessage` | `func(format string, args ...any) *Error` | 注入用户文案 |
| `(*Error).WithDetails` | `func(map[string]string) *Error` | 注入 i18n 变量 |
| `(*Error).WithCause` | `func(error) *Error` | 包装底层错误 |
| `(*Error).WithCode` | `func(uint32) *Error` | 覆盖传输类别（逃生舱） |
| `(*Error).StackTrace` | `func() string` | 调用栈格式化 |
| `Is` / `As` / `Unwrap` / `Join` | 桥接标准库 | 重导出（注意 `New` 因签名冲突不重导出） |
| `FromError` | `func(err error) (*Error, bool)` | 从 error 链提取 `*Error` |
| 错误码常量 | `CodeOK=0 … CodeUnauthenticated=16`（与 gRPC codes 1:1） | 不要改数值 |
| 便捷工厂 | `BadRequest/NotFound/AlreadyExists/PermissionDenied/ResourceExhausted/FailedPrecondition/Unauthenticated/Internal/Unimplemented/Unavailable/DeadlineExceeded(reason) *Error` | 一行构造 |

### 7.1 `berrors/grpcerr`（gRPC 边界，可选 import）

| 符号 | 签名 | 说明 |
|---|---|---|
| `ToStatus` | `func(err error) *status.Status` | `*Error` → gRPC status + `errdetails.ErrorInfo`；未命中退回 Unknown |
| `FromStatus` | `func(st *status.Status) error` | status → `*Error`（还原 Reason/Details） |

### 7.2 `berrors/httperr`（HTTP 边界，可选 import，仅标准库）

| 符号 | 签名 | 说明 |
|---|---|---|
| `CodeToHTTP` | `func(code uint32) int` | Code → HTTP 状态（未知兜底 500） |
| `HTTPToCode` | `func(httpStatus int) uint32` | 反向映射（多对一取最贴切，未知兜底 Unknown） |
| `StatusCode` | `func(e *berrors.Error) int` | `= CodeToHTTP(e.Code)`（替代原核心包 `Error.StatusCode()` 以免循环依赖） |

---

## 8. `log` + `log/bslog` — 日志两层（2026-09-05 对齐 go-wind 三层拆分；同日并入单 module）

> 布局对齐 transport：**同一 module 内「顶层契约包 + 实现子包」**（2026-09-13 子包 `log/slog`→`log/bslog`、包名 `slogadapter`→`bslog`——目录=包名自造词，既避标准库冲突又免消费点强制别名）。契约核心依旧零三方依赖，slog 生态重依赖（pflag/lumberjack/errgroup）收敛在子包，Go 按包链接不影响契约使用者的二进制。

### 8.1 `log`（契约层，`github.com/kalandramo/bald/log`）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Logger` | `interface { Debug/Info/Warn/Error(ctx context.Context, msg string, args ...any); Enabled(level Level) bool; With(args ...any) Logger }` | 框架最小日志契约（第一参 ctx，后端可提取 trace_id） |
| `Level` | `type int`；`LevelDebug = iota … LevelError` | 级别 0-3 |
| `SetLogger` / `GetLogger` | `func(l Logger)` / `func() Logger` | 全局句柄（并发安全；nil 回退 nop，零副作用） |
| `ContextWithAttrs` / `ContextAttrs` | `func(ctx, attrs ...slog.Attr) context.Context` / `func(ctx) []slog.Attr` | ctx 属性流（请求范围日志自动携带 trace_id；ContextAttrs 供适配器层落盘前合并） |
| `NewMultiLogger` | `func(loggers ...Logger) Logger` | 多源广播装饰器：每条日志写全部后端（本地 + 远程并存）；Enabled 任一即真；With 组合传播；nil sink 过滤 |

依赖：仅标准库（含 log/slog）；无第三方依赖。后端实现见 §8.2，契约驱动装配见 bootstrap（`LogRegistry` + `BslogLoggerProvider` + `BuildLogger`）。

### 8.2 `log/bslog`（适配器子包，`github.com/kalandramo/bald/log/bslog`，包名 `bslog`）

把「已配置好的后端」包装为契约 Logger；依赖 `bald/log` + pflag/lumberjack/errgroup。

| 符号 | 签名 | 说明 |
|---|---|---|
| `Options` | `struct { Level, Format string; OutputPaths []string; Rotate *RotateOptions }` | 日志配置（`NewOptions()` 取默认；`(*Options).AddFlags(fs)` 注册 `--log.*` 含 `--log.rotate.*`） |
| `RotateOptions` | `struct { Enabled bool; MaxSize, MaxBackups, MaxAge int; Compress bool }` | 文件轮转（lumberjack，按大小切割） |
| `New` | `func(o *Options, opts ...Option) log.Logger` | 标准库 `log/slog` 适配层后端（默认 console） |
| `Option` | `func(*config)`（内部） | 构造选项 |
| `WithFilter(f)` / `WithAttrs(attrs...)` / `WithHandler(h)` / `WithOTelHandler(h)` | `func(...) Option` | 脱敏 / 固定属性 / 自定义 slog handler / OTel Logs 后端 |
| `Filter` / `FilterKey` | `type func(slog.Attr) slog.Attr` / `func(key string) Filter` | 敏感字段脱敏（password/token 自动替换） |

用法：`log.SetLogger(bslog.New(opts, bslog.WithFilter(bslog.FilterKey("password"))))`。

> 核心零依赖（接口 + nop 默认），后端由进程入口装配期 `SetLogger` 注入；`GetLogger()` 供全框架消费。适配器子包仅依赖 pflag/lumberjack/errgroup，与契约同 module（主模块 require 一次即可）。

> `NewMultiLogger` 属契约层通用装饰器（纯 Logger 组合、不依赖 slog），已上移至 §8.1 的 `log` 包；对应 transport 顶层 `Serve` 的位置。

### 8.3 `log/<backend>`（远端/终端日志后端，独立 module × 5，移植 go-wind）

直连 SDK 的契约 Logger 实现：`aliyun`（SLS producer）、`tencent`（CLS producer）、
`loki`（HTTP push 批量）、`sentry`（错误追踪）、`charm`（终端彩色输出）。
根包零契约依赖；`contract` 子包是唯一 import bconf 的地方（契约字段 → 构造选项，
`Provider(ctx, *bootstrapv1.Logger_Backend) (log.Logger, func(), error)`），与 registry/<backend> 同布局。

| module | 直连 SDK | 契约段 | 清理语义 |
|---|---|---|---|
| `log/aliyun` | aliyun-log-go-sdk（producer） | `logger.aliyun` | Close 冲刷并关闭 producer |
| `log/tencent` | tencentcloud-cls-sdk-go（producer） | `logger.tencent` | 同上 |
| `log/loki` | 标准库 net/http（批量 push） | `logger.loki`（endpoint 必填） | Close 停后台推送 |
| `log/sentry` | sentry-go | `logger.sentry`（dsn 必填） | Close = Flush |
| `log/charm` | charmbracelet/log | `logger.charm`（level/format/output_path 与 slog 段同形状） | 关输出文件（stderr/默认无操作） |

接线（显式注册，不用 init()+blank import；注册名 = 契约后端项 `type` 小写字符串）：

```go
reg := baldbootstrap.NewLogRegistry()
reg.MustRegister(aliyuncontract.Type, aliyuncontract.Provider) // "aliyun"
appkit.FromBootstrap(cfg, appkit.WithLogRegistry(reg))
```

appkit 日志工厂两级分发：`WithLogRegistry`（阶段 A 无契约回退默认 slog 保证启动日志可见，
阶段 B 按 `logger.backends` 逐项查表重建）> 默认纯函数路径（仅 slog 项 + deco；其余
项 fail-fast 教学报错给出 WithLogRegistry 用法）。注册表内未注册的 type fail-fast（错误
列出已注册项并定位到项）；契约各后端段（aliyun/tencent/loki/sentry/charm）自本节起全部有消费者。

---

## 9. `pkg/middleware` — 中间件（依赖倒置）

> R1 合并（2026-09-12）：gin/gRPC 可观测性中间件的传输无关核心（trace header 常量、`TraceInjectionMode`、header 计算 `InjectTrace(mode, customHeader, spanCtx, set)`、通配匹配 `MatchWildcard`）收敛至子包 `pkg/middleware/shared`——改一处两传输层生效；两侧以类型/常量别名维持既有导出 API。

### 9.1 gin 中间件

| 符号 | 签名 | 注入接口 |
|---|---|---|
| `authz.NewAuthorizer` | `func(matcher Matcher, denyHandler Handler) gin.HandlerFunc` | `Matcher func(*gin.Context) (allow bool, reason string)`、`Handler` |
| `authn.NewAuthenticator` | `func(extractor TokenExtractor, resolver UserResolver) gin.HandlerFunc` | `TokenExtractor func(*gin.Context) (token string, err error)`、`UserResolver func(ctx, token) (*User, error)` |
| `ginmw.Observability` | `func(opts ...Option) gin.HandlerFunc` | 内部 `otel.Tracer` 起 span + `ContextWithAttrs(trace_id/span_id)` |

### 9.2 grpc 中间件（均返回 `grpc.UnaryServerInterceptor` / `StreamServerInterceptor`）

| 符号 | 返回 | 注入接口 |
|---|---|---|
| `RequestIDInterceptor` | `UnaryServerInterceptor` | 无（内置） |
| `ErrorInterceptor` | `UnaryServerInterceptor` | 收口 `*berrors.Error` → gRPC status；**必须最外层** |
| `ValidatorInterceptor` | `UnaryServerInterceptor` | `MessageValidator func(ctx, rq any) error` |
| `DefaulterInterceptor` | `UnaryServerInterceptor` | 对实现 `Defaulter` 的请求调 `Default()` |
| `AuthnInterceptor` | `UnaryServerInterceptor` | `authn.Authenticator` + `AuthnOption`（`AuthnWithAuditor`，认证失败发 ResultDeny 审计事件——D3）；认证后注入 `AuthClaims` + `contextx.WithTenantID` |
| `AuthzInterceptor` | `UnaryServerInterceptor` | `authz.Authorizer` + `AuthzOption`（`WithObjectResolver/WithActionResolver/WithSubjectResolver`，P9 归一化可选） |
| `RecoveryInterceptor` | `UnaryServerInterceptor` | 捕获 panic → `*berrors.Internal`（由最外层 Error 收口 status）；置于 Error 之后第二外层（D5） |
| `AuditInterceptor` | `UnaryServerInterceptor` | `AuditOption`（`AuditWithAuditor/Metrics/ObjectResolver/ActionResolver/SubjectResolver`；**签名无首参**，D6）；handler 返回后记录，deny/error 结果捕获 |
| `UnaryObservability` / `StreamObservability` | 拦截器 | 内部 OTel span + `trace_id`/`span_id` 注入 ctx；Logger 请求期惰性解析（`WithLogger` 显式注入优先，D8） |

> 拦截器链原则（bundle 已固化）：`ErrorInterceptor` 最外层 → Recovery → RequestID → Observability → Authn → **Audit** → Authz（Audit 在 Authn 内侧读 subject/tenant、Authz 外侧捕 deny/error；Authn 失败由 Authn 层显式发审计留痕，D3）。

### 9.3 `pkg/middleware/bundle` — 横切关注点门面（P10）

| 符号 | 签名 | 说明 |
|---|---|---|
| `New` | `func(opts ...Option) *Bundle` | 一次构造双传输产出 |
| `(*Bundle).Gin` | `func() []gin.HandlerFunc` | Recovery→RequestID→Logging→CORS→Secure→Authn→Audit→Authz（未注入层省略） |
| `(*Bundle).GRPCInterceptors` | `func() []grpc.UnaryServerInterceptor` | Error→Recovery→RequestID→Observability→Authn→Audit→Authz（Recovery 第二外层，D5） |
| `(*Bundle).GRPCChain` | `func() []grpc.ServerOption` | 直接传 `grpc.NewServer` / `NewGRPCServerWithRegister` |
| Option | `Authn/Authz/Audit/Metrics/Normalized/Raw/CORS/Secure/NoLogging` | 依赖构造器注入；**归一化默认开启**（D7），`Raw()` 显式 opt-out 回 P7 原始语义；auditor 未注入显式接 Nop（不吃全局兜底），并注入 Authn 层（authn 失败审计与请求审计同源，D3） |

---

## 10. `pkg/validation` — 命令式校验分发器

| 符号 | 签名 | 说明 |
|---|---|---|
| `Validator` | `type struct`（内部 `map[类型名]→func`） | 按请求类型名分发校验方法 |
| `NewValidator` | `func(validators any) (*Validator, error)` | 反射注册对象上全部 `Validate<XxxRequest>(ctx, *XxxRequest) error` 方法（nil 合法）；签名不符返回 error |
| `(*Validator).Register` | `func(validators any) error` | 追加注册（意图声明：`Validate` 前缀方法任一约定不满足即报错，不静默失效） |
| `(*Validator).Validate` | `func(ctx context.Context, request any) error` | 按类型名分发；未注册放行（返回 nil） |
| `RequestTypeName` | `func(request any) string` | 请求类型名（去指针） |
| `ValidRequired` | `func(fields map[string]any) error` | 必填字段非空检查 |
| `ValidRange` | `func(field string, value, min, max any) error` | 数值/字符串长度区间 |
| `ValidPattern` | `func(field, value string, rules ...string) error` | `prefix`/`suffix`/`contains`/`len` 规则 |

> 与 miniblog 的 `Rules map[string]ValidatorFunc`（字段名字符串匹配）不同：方法名拼错即报错，改 proto 字段名不会静默 panic。

---

## 11. `pkg/contextx` — 请求上下文

| 符号 | 签名 | 说明 |
|---|---|---|
| `WithUserID` / `UserIDFromContext` | `func(ctx, string) context.Context` / `func(ctx) string` | 用户 ID（认证中间件写入；缺失返回空串） |
| `WithUsername` / `UsernameFromContext` | 同上 | 用户名 |
| `WithTenantID` / `TenantIDFromContext` | 同上 | 租户 ID（store 多租户自动隔离） |
| `WithTraceID` / `TraceIDFromContext` | 同上 | trace_id（可观测中间件写入） |
| `WithRequestID` / `RequestIDFromContext` | 同上 | request_id |

> 五键均为扁平 string 键（写读配对、缺失返回空串），供 middleware / business / store 跨包共享，避免各处重复定义私有 context 键。

---

## 11.5 `pkg/store` — 数据访问层（泛型，零引擎依赖）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Store[T]` | 泛型 CRUD 门面 | `Create/Update/Delete/Get/List/Count` + `ListWithPaging(ctx, *storev1.PagingRequest)` |
| `DBProvider[T]` | `interface { DB(ctx) (Queryable[T], error); Close() error }` | 引擎句柄注入点（依赖倒置） |
| `Queryable[T]` | `interface { Create/Update/Delete/Get/List/Count/Migrate }` | 后端实现契约（GORM/Mongo 各自实现） |
| `Where` | `struct { Offset, Limit int; Filters []*FilterCondition; Sorting []*Sorting }` | 引擎无关条件 |
| `Eq/Gt/Gte/Lt/Lte/Ne/In/Nin/Like/Contains/Cond` | `func(field, value string) *FilterCondition`（`In/Nin` 变参、`Cond(field, op, value)`） | 条件便捷构造 |
| `Sort/SortDesc` | `func(field string) *Sorting` | 排序便捷构造 |
| `Logger` / `NopLogger` / `FromLogger` | 日志接口 / 空转 / 适配器 | 最小子集，避免反向依赖 `bald/log`（契约 module） |
| 哨兵错误 | `ErrNotFound` / `ErrConflict` / `ErrInvalidToken` | |
| `inmemory.NewProvider[T]` | `func(keyOf func(*T) string) *Provider[T]` | 内存实现（零依赖，e2e/单测用） |

> 分页/过滤/排序契约由 `storev1` 定义（`bconf/proto/bald/store/v1` buf managed mode 生成至
> `bconf/gen/go/bald/store/v1`，2026-09-05 自 api/proto 迁入），与 gRPC/HTTP 共享类型。
>
> 多租户（P8）：`RegisterTenant(key, valueFunc)` 注册隔离维度（重复注册覆盖）；
> `UnregisterTenant(key)` 逆操作（幂等，T1 对偶成员）；`DefaultTenantFunc` 从
> `contextx.TenantID` 取值；写路径自动注入 `tenant_id`。数据权限（可选）：`RegisterDataScope(fn DataScopeFunc)`
> 注册行级范围推导（`DataScopeFunc(ctx, *authn.AuthClaims) []*storev1.FilterCondition`）。
> 测试隔离用 `t.Cleanup(func() { store.UnregisterTenant("tenant_id") })`。

## 11.6 `pkg/authn` — 认证抽象

| 符号 | 签名 | 说明 |
|---|---|---|
| `Authenticator` | `interface { Authenticate(ctx context.Context) (*AuthClaims, error); AuthenticateToken(token string) (*AuthClaims, error) }` | 认证器抽象（实现契约含过期校验；中间件消费） |
| `AuthClaims` | `struct { Subject, TenantID, Name, Issuer string; Scopes, Roles []string; ExpiresAt time.Time }` | 认证后的身份声明 |
| `(*AuthClaims).HasScope` / `HasRole` / `Expired` | 断言 | scope/role 精确匹配、过期判定（ExpiresAt 零值=不过期） |
| `ContextWithAuthClaims` / `AuthClaimsFromContext` | ctx 注入/读取 | 注入并同步写 contextx 的 user/tenant 键；审计/授权/多租户消费 |
| `TokenExtractor` | `interface { Extract(ctx context.Context) (string, error) }` | 传输层（gin/grpc）抽取原始 token |
| `ContextWithToken` / `TokenFromContext` | ctx 注入/读取 token | 中间件抽取后经 `Authenticate` 消费 |

## 11.7 `pkg/authz` — 授权抽象（P7/P9）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Authorizer` | `interface { Authorize(ctx context.Context, subject, object, action string) (bool, error) }` | 授权器抽象（false+nil=拒绝→403；err=判定出错→500） |
| `Func` / `AllowAll` / `DenyAll` | `type func(ctx, subject, object, action string) (bool, error)` / `func() Authorizer` | 函数适配 / 全放行 / 默认拒绝（fail-closed） |
| `ObjectResolver` / `ActionResolver` | `type func(request string) string` | 拦截器可覆盖对象/动作推导（返回空=用默认） |
| `DefaultGRPCObject` / `DefaultGRPCAction` | `func(fullMethod string) string` | P9 归一化：FullMethod → 资源名/动作（`/svc.Svc/GetSecret` → `svc`/`get`） |
| `DefaultHTTPObject` / `DefaultHTTPAction` | `func(path/method string) string` | P9 归一化：path/method → 资源名/动作 |

> 桥接只做纯 Enforce，归一化在核心拦截器层完成（勿在桥接重复，双命名空间根源）。

## 11.8 `pkg/audit` — 审计抽象（M7）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Auditor` | `interface { Record(ctx, AuditEvent) }` | 审计后端抽象（旁路：panic/错误降级不阻断） |
| `AuditEvent` | `struct { Time time.Time; Subject, TenantID, Object, Action string; Result Result; Error string; Meta map[string]any }` | 审计事件（三元组与 P9 归一化同源） |
| `ResultAllow/Deny/Error` | 结果常量 | 由错误/状态推导 |
| `SetAuditor` / `GetAuditor` / `NopAuditor` | 全局注入点 / 兜底 | 默认 nop；bundle 走构造器注入 |

## 11.9 `pkg/metrics` — 指标抽象（M8）

| 符号 | 签名 | 说明 |
|---|---|---|
| `Recorder` | `interface { Record(ctx, Event, Transport, duration float64); RecordActive(ctx, Event, Transport, delta int64) }` | 指标记录器抽象（otel API 封装，默认 no-op） |
| `Event` | `struct { Object, Action, Result, Error string; Request RequestInfo }` | 指标维度（Error 为字符串，避免 metrics 反向依赖 audit；Request 为协议维度，semconv v1.43.0 对齐） |
| `Transport` / `TransportGRPC` / `TransportHTTP` | 传输维度常量 | |
| `New` | `func(scope string) Recorder` | 基于全局 MeterProvider（semconv 指标 `http.server.request.duration` / `http.server.active_requests` / `rpc.server.call.duration` + `bald_audit_events_total`，2026-09-15 对齐）；instruments 惰性创建（v0.2.1，首次 Record 时绑 meter——根治 SetMeterProvider 前构造的 noop-bind） |

自动装配（v0.2.1）：契约 `metrics` 段经 `appkit.WithMetricsRegistry` 查表
（`observability-otlp/contract` 注册 prometheus/otlp Provider），暴露端独立
于业务 server（缺省 `:9091`），停机 flush 最后执行。详见《Bald 指标设计》「自动装配路径」。

## 11.10 `pkg/testkit` — 测试工具（P13）

| 符号 | 签名 | 说明 |
|---|---|---|
| `FreeAddr` | `func(tb testing.TB) string` | 分配空闲 TCP 地址 `127.0.0.1:port`（e2e「先拿地址再监听」；勿用 `:0`） |

## 11.11 `pkg/id` — 框架内置 ID 生成（2026-09-10 自 bald-utils/id 吸收 UUID 子集）

仅保留纯 google/uuid 依赖的两个变体（「核心零依赖」纪律，其余变体留在 bald-utils）：

| 符号 | 签名 | 说明 |
|---|---|---|
| `NewGUIDv4` | `func(withHyphen bool) string` | UUID v4（随机） |
| `NewGUIDv7` | `func(withHyphen bool) string` | UUID v7（时间有序，数据库索引局部性友好；时钟不可靠时回退 v4）——业务主键/文件 GUID 等场景（go-wind-admin 消费 bald-utils 孪生同形 API） |

消费方：`transport/{tcp,webrtc}` session ID。

## 12. contrib 独立 module 桥接（`bald/contrib/`，独立 go.mod 按需引入）

| module | 包名 | 实现的核心抽象 | 说明 |
|---|---|---|---|
| `bald-authn-jwt` | `authnjwt` | `authn.Authenticator` | JWT（HMAC/RS256/ES256 Option 化），M6.5 |
| `bald-store-gorm` | `baldborm` | `store.DBProvider[T]` | GORM 全引擎（DSN scheme 路由 + 多租户），P8/M6 |
| `bald-cache-redis` | `rediscache` | —（旁路缓存组件） | Cache-Aside `Get/Delete/Key`（键须含租户），P11 晋升 |
| `bald-authz-casbin` | `authzcasbin` | `authz.Authorizer` | 内嵌通用 RBAC 模型，策略由调用方注入（`New(policyCSV)`），纯 Enforce，P11 晋升 |
| `bald-observability-otlp` | `metrics`/`trace`/`contract` | `metrics.Recorder` + 全局 TracerProvider + 契约 Provider | Prometheus + OTLP 双通道；`contract` 子包接 `appkit` 双 Registry 自动装配（tracer/metrics 段查表），P11 晋升 |

## 13. 契约接入速记

| 能力 | 核心抽象 | 桥接/实现 |
|---|---|---|
| 服务注册 | `registry.Registrar` | `registry/<backend>` 契约装配（`RegistrarRegistry`）/ `inmemory.New()` |
| 配置中心 | `config.RemoteSource`（`bootstrap/config`） | `config.FromKratosSource(...)`；或 `Registry.Build` 产出 `[]config.Layer` 直通 |
| 请求校验 | `web.Validator[T]` / `validation.Validator` | 业务实现 |
| 错误模型 | `berrors.Error` | 边界 `grpcerr` / `httperr` |
| 可观察性 | OTel no-op tracer | 契约自动装配（v0.2.1）：`WithTracerRegistry`/`WithMetricsRegistry` + `observability-otlp/contract` Provider；手动路径 `metrics.New` + 全局 TracerProvider 仍可用 |
| 数据访问 | `store.Store[T]` + `DBProvider[T]` | `store/inmemory` 内存实现；`bald-store-gorm` 等独立子模块经 `DBProvider` 桥接 |
| 配置契约 | `bootstrapv1.*`（bconf/proto，buf managed mode） | `bconf.NewBootstrap` / `Validate` / `BindFlags` |
| 认证 | `authn.Authenticator` | `bald-authn-jwt`；经 `AuthnInterceptor`/`AuthnMiddleware` 接入 |
| 授权 | `authz.Authorizer` | `bald-authz-casbin`（纯 Enforce）；归一化在拦截器层（P9） |
| 审计 | `audit.Auditor` | 自定义后端（落库/日志/消息总线）；`AuditInterceptor/Middleware` 旁路记录 |
| 多租户 | `store.RegisterTenant/UnregisterTenant` | `bald-store-gorm` 自动注入；Authn 后须 `contextx.WithTenantID` |
| 横切组装 | `middleware/bundle` | `bundle.New(...)` 双传输链序固化（P10） |
| 装配校验 | `appkit.Provides/Requires` | `Resolve()` 启动期 fail-fast（S1） |
| 拆卸正确性 | `appkit.Effect` / `Component` / `Registry.Mount` | 停机逆序回放/Dispose；e2e 隔离用 `UndoEffects`（T1/C1/A1） |
| 配置细粒度热更新 | `appkit.OnKeyChange` | key 级 diff 触发，与 `OnConfigChange` 共存（R1） |
| 报文编解码 | `encoding.Codec` | `encoding/<fmt>` ×12 独立 module，装配期显式 `MustRegister` |
| 数据库客户端 | `bootstrap.DatabaseRegistry` | `database.<engine>` 段查表装配（多段并存）；`contrib/database/{gorm,mongodb}` 首批落地；`appkit.WithDatabaseRegistry` 注入、`AppKit.Database` 取用（2026-09-15 自 appkit 迁 bootstrap） |
| 缓存实例 | `bootstrap.CacheRegistry` | `cache.{local,redis}` 段查表装配；`bald/cache/{local,redis}` 独立 module，停机先于数据库关；`appkit.WithCacheRegistry` 注入 |
| 对象存储 | `bootstrap.StorageRegistry` | `storage.{minio,s3}` 段查表装配；`bald/oss/{minio,s3}` 独立 module（自 go-wind-plugins/oss 移植）；`appkit.WithStorageRegistry` 注入 |
| AI 模型客户端 | `bootstrap.AiRegistry` | `ai.{openai,langchaingo,eino}` 段查表装配；`bald/ai/{openai,langchaingo,eino}` 独立 module；model_type 1=local/2=cloud；`organization` 仅 openai/langchaingo 消费（eino-ext 无此概念）；`appkit.WithAiRegistry` 注入 |
| 工作流引擎 | `bootstrap.WorkflowRegistry` | `workflow.argo` 段查表装配（多段并存）；`bald/workflow/argo` 独立 module（纯 REST 客户端，token/insecure_skip_verify 消费）；`appkit.WithWorkflowRegistry` 注入 |
| 消息代理 | `bootstrap.BrokerRegistry` | `broker.{kafka,rabbitmq,redis,rocketmq}` 段查表装配（多段并存）；`bald/broker/<impl>` 独立 module；先 Init 再 Connect，停机在 workflow 之后（in-flight 消息先排干）；`appkit.WithBrokerRegistry` 注入 |

> 全部契约遵循「核心定最小接口，实现由调用方注入」；新增注册中心后端只需 registry/<backend>
> 下加直连 provider（contract 子包映射契约段），配置后端接 `FromKratosSource` /
> `Registry.Build`，不改核心代码。

## 14. `encoding` — 编解码抽象（module `github.com/kalandramo/bald/encoding`，2026-09-06 自 go-wind-plugins/encoding 移植）

统一编解码契约 + 全局注册表，为所有传输面提供可拔插序列化；业务代码零改动切换格式。根包零三方依赖；12 种格式各自独立 module（`encoding/<fmt>`，require 根 module + `replace => ..`），按需引入，未引入的格式不进依赖图。

- **契约**：`Codec`（`Marshal` / `Unmarshal` / `Name`）
- **显式注册（与 go-wind 的差异）**：不用 init() + blank import——格式包导出 `New()`（avro 另有 `NewCodec(schema)`），应用装配期 `encoding.MustRegister(jsoncodec.New())`；nil / 空名 / 重名 panic（fail-fast），不静默覆盖；`GetCodec` 大小写无关、未注册返回 nil 由调用方 fail-fast；`Names()` 诊断枚举
- **格式**：文本 json/xml/yaml/toml；二进制 proto/msgpack/bson/cbor/gob/thrift；schema 驱动 avro/flatbuffers
- **特殊约束**：proto 须 `proto.Message`；thrift 须 `TStruct`；flatbuffers 须 flatc 生成码接口；avro 默认 `New()` 仅原始类型，复杂记录走 `NewCodec(schema)`
- **无 bconf 契约段**（纯能力层，符合「契约字段须全有消费者」）；未来传输层要按契约选 codec 时再加 contract 子包与 `WithCodec` 装配

## 15. `workflow/argo` — 工作流引擎客户端（module `github.com/kalandramo/bald/workflow/argo`，2026-09-06 自 go-wind-plugins/workflow/argo 移植）

Argo Workflows 的 REST 客户端（纯 stdlib，零三方依赖）：提交/查询/列表/删除/挂起/恢复/终止/重跑/重试/停止/日志全生命周期。

- **契约装配**：`workflow/argo/contract` 消费 `workflow.argo` 段（`server_url`/`namespace`/`token`/`insecure_skip_verify`），`Provider` 返回 `*argo.WorkflowClient`（`Close` 关闭空闲连接）
- **appkit**：`appkit.WithWorkflowRegistry` + `WorkflowRegistry.MustRegister("argo", ...)`；`app.Workflow(typ)/Workflows()` 取用；段存在无注册表 fail-fast
- **测试**：httptest 模拟 Argo Server 验证完整生命周期 + `Authorization: Bearer` 头 + 404 错误传播
- **不移植**：temporal/conductor/goworkflows（重型 SDK，按需评估）

## 16. `broker` — 消息代理抽象（modules `github.com/kalandramo/bald/broker` 及 `broker/<impl>`，2026-09-06 自 go-wind-plugins/broker 移植）

Kafka / RabbitMQ / Redis(PubSub+Stream) / RocketMQ(v2) 四后端。根包 `Broker` 契约 + `Message`/`Event`/`Options` + 泛型 `Subscribe[T]`；依赖 `bald/encoding`（消息体序列化，默认 json）。

- **契约装配**（各 `broker/<impl>/contract` 消费 `broker.<impl>` 段，先 `Init` 再 `Connect`，cleanup=`Disconnect`）：
  - `kafka`：`brokers` + `auth_type`（none/plain/scram-sha256/scram-sha512 → SASL 机制）
  - `rabbitmq`：`url`（缺 `amqp://` 自动补；无 userinfo 时注入 `username/password`）+ `exchange`（默认交换机）+ `queue`（`WithDefaultQueue`，`Subscribe("")` 回退）
  - `redis`：`address`/`password`/`db` 组装为 redigo DialURL；默认 pubsub 驱动，stream 驱动为能力层（`redis.NewBroker(option.DriverTypeStream, ...)`）
  - `rocketmq`：v2 客户端（rocketmq-client-go），`name_servers`/`name_server_url`(HTTP resolver)/`access_key`/`secret_key`/`security_token`/`group_name`/`namespace`/`instance_name`/`retry_count`/`topic`(默认主题) 全字段贯通
- **binder 类型化语义修复（与源库差异）**：`broker.Unmarshal` 对 binder 场景（`outValue` 为指向 any 的指针且 any 已装具体类型指针）先解引用一层再交 codec——标准反序列化器对 `*any` 会用 map 顶掉原类型，导致 binder 绑定的具体类型丢失；修复后 `pub.Message().Body.(*T)` 稳定成立
- **不移植**：tracer（消息链路 span，与全局 tracer 一起去留一致；otlp 已覆盖基础追踪）、其余 7 个 broker 后端（nats/nsq/pulsar/sqs 等按需补）、rocketmq V5/aliyun 驱动（契约字段以 v2 形状定义，`gogap/errors` 伪版本 replace 到 `v0.0.0-20210818113853`）
- **停机链序**：servers→registrar→database→cache→storage→ai→workflow→broker（逆序回放：服务器先 drain，broker 最后关、db 最先关之外的新增最后段）
