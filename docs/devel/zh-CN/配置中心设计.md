# bald 配置中心设计文档

> 状态：**核心实现已落地**（配置加载层 `pkg/config` + proto 配置契约 `pkg/conf`；
> 原 `pkg/options` 业务配置结构体层已于 2026-08-29 废弃删除），本档沉淀决策与取舍。
> 最后更新：2026-08-29
> 包路径：
> - `pkg/config`（配置加载层：本地文件 + env + flag + 远程中心，四源合并）
> - `pkg/conf`（配置契约层：Protobuf，提供默认值/校验/BindFlags/ResolveTLS，详见 [proto 配置契约设计](./proto%20配置契约设计.md)）
>
> 更新记录：
> - 2026-08-29：9.1 节的「业务 flag 未进入 viper override 层」已修复（`appkit.Bind`）；
>   新增 proto 配置契约（阶段 1），详见独立设计文档。
> - 2026-08-29（P1 阶段 2）：废弃 `pkg/options`，server 层直接消费 `confv1.*` proto；
>   flag 绑定改由 `conf.BindFlags` 遍历 proto 描述符生成，TLS Smart Mode 迁至 `conf.ResolveTLS`。

---

## 1. 背景与问题

bald 的配置加载层（`pkg/config`）基于 viper 实现了「本地文件 + 环境变量 + flag + 远程配置中心」，
但 viper 的**标准 remote 机制**存在硬性缺陷，无法直接用于生产级配置中心：

| 问题 | 说明 |
|---|---|
| 强制 JSON | viper remote 内部用 `json.Unmarshal` 解析远程字节，etcd/consul 里存 yaml 会直接报错 |
| watch 不可靠 | `WatchRemoteConfigOnChannel` 依赖各 provider 自行实现 `Watcher`，且经全局 channel 推送、无法精确区分 key，常丢事件 |
| 无鉴权/TLS 透传 | `AddRemoteProvider` 注册时拿不到 client 配置（证书、token），复杂环境不可用 |
| 格式单一、合并弱 | 远程只覆盖不叠加，缺少多源优先级与多环境 |

> 结论：**viper remote 机制不可靠，业界框架（Kratos 等）均绕开它、自建 Source 抽象。**
> 因此本设计采用「Kratos 的 Source 抽象」+「viper 作为壳」的混合方案：既解决 viper remote 的全部痛点，
> 又满足「配置加载与热更新仍基于 viper」的诉求，业务继续用 `app.Viper().Unmarshal` 取配置。

---

## 2. 设计目标

1. 支持**本地配置 + 远程配置中心**混合。
2. 配置文件格式支持 **json / yaml / toml**（远程字节格式由后端自声明，不强制 JSON）。
3. 支持**多环境**（dev/test/prod 等）。
4. 配置加载与热更新**仍基于 viper**。
5. 远程 watch 可靠，变更能稳定触发 `OnConfigChange`。
6. 加载层与配置契约解耦，但能无缝回填（框架级配置经 `pkg/conf` 按 proto 契约解析；
   原 `pkg/options` 已于 2026-08-29 废弃）。

---

## 3. 核心抽象：`RemoteSource`

借鉴 Kratos `config.Source`：把「远程存储」抽象为一个接口，每个后端自己负责把远程数据
适配成「原始字节 + 格式」。格式不再由 viper 决定，而由后端声明，从而绕开强制 JSON。

```go
// pkg/config/source.go（当前实现）

// RemoteSource 表示一个远程配置源。
// 每个后端（etcd/consul/nacos/apollo...）实现该接口，自行声明字节格式。
// 鉴权/TLS 等凭据由各后端在构造时自行持有（接口本身不携带），不污染抽象。
type RemoteSource interface {
    // Read 拉取一次远程配置，返回原始字节与格式（"json"/"yaml"/"toml"...）。
    Read(ctx context.Context) (data []byte, format string, err error)

    // Watch 监听远程变更，每次变更通过 onChange 推送最新字节与格式。
    // ctx 取消即停止监听（实现方应在内部退出监听 goroutine）。
    Watch(ctx context.Context, onChange func(data []byte, format string)) error
}
```

> 与 Kratos 的差异：Kratos 的 `Source.Watch()` 返回 `Watcher`，由 reader 主循环 `Next()` 重新 `Load`；
> 本设计改为**直接回调 `onChange`（push 模式）**，省一次 IO，且与 viper 注入流程衔接更自然。
> `Read(ctx)` 的 `ctx` 参数在 Kratos 桥接场景下透传给 `src.Load()`（Kratos v1.5+ 的 `Load(ctx)`），
> 纯内存源可忽略。

### 3.1 桥接 Kratos 后端（不重复造轮子）

bald **不**手写 etcd/consul/nacos 适配器，而是提供 `FromKratosSource` 桥接 kratos contrib 已实现的后端：

```go
import (
    "github.com/kalandramo/bald/pkg/config"
    etcdconfig "github.com/go-kratos/kratos/v3/contrib/config/etcd/v3"
)

// 用户自行引入具体 contrib 后端模块，bald 核心仅依赖 kratos 核心库 config 接口。
src := config.FromKratosSource(
    etcdconfig.New(client, etcdconfig.WithPath("/config/demo/prod.yaml")),
)
appkit.RemoteConfig(src)
```

nacos 配置中心后端（同 §3.1 桥接，dataID 带扩展名 `.yaml` 以便 contrib 识别格式）：

```go
import (
    "github.com/nacos-group/nacos-sdk-go/clients"
    "github.com/nacos-group/nacos-sdk-go/clients/config_client"
    "github.com/nacos-group/nacos-sdk-go/common/constant"
    "github.com/nacos-group/nacos-sdk-go/vo"

    "github.com/kalandramo/bald/pkg/config"
    nacosconfig "github.com/go-kratos/kratos/v3/contrib/config/nacos/v3"
)

func newNacosConfigSource() config.RemoteSource {
    cli, err := clients.NewConfigClient(vo.NacosClientParam{
        ClientConfig:  &constant.ClientConfig{NamespaceId: "public", TimeoutMs: 5000},
        ServerConfigs: []constant.ServerConfig{*constant.NewServerConfig("127.0.0.1", 8848)},
    })
    if err != nil {
        panic(err)
    }
    // NewConfigClient 返回即 config_client.IConfigClient，无需断言。
    return config.FromKratosSource(nacosconfig.NewConfigSource(
        cli.(config_client.IConfigClient),
        nacosconfig.WithDataID("bald-demo.yaml"),
        nacosconfig.WithGroup("DEFAULT_GROUP"),
    ))
}
```

桥接器 `kratosSourceAdapter` 的关键行为：
- `Read`：调 `src.Load()`，取 `kvs[0].Value` 与 `kvs[0].Format`；多 KV（如目录型 file source）
  合并为单一文档并加 `# key=...` 注释，避免静默丢配置。
- `Watch`：调 `src.Watch()` 拿到 `Watcher`，起 goroutine `Next()` 循环；`ctx` 取消后 `defer w.Stop()`
  退出，避免独立 goroutine 泄漏；监听出错（连接中断）退避 1s 后继续，对齐 Kratos 主循环韧性。

> 取舍：桥接层只依赖 kratos **核心** config 接口，具体后端 SDK 由业务 `go get` 引入——
> 保证 bald 核心零后端耦合，也避免把 kratos 整个生态锁死进框架。

---

## 4. 加载流程（绕过 viper 强制 JSON）

关键技巧：**远程字节不经过 viper 的 `AddRemoteProvider`，而是手动注入 viper**。

```go
// pkg/config/config.go

// injectRemote 将远程字节手动注入 viper。
// 先 SetConfigType 声明格式，再 ReadConfig 解析，
// 因此 etcd/consul 里存 yaml 也能正确解析（绕过 viper 强制 JSON 的限制）。
func injectRemote(v *viper.Viper, data []byte, format string) error {
    v.SetConfigType(format)
    return v.ReadConfig(bytes.NewReader(data))
}
```

### 4.1 优先级模型（核心决策）

四源合并优先级（高 → 低），符合 K8s/容器部署运维预期：

```
命令行 flag（--config / 业务 flag）
   > 环境变量（NAME_ 前缀，容器化部署下运维最常用来覆盖配置）
      > 本地文件（覆盖远程基准）
         > 远程配置中心（基准源）
```

viper 内部有「override 层」与「底层 config map」两层。实现要点：

- **远程与本地同处底层 config**：远程先 `ReadConfig`（基准），本地后 `MergeConfigMap`（后写赢），
  同名 key 本地覆盖远程 → 底层内部「本地 > 远程」。
- **flag（`BindPFlags`）与 env（`AutomaticEnv`，`NAME_` 前缀）在 override 层**，天然压过底层 config；
  其中 flag 优先级高于 env（viper 默认语义）。
- 最终优先级：**flag > env > 本地 > 远程**。

`bindViperBasics` 的环境变量约定：前缀 `strings.ToUpper(Name)`（如 `BALD_DEMO_`），
key 替换 `.`→`_`、`-`→`_`（即 `http.addr` → `BALD_DEMO_HTTP_ADDR`）。

### 4.2 watch 互不污染

| 触发源 | 重建方式 |
|---|---|
| 远程变更 | `injectRemote`（Reset 底层为远程基准）→ 重新 `MergeConfigMap(localMap)` 叠加本地覆盖 |
| 本地变更 | 重置底层（远程基准重注入 / 无远程则注入空 `{}`）→ 重新合并本地 map |

两者都整体重建底层，避免：① 磁盘已删的 key 在 viper 残留；② 远程基准被 `ReadConfig` 清掉。
并发由 `sync.RWMutex`（`vMu`）保护 viper 底层读写（viper 本身非并发安全，本地/远程回调可能并发）。

### 4.3 踩坑记录：层级选择与 flag 优先级

> 来自 `@command://cr` 审查发现的 🔴 功能正确性 bug，并在补充单测时进一步修正（flag 必须高于本地）。

- **版本一（错误）**：远程进 override 层、本地进底层——两层装反，远程 `:8080` 压住本地 `:9090`。
  测试 `TestLoad_RemoteBaselineThenLocalOverride` 抓到回归。
- **版本二（仍错误）**：本地进 override、远程进底层——本地覆盖远程了，但漏掉 flag：
  `v.Set`（override 层）优先级高于 `BindPFlags` 的 flag 层，`--http.addr` 无法压过本地文件。
  测试 `TestLoad_FlagHighestPriority` 抓到回归。
- **版本三（最终正确）**：远程与本地都落底层、靠加载顺序定胜负（远程先写、本地后写赢）；
  flag/env 在 override 层自然压底层。

**被测试揭示的硬约束**：
1. viper 的 flag **只有显式 Parse 且有值才生效**，默认值不覆盖 config 层。
2. 远程 watch 必须 `injectRemote` 后**重新叠加缓存的 `localMap`**，否则本地被清。
3. 本地 watch 回调内必须 Reset 重建底层（重注入远程基准 + 合并本地），否则删 key 残留、远程基准丢失。
4. env 经 `AutomaticEnv` 在 override 层动态查询，**始终压过底层**（含本地文件与远程）—— 正是 K8s 下运维预期。

> **一句话经验**：「覆盖远程 / 压本地」交给 override 层（flag > env）；远程与本地同处底层、**本地后写赢**；
> watch 各自重建底层、互不污染。

---

## 5. 多环境设计

> ✅ 已拍板：**路线 1（env 进入 path/namespace）**。

- **本地**：`Options.Env` 非空时按 `{Name}-{Env}.yaml/json` 选择默认文件（如 `bald-demo-prod.yaml`）；
  也可经 `appkit.Env("prod")` 在 AppKit 层设置。查找顺序：显式 `ConfigFile` > `Name-Env` > `Name`，
  搜索路径含 `.` / `./configs` / `$HOME/.config/<Name>`。
- **远程**：`--env` 由用户在构造后端 path 时拼接（如 `/config/demo/prod.yaml`），`Load` 不感知远程 path；
  鉴权/TLS 等凭据由后端构造时持有（`RemoteSource` 接口不携带，避免过度耦合）。
- 本地文件缺失**不报错**（对齐 onexstack 行为）：允许纯远程 / 纯 flag 配置。

| 路线 | 做法 | 优点 | 缺点 |
|---|---|---|---|
| **路线 1：env 进入 path/namespace** ✅ | `--env=prod` 拼到远程路径/namespace；本地按 `config-prod.yaml` | 环境隔离清晰、远程一套存储多环境 | 需约定路径规范 |
| 路线 2：env 仅选 source | `--env` 只决定用哪套 Sources | 实现简单 | 环境切换不灵活 |

---

## 6. 热更新

- **本地**：保留 viper `WatchConfig`（fsnotify），变更触发 `OnConfigChange`。
- **远程**：`RemoteSource.Watch` 回调拿到新字节 → `injectRemote` 重新注入 viper → 触发 `OnChange(v)`。
- 业务在 `OnChange` 内重新 `v.Unmarshal(&opts)` 完成热重载。
- `OnChange` 形参为**裸 viper**（粒度由业务自定，决策见第 7 节），且本地/远程变更走同一回调，
  业务无需区分来源。

---

## 7. 配置契约层对接（pkg/conf；原 pkg/options 已废弃）

> **2026-08-29 更新（P1 阶段 2）**：`pkg/options` 整层已删除。全栈 proto 化后
> （框架配置、业务配置、API、请求参数、前后端类型均由 proto 生成），proto 是唯一
> 契约与类型真相源，`options` 作为「proto → Go struct」的中间翻译层已无存在必要。
> onexstack 仅为**借鉴实现**，无需对其 `IOptions` 契约保持对齐。

配置加载（`pkg/config`）与配置契约（`pkg/conf`）解耦：加载层只产出 `*viper.Viper`，
由 `conf` 按 proto 契约解析成强类型 `Bootstrap`，server 层直接消费其子消息。

### 7.1 契约来源

框架级配置定义于 `api/proto/bald/config/v1/*.proto`，生成到 `pkg/conf/gen/go/`：

- `conf.NewBootstrap()` — 默认值（由 `protoc-gen-defaults` 依据 `(defaults.value)` 注解生成 `Default()`）
- `conf.Validate(cfg)` — 一次返回全部校验问题
- `conf.BindFlags(fs, msg, prefix)` — flag 绑定（替代原 `options.AddFlags`）
- `conf.ResolveTLS(cfg)` — TLS 运行时视图（替代原 `options.TLSOptions`）
- `config.Unmarshal(v, msg)` — viper → proto 桥接

### 7.2 注册与消费闭环

```
业务构造 bootstrap（conf.NewBootstrap，含 proto 注解默认值）
   │
   ├─ appkit.Bind("http", bootstrap.GetHttp())   // proto.Message 分支
   │     → 内部 conf.BindFlags 遍历描述符注册 --http.addr / --http.tls.enabled 等
   │
   ├─ appkit.Run → config.Load（四源合并）→ 得到 *viper.Viper
   │
   └─ appkit.BeforeStart / OnConfigChange 内：
         config.Unmarshal(app.Viper(), bootstrap)   // viper key（http.addr）→ proto 字段
         conf.Validate(bootstrap)
```

**关键**：server 在构造时就持有 `bootstrap.GetHttp()` / `GetGrpc()` 指针，
flag 与 `Unmarshal` 改的都是同一个对象，因此**无需回填**（原 `*httpOpts = *ToSecureServing(...)`
那一步被消除）。

proto 字段名（经 `json_name`）决定键路径，与 flag 前缀天然对齐——
**命令行、env、文件、远程用同一套键路径**。

### 7.3 flag 前缀约定

`conf.BindFlags(fs, msg, prefix)` 的 `prefix` 是**配置键前缀**（不带末尾点，如 `"http"`），
生成的 flag 名为 `<prefix>.<字段名>`，嵌套 message 递归为 `<prefix>.<子消息>.<字段>`：

```go
conf.BindFlags(fs, bootstrap.GetHttp(), "http")  // → --http.addr / --http.tls.enabled
appkit.Bind("http", bootstrap.GetHttp())         // appkit 内部调用它
```

支持标量（string/bool/int32/int64/uint32/uint64/float/double）、`google.protobuf.Duration`
（形如 `"10s"` 的字符串）与嵌套 message（递归，`Mutable` 保证未设置时自动分配）。
`repeated`/`map`/`enum` 暂不支持自动绑定，需业务自行处理。

### 7.4 TLS Smart Mode（pkg/conf/tls.go）

`confv1.Tls` 的 `CA/Cert/Key` 字段支持三种输入形式，`conf.ResolveTLS` 自动识别：
1. 含 `-----BEGIN` 的原始 PEM 字符串；
2. 文件路径（磁盘存在则读取）；
3. Base64 编码的 PEM（K8s Secret 常以 Base64 注入）。

`ResolveTLS(cfg)` 返回 `*tls.Config`（未启用时返回 `nil, nil`），
`Scheme(cfg)` 返回 `http`/`https`，`Validate` 校验 Cert/Key 对称性。

---

## 8. 决策记录（已拍板）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 配置优先级 | ✅ **flag > env > 本地文件 > 远程**（远程与本地同处底层 config、本地后写赢；flag/env 在 override 层压本地，其中 env 高于本地文件；watch 互不污染，详见 4.2/4.3）|
| 2 | 多环境路线 | ✅ **路线 1**（env 进入 path/namespace；本地按 `Name-Env.yaml`，远程 path 用户构造时拼接）|
| 3 | 远程后端范围 | ✅ **接入 Kratos 实现**：`config.FromKratosSource(kratosconfig.Source)` 桥接 etcd/consul/nacos/apollo，bald 核心仅依赖 kratos 核心 config 接口，零后端耦合 |
| 4 | 热更新回调粒度 | ✅ **裸 viper**（`OnConfigChange(*viper.Viper)`），业务自 Unmarshal；本地/远程走同一回调 |
| 5 | 鉴权字段 | ✅ **预留**：鉴权/TLS 由后端构造时持有，`RemoteSource` 接口不携带 |
| 6 | 配置契约形态 | ✅ **proto 契约**（`pkg/conf`，server 直消费 `confv1.*`）。原 `pkg/options` 的 `IOptions{Validate(); AddFlags()}` 已于 2026-08-29 废弃删除：全栈 proto 化后它是多余的二次翻译层，onexstack 仅为借鉴、无需对齐 |
| 7 | flag 前缀拼接 | ✅ `conf.BindFlags(fs, msg, prefix)` 遍历 proto 描述符生成 `<prefix>.<字段>`，嵌套 message 递归；前缀由调用方给（不带末尾点），无双点隐患（见 7.3）|
| 8 | TLS 形态 | ✅ `confv1.Http` 内嵌 `confv1.Tls`（Smart Mode），`NewHTTPServer` 收 `*confv1.Http`，`Tls.Enabled` 控制明文/HTTPS；运行时视图由 `conf.ResolveTLS` 生成 `*tls.Config` |
| 9 | 业务 flag 接入 override 层 | ✅ **已修复**：`appkit.Bind(prefix, opt)`；prefix 用配置键前缀（如 `"http"`），使 flag / env / 文件键路径一致（见 9.1）|
| 10 | 配置契约（schema） | ✅ **框架级配置用 Protobuf**（`pkg/conf` + `api/proto/bald/config/v1`），业务配置不受约束；渐进路线见 [proto 配置契约设计](./proto%20配置契约设计.md) |
| 11 | TLS 键路径 | ✅ 修正为 `http.tls.*`。原 `mapstructure:",squash"` 内嵌使 viper 期望 `http.enabled`，与配置文件实际书写的 `http.tls.enabled` 不符，**配置文件里的 tls 段被静默忽略** |

---

## 9. 取舍与未做项（诚实记录）

1. ~~**业务 flag 接入 viper override 层的桥接尚未闭环**~~ —— **已于 2026-08-29 修复**。

   原问题：`appkit.loadConfig` 传给 `config.Load` 的 `Flags` 仅含内部 `--config`，业务 flag
   由调用方注册到 **全局** `pflag.CommandLine`，未进入 `config.Load.Options.Flags`，
   导致「flag 层」实际为空，业务 flag 压不过本地文件与远程基准。

   修复：新增 `appkit.Bind(prefix, opt)`，在 `loadConfig` 中把配置对象的 flag 注册进
   AppKit 自己的 FlagSet，并整体传给 `config.Options.Flags`（`viper.BindPFlags`）。

   关键点是 **prefix 的语义**：它必须是**配置键前缀**（如 `"http"`），而不是带应用名的
   flag 前缀。因为 viper 以 flag 名作为键路径，只有 flag 名 == 配置键时才对得上：

   | 源 | 写法 | 键路径 |
   |---|---|---|
   | flag | `--http.addr` | `http.addr` |
   | env | `BALD_DEMO_HTTP_ADDR` | `http.addr` |
   | 文件 | `http: { addr: ... }` | `http.addr` |

   应用名隔离由 env 前缀（`BALD_DEMO_`）承担，不需要再叠加到 flag 名上。
   旧的 `Join(appName, "http")` 写法会产生 `--bald-demo.http.addr`，键路径变成
   `bald-demo.http.addr`，与文件/env 对不上，是 flag 失效的另一半原因。

   回归测试见 `pkg/appkit/bind_test.go`。

2. **远程多 KV 合并为单文档**：桥接器对目录型 source 的多 KV 用 `# key=` 注释拼接，适合扁平配置；
   若远程存的是多文件独立命名空间，建议业务自行实现 `RemoteSource` 而非依赖合并。

3. **`AutomaticEnv` 全局生效**：env 会动态覆盖所有底层 key，包括嵌套键（如 `BALD_DEMO_HTTP_ADDR` 覆盖
   `http.addr`）。这是设计预期，但业务若用 `.` 之外的分隔符需注意 replacer 规则。

4. **未做**：配置变更的结构化 diff 日志、变更审计、灰度下发。当前 `OnChange` 仅透传 viper，业务自定。

---

## 10. 与现有 `pkg/config` 的关系

- 保留：viper 作为内存配置中心、`OnConfigChange` 回调、`ConfigFile` / `WatchConfigFile` / flag / env 解析。
- 替换：已删除 `RemoteProvider{AddRemoteProvider/ReadRemoteConfig}` 的 viper remote 用法，
  改为 `RemoteSource` 接口 + `injectRemote` 手动注入（见 `pkg/config/source.go`）。
- 兼容：对外 Option 形态稳定（`RemoteConfig(src RemoteSource)` 取代旧 `RemoteConfig(rp *RemoteProvider)`）。
- 新增：`pkg/conf`（框架级配置契约层，Protobuf；server 直消费 `confv1.*`）。
  原 `pkg/options` 已于 2026-08-29 删除，其职责（默认值/校验/flag 绑定/TLS）已收归 `pkg/conf`。

---

## 11. 参考实现

| 框架 | 方案 | 借鉴点 |
|---|---|---|
| **Kratos** | `Source`/`Watcher` 接口 + `Reader` 深度合并；后端自带 `Format`；watch 由各 SDK 保证 | Source 抽象、格式自声明、多源合并 |
| **go-lulu** | `Reader`/`ValueWatcher`/`Decoder` 正交拆分；etcd `WatchValue` 直接推新字节 | 推值模式（省一次重新 Get）、连接探活 |
| **go-lulu（配置契约）** | `BootstrapConfig` 用 Protobuf 定义，`protojson` 加载，builder 注册表扩展 | **proto 作为配置契约**、`optional` 表达多源可选 |
| **onexstack** | `IOptions{Validate()[]error; AddFlags(fs,prefix)}` + `SecureServingOptions` + TLS Smart Mode | 仅**借鉴**其配置契约与 HTTPS serving 形态的思路；bald 已改为 proto 契约，不对其 `IOptions` 保持兼容 |
| **viper（仅本地）** | `SetConfigFile`/`ReadInConfig`/`WatchConfig` | 本地加载与热更新壳 |

---

## 11.1 框架级配置契约（Protobuf）

自 2026-08-29 起，框架级配置（app / http / grpc / log）引入 Protobuf 作为**配置契约**，
viper 仍作为**加载器**不变。两者的分工：

```
配置文件 / env / flag / 远程中心
        ↓  viper（pkg/config）多源合并 + 热更新
   *viper.Viper
        ↓  config.Unmarshal(v, msg)  ← 类型规范化 + protojson
   confv1.Bootstrap（pkg/conf，Protobuf，强类型，唯一真相源）
        ↓  直接传子消息指针（bootstrap.GetHttp() / GetGrpc() / GetLogger()）
     server 层
```

> 2026-08-29（P1 阶段 2）：已去掉 `conf.ToSecureServing / ToGRPCOptions / ToLogOptions`
> 这一层 proto → options 的反向适配，server 直接消费 proto。默认值由 proto 的
> `(defaults.value)` 注解生成（`cfg.Default()`），校验由 `conf.Validate`，
> flag 由 `conf.BindFlags`，TLS 运行时视图由 `conf.ResolveTLS`。

收益：字段名与类型编译期可查，消除 `mapstructure` tag 字符串匹配导致的静默失效
（本次改造即发现两个：业务 flag 未生效、配置文件 `http.tls` 段被忽略）；
并彻底消除 proto 与 options 的双真相源。

完整设计、迁移路线与坑位记录见 **[proto 配置契约设计](./proto%20配置契约设计.md)**。

---

## 12. 可运行示例

完整的「本地 + 远程 + 多环境 + 热更新 + proto 契约回填」可运行示例见 [`_example/bald/main.go`](../../../_example/bald/main.go)：

- 通过 `appkit.ConfigFile` / `appkit.Env` / `appkit.WatchConfigFile` / `appkit.RemoteConfig` / `appkit.OnConfigChange` 演示本文全部决策点；
- 远程后端给出 **etcd** 与 **nacos** 两种 `config.FromKratosSource` 桥接写法（含 `go get` 提示）；
- nacos 同时作为**注册中心 + 配置中心**的接线示例见
  [`_example/bald/register_nacos.go`](../../../_example/bald/register_nacos.go)（build tag `nacos`，
  默认构建不引入 nacos SDK、无需 nacos 进程即可编译运行，用 `-tags nacos` 才接入真实后端）；
- 框架级配置按 proto 契约解析：`appkit.Bind("http", bootstrap.GetHttp())` 注册 flag，`BeforeStart` 内 `config.Unmarshal(v, bootstrap)` + `conf.Validate(bootstrap)` 解析（原 `pkg/options` 已废弃删除，不再用 `AddFlags`/`Unmarshal(&opts)`）；
- 该示例同时演示了 `pkg/log` 全局日志接入（`--log.*` flag + `FilterKey` 脱敏）与 `appkit.Registrar(inmemory.New())` 服务注册端到端流程；
- 本地配置样例见 [`configs/bald-demo.yaml`](../../../configs/bald-demo.yaml)，说明远程同名 key 作基准、本地覆盖的语义。
