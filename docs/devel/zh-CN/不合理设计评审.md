# bald 不合理设计评审

> 评审范围：`pkg/appkit`、`pkg/conf`、`pkg/config`、`pkg/store`、`pkg/middleware`（bundle/gin/grpc）、`pkg/audit`、`pkg/authn`、`pkg/authz`、`pkg/log`、`pkg/web`、`pkg/options`。
> 方法：源码走读 + 关键路径实测核对。每条标注 **[严重度]** 与 **[文件:行]**，严重度分为 高（安全/正确性）/ 中（API 与一致性）/ 低（死代码/风格）。
> 结论：框架整体方向（proto-first、效应账本、归一化、零后端耦合）合理；问题集中在**安全降级陷阱、全局状态多出口、构造期快照、归一化非默认**四处，建议优先修高/中危项。

---

## 一、高危（安全 / 正确性）

### D1 `MustTLSConfig` 失败降级为 `InsecureSkipVerify`
**[高] `pkg/conf/bindflags.go:348-356`**

```go
func MustTLSConfig(...) *tls.Config {
    if tlsConfig, err := buildTLSConfig(...); err != nil {
        // 注意：失败时降级为「跳过证书校验」而不是报错。
        return &tls.Config{InsecureSkipVerify: true}
    }
    return tlsConfig
}
```

`Must` 前缀按惯例是「出错即 panic」，但此处反而**静默关闭证书校验**。一旦证书路径配错/缺失，框架对外暴露的 TLS 端点等于明文中间人，且调用方完全无感知。

- 影响：安全反模式，运维误配即被降级为不安全链路。
- 建议：`Must` 失败应 `panic`（或返回 error 交由调用方 fail-fast）；绝不在 `_ = buildTLSConfig()` 出错时回退到 `InsecureSkipVerify`。如确需「开发期跳过校验」，用显式开关 `WithInsecureSkipVerify()` 而非默认行为。

---

### D2 日志脱敏 `FilterKey` 被 `With` / `WithAttrs` 绕过
**[高] `pkg/log/filter.go`（`WithAttrs` / `WithGroup` 未对传入 attrs 过滤）**

`filterHandler.WithAttrs(attrs)` 把属性直接转交 `h.next.WithAttrs(attrs)`，未过 `filter`：

```go
func (h *filterHandler) WithAttrs(attrs []slog.Attr) slog.Handler {
    return &filterHandler{next: h.next.WithAttrs(attrs), filter: h.filter} // attrs 不过滤
}
```

slog 中 `WithAttrs` 的属性在 `Handle` 阶段由内层 `withAttrs` 合并进 record，但本层 `filterHandler` 在 `WithAttrs` 时已把属性下沉到 `next`，导致这些属性**绕过了最外层 filter 的 `Handle` 过滤**（`FilterKey("password")` 对 `slog.With("password", x)` / `handler.WithAttrs(...)` 携带的字段无效）。

- 影响：脱敏是合规红线，旁路即等于明文落盘。与「脱敏是安全特性」的定位直接矛盾。
- 建议：在 `WithAttrs` 内对传入 attrs 先做 `a := h.filter(a)` 再 `h.next.WithAttrs(a)`（或统一在 `Handle` 阶段对 `r.Attrs` 全量过滤，包括来自 `WithAttrs` 的属性）。补充针对 `With("password",...)` 的单元测试。

---

### D3 认证失败（401/403）不产生审计事件
**[高] `pkg/middleware/gin/authn.go:29-34,46` + bundle 链序；`pkg/audit/audit.go:28-29`**

gin 认证中间件在失败时 `c.AbortWithStatusJSON(...)` 后 `return`，**不调用 `c.Next()`**：

```go
// gin/authn.go
token, err := bearerFromHeader(...)
if err != nil {
    c.AbortWithStatusJSON(...); return   // 直接返回，后续中间件（含 audit）被跳过
}
```

bundle 两条链中 `Audit` 都注册在 `Authn` 之后（gin：`Recovery→RequestID→Logging→CORS→Secure→Authn→Audit→Authz`；gRPC：`Error→RequestID→Observability→Authn→Audit→Authz`）。因此认证失败（缺 token / 校验失败 / 过期）时 `Audit` 根本不执行——gin 侧 `Audit` 中间件不被调用，依赖 `c.Writer.Status()==401/403` 派生 deny 的逻辑永远触不到；gRPC 侧 `AuthnInterceptor` 直接返回 Unauthenticated，内侧的 Audit 拦截器同样被跳过（**双传输同病**）。

而 `pkg/audit/audit.go` 文档明确写：`ResultError` = 「**含认证失败**/内部错误」——该语义在 authn abort 路径永远不会被触发。需精确化边界：示例中 `/v1/login` 这类**未挂 Authn 的路由**，其登录失败（业务层 401）仍会被全局链上的 Audit 记录；缺口是**受保护路由的 Authn abort 路径（401）与 gRPC Unauthenticated**——即 token 缺失/过期/无效的访问在两条传输上都无审计痕迹。

- 影响：合规审计缺口——登录爆破、越权试探、token 失效重试全部无痕迹；文档与实现矛盾。
- 建议：审计应下沉为**最外层**（在 Authn 之前注册，或 Authn 失败分支显式发一条 `ResultError` 审计事件），保证 401/403 必留痕。

---

### D4 审计后端多路径 → 分裂（split-brain sink）
**[高] `pkg/middleware/bundle/bundle.go`（`ginAudit`/`grpcAudit`）+ `pkg/appkit/mount.go:176` + `pkg/middleware/gin|grpc/audit.go`**

存在**三条**审计后端解析路径，优先级不统一：

1. `bundle` 的 gin/grpc audit：仅用 `b.auditor`，**为 nil 时落到 `NopAuditor`，不回退全局**。
2. `pkg/appkit/mount.go:176`：`audit.GetAuditor()`（全局）记录组件挂载/卸载审计。
3. 直接调用 `gin/grpc.AuditMiddleware`（不经 bundle）默认 `audit.GetAuditor()`（全局）。

后果：用 `bundle.Audit(a)` 注入审计器后，请求级审计走 `a`，但 `appkit.MountComponent` 的组件重组审计仍走全局（可能为 nop）；且 `appkit` 没有 `Auditor` Option，无法注入。`pkg/audit/global.go` 的 `SetAuditor/GetAuditor` 与 bundle 的注入互不连通。

- 影响：同一进程两条审计出口，部分事件静默丢弃或写错后端，排查时极难发现。
- 建议：统一**单一解析顺序**（注入优先 → 全局 → nop），三条路径复用同一个 `resolveAuditor()`；`appkit` 增加 `Auditor` Option 以消费注入实例。

---

### D5 gRPC 链缺显式 Recovery，且 `metrics.Record` 未被旁路 recover 覆盖
**[中/高] `pkg/middleware/bundle/bundle.go`（`GRPCInterceptors`/`GRPCChain`）+ `pkg/middleware/grpc/audit.go:116`**

- gin 链首道是 `ginmw.Recovery()`，gRPC 链**没有对应的 recovery 拦截器**（不对称）。
- `grpcAudit` 中 `rec.Record(...)`（metrics）写在 `recordSafely(...)` **之外**：

```go
// grpc/audit.go
recordSafely(ctx, func() { auditor.Record(ctx, event) }) // 仅此被 recover
if b.metrics != nil { b.metrics.Record(ctx, metrics...) } // 未被 recover，panic 逃逸
```

- 影响：与文档「旁路不阻断」承诺矛盾；metrics recorder panic 直接中断请求，且 gRPC 无兜底恢复。
- 建议：把 `metrics.Record` 也纳入 `recover` 保护（与 auditor 同级）；gRPC 链按需加 `otel/grpc` 或 `grpc.RecoveryInterceptor`（注意与「ErrorInterceptor 最外层」约定共存）。

---

## 二、中危（API / 一致性）

### D6 `AuditMiddleware` 首参 `audit.Auditor` 被丢弃（死参）
**[中] `pkg/middleware/gin/audit.go:46`、`pkg/middleware/grpc/audit.go:55`**

```go
func AuditMiddleware(_ audit.Auditor, opts ...Option) gin.HandlerFunc { ... }
```

首参 `_` 在签名里但**从未使用**，实际生效的只有 `AuditWithAuditor` option。文档示例与调用方按直觉传入位置审计器会**静默无效**，误导性强。

- 建议：删除首参，统一用 `AuditWithAuditor`；或真正消费首参（无 option 时回退首参）。

---

### D7 P9 归一化是 opt-in，默认仍是双命名空间 RBAC
**[中] `pkg/middleware/bundle/bundle.go:85`（`Normalized()` 默认 `false`）**

```go
func (b *Bundle) Normalized() bool { return b.normalized } // 默认 false
// grpcAuthz / grpcAudit 仅在 b.normalized 时做归一化
```

P9 的核心收益是「根治 REST/gRPC 双命名空间 RBAC」，但该行为必须显式 `bundle.Normalized()` 才生效，漏配即回到旧问题。这与「归一化是默认正确行为」的预期相悖。

- 建议：默认开启归一化（向后兼容可由 opt-out 控制），或启动期检测到未归一化时打印告警。

---

### D8 中间件构造期快照全局 Logger，运行期 `SetLogger` 无效
**[中] `pkg/middleware/gin/observability.go:139`、`pkg/middleware/grpc/observability.go:135`（audit 同）**

```go
config := &ObservabilityOptions{ ..., Logger: log.GetLogger() } // 构造期固化
```

中间件在构造时捕获 `log.GetLogger()`，之后全局 logger 被替换也不影响已构造的中间件。示例 `main.go` 的时间线直接暴露该问题：

- `:63` `setLogger(logOpts)`（带 `FilterKey("password")`）
- `:128` `ginBundle.Gin()` 构造 observability/audit 中间件 → 快照了 `:63` 的 logger
- `:262`（BeforeStart）`setLogger(baldconf.LogOptions(...))` 重建全局 logger

结果：**请求级日志**用 `:63` 的旧实例，**app 级日志**（`appkit`、组件审计）用 `:262` 的新实例，两者分裂；且脱敏 FilterKey 行为在两实例间可能不一致。

- 影响：运行期换 logger（不同 level/format/transport）只影响部分日志；脱敏可能在请求链路上失效。
- 建议：中间件每次请求惰性 `log.GetLogger()` 解析，或要求 `WithLogger` 在构造前显式注入同一实例（并在示例里先 `setLogger` 完再构建 bundle）。

---

### D9 HTTP 错误响应体形状不统一
**[中] `pkg/web/response.go:14-22` vs `pkg/middleware/gin/authn.go:29`**

- `pkg/web.WriteResponse`/`ErrorResponse` → `{ "error": { "code", "message" } }`
- gin `AuthnMiddleware` → `gin.H{ "reason", "message" }`

认证失败是最高频的错误响应，却用了与全站 `web` 包不同的 JSON 形状，客户端/网关按统一契约解析会漏字段。

- 建议：`AuthnMiddleware` 复用一个统一错误体构造器（如 `web.ErrorResponse` 等价的 helper），保证全链路形状一致。

---

### D10 `ResolveTLS` 提供 CA 即强制 mTLS（要求客户端证书）
**[中] `pkg/conf/bindflags.go:303-316`**

```go
if sc.CA != "" {
    c.ClientAuth = tls.RequireAndVerifyClientCert // 双向 mTLS
}
```

仅想校验证书签发链（服务端单向 TLS 校验对端链）却会被强制要求客户端证书——典型部署反模式（大多数对外服务不需要客户端证书）。

- 建议：用独立开关区分 `RootCA`（服务端验证对端链）与 `ClientCA`（mTLS 强制客户端证书），不要由 CA 非空隐式触发 mTLS。

---

## 三、低危（死代码 / 一致性）

### D11 `pkg/config.Marshal` 导出后无调用方（疑似死代码）
**[低] `pkg/config/config.go:260`**

全仓搜索 `config.Marshal(` 仅命中定义处，无调用方。导出一个无消费者的序列化函数易误导为「支持配置写回」能力。

- 建议：删除，或补一个配置导出/备份场景的测试与文档说明。

---

### D12 `pkg/conf` 强耦合 `pkg/log`，与 `pkg/store` 去耦风格不一致
**[低] `pkg/conf/conf.go:27`（`import "github.com/kalandramo/bald/pkg/log"`）**

`pkg/store` 为可测试性刻意去除对 `pkg/log` 的直接依赖（改用 context 透传，见 `pkg/store/logger.go`），而 `pkg/conf`（同属框架契约层）却硬依赖 `baldlog`。同一层出现两种日志获取风格。

- 建议：统一为「全局 `log` 门面惰性获取」或统一 context 透传，消除核心模块硬依赖。

---

## 四、附：已合理 / 无需改（避免误报）

- `recordSafely` 吞 `Auditor` panic 是刻意的旁路语义（已在文档声明），**仅 D5 的 metrics.Record 逃逸属遗漏**，不是该设计本身的问题。
- `SetLogger/GetLogger` 全局门面本身是合理抽象；D8 的症结在**消费方构造期快照**，不在门面。
- `MustTLSConfig` 的「失败降级」才是问题；`buildTLSConfig` 的错误传播逻辑本身 OK。

---

## 修复优先级建议

| 优先级 | 项 | 理由 |
|---|---|---|
| P0 | D1 | 安全降级，一行即中招 |
| P0 | D2 | 脱敏旁路，合规红线 |
| P0 | D3 | 审计缺口，文档已自相矛盾 |
| P1 | D4 | 分裂 sink 致事件丢失 |
| P1 | D5 | 旁路承诺未兑现 |
| P1 | D7 | P9 收益默认不生效 |
| P2 | D8 / D9 / D10 / D6 | API/一致性，影响维护与对接 |
| P3 | D11 / D12 | 死代码/风格 |

---

## 五、可精简代码（冗余 / 死代码）

> 评审方法：对候选符号在 `bald` 全仓（含 `pkg/**`、`_example/**`、`internal/codegen`、`*_test.go`）用 `search_content` 做调用点确认。判定口径：**只有定义、无真实调用点（仅测试/注释/文档引用）即标为死代码**。每条标注 **[类型: 直接可删 / 需决策]** 与 **[文件:行]**。
>
> 说明：「直接可删」指纯转发/零语义/无保留价值的死代码；「需决策」指看似无调用、但属**预留插件体系或可选能力层**的符号——删之减负担，留之保扩展性，由你定夺。

### E1 [直接可删] gin/grpc 共 15 个 `ObservabilityWithXxx` 便捷函数 + 其独占 setter
- `gin/observability.go:374-402`（5 个 `ObservabilityWith*`）、`grpc/observability.go:402-459`（10 个 `UnaryObservabilityWith*` / `StreamObservabilityWith*`，外加核心流拦截器 `StreamObservability` 也零装配）。
- 全仓对其**零调用**（仅定义 + 文档表）。它们各自只转发一个底层 `WithXxx` option，且这些底层 setter（`gin/grpc` 的 `WithTraceInjection`/`WithCustomTraceHeader`/`WithSkipPaths`/`WithSkipMethods`/`WithLogger`/`WithDisableBodyLog`/`WithSkipMetrics`）**除 `gin.WithSkipMetrics` 被 `logging.go:8 Logging()` 使用外，其余全部仅被这些死便捷函数引用**。
- 建议：删除 15 个便捷函数 + 上述无人调用的 setter（保留 `Observability`/`UnaryObservability`、结构体字段与 `gin.WithSkipMetrics`）。删除后 `go build ./...` 不受影响（核心只用 `for _, opt := range opts { opt(config) }` 迭代，不主动调这些 setter）。同步清理 `docs/.../框架契约总览.md:270` 的「便捷变体」表，避免文档与代码漂移。

### E2 [直接可删] `pkg/conf` 三处纯转发 / 死分支
- `bindflags.go:367` `ValidateAddress`（转发 `validateAddress`，零调用方；真正用 `validateAddress` 在 `conf.go:92/114`）。
- `bindflags.go:349` `MustTLSConfig`（生产 `pkg/server/http_server.go:148` 直接用 `ResolveTLS` 自行处理错误，零调用；且与 D1 安全降级问题同源，删之顺手消除隐患）。
- `conf.go:149` `cfgToLogOptions`（仅 `conf.go:78` 一处内联可用，转发 `LogOptions`）。
- 建议：前三处直接删；`cfgToLogOptions` inline 进 `conf.go:78` 后删。删 `MustTLSConfig` 后清理 `bindflags.go` 里仅它使用的 `log/slog` import。

### E3 [直接可删] `pkg/config.Marshal` + 整个 `encoder.go`
- `config.go:260` `Marshal` 全仓零调用；其唯一消费者 `encoder.go`（`encoder` 结构仅 2 字段、仅 `Encode` 一个方法）随之无价值。
- 建议：删除 `Marshal` 与 `encoder.go`，并清理连带的 `bytes`/`encoding/json`/`gopkg.in/yaml.v3` import。

### E4 [直接可删] `bindflags.go` 死变量与重复 doc
- `bindflags.go:108` `bindDurationFlag` 内 `d` 取出仅 `_ = d` 空操作，从未真正使用。
- `bindflags.go:20-29` 与 `:30-43` 是**逐字重复**的 `BindFlags` 文档（且还提及已废弃的 `options.AddFlags`，易误导）。
- 建议：去掉 `_ = d`，删重复 doc 副本一份。

### E5 [可合并] gin 与 grpc 可观测性平行实现
- 头常量块、`TraceInjectionMode` 类型 + iota、`Option` 类型与多数 setter、`matchWildcard`、`injectTraceHeaders`/`injectTraceMetadata` 在 `gin/observability.go` 与 `grpc/observability.go` 几乎逐行重复（两侧 `:28-106`、`matchWildcard` `:303-325`/`:329-344`、`injectTrace` `:328-372`/`:347-390`）。
- 建议：抽取到新建 `pkg/middleware/shared`：`TraceInjectionMode`、常量、`Option`、`matchWildcard`、传输无关的 `InjectTrace(mode, traceID, spanID, spanCtx, set func(string,string))`（gin 传 `c.Header`、grpc 传 `md.Set`）。两侧只留「装配拦截器 + 调 shared」薄层。属「可合并/可简化」而非删除，收益是「改一处即两处生效」。E1 的死代码清理应先行，降低合并噪音。

### E6 [直接可删] `gin/logging.go:11-14` `RequestID()` 与 `RequestIDMiddleware` 别名
- `RequestID()` 仅一行转发 `RequestIDMiddleware()`，两者指同一件事（`bundle.go:111` 与 `_example/bald/main.go:91` 用的是 `RequestID()`，非死代码但属冗余别名）。
- 建议：合并为一个名字（保留 `RequestIDMiddleware` 或 `RequestID` 其一）。

### E7 [需决策] `appkit/registry.go` 预置实例 + 泛型 `NewRegistry[T]` + `RegisterStoreProvider`
- `ServerRegistry`(`registry.go:74`)、`MiddlewareRegistry`(`:78`) 全仓 **0 引用**（连测试都没碰）；`ProviderRegistry`(`:82`) 仅 `appkit_test.go` 引用；`RegisterStoreProvider`(`:91`) 仅注释引用；`NewRegistry[T]`(`:29`) 仅测试引用。生产/示例/codegen 真实调用：**0**。
- 这是「为未来插件自注册体系预留但未落地」的脚手架（注释明示 grpc/http/gateway 变体插件、存储后端工厂）。
- 建议：**若短期内不接入插件自注册（`import _ ".../register"` 桥接子模块），整体删 `registry.go:71-93` 及 `NewRegistry`/测试**，待需要时再恢复；若想保留扩展性，至少补一个真实接线示例以免沦为死 API。

### E8 [需决策] `appkit` 7 个无调用 Option
- `appkit.ID`(`:193`)、`KratosRegistrar`(`:199`)、`Env`(`:218`，仅 `_example/bald/main.go:237` **被注释掉**)、`BeforeStopTimeout`(`:235`)、`AfterStopTimeout`(`:239`)、`effect.go:55` `EffectTimeout`、`component.go:68` `ComponentTimeout`。
- 其余 Option（`Name`/`Version`/`Servers`/`Provides`/`Requires`/`Reconcile`/`Effect` 等）均已被 codegen 模板或 `_example` 调用，唯此 7 个零调用。
- 建议：属标准 functional-option 写法（每个只设一字段），本身合理，但当前确无消费者。保留 API 面（它们表达合法配置意图）或在 `_example` 补一例真实调用即可；若确认不对外保留可删。

### E9 [需决策] `pkg/store/mapper.go` 整包（DTO/Entity 可选映射层）
- `Mapper[DTO,Entity]` 接口、`CopierMapper`、`NewCopierMapper`、`ToEntity`/`ToDTO`（反射 `copyFields`）仅 `mapper.go`(定义) 与 `mapper_test.go`(测试) 使用；业务 handler（`_example/bald/user/entity.go`）、`contrib/store-gorm` 均未采用。文件头注释自述「可选使用，不强制所有 Store 采用」。
- 建议：当前仓库只用 `Store[T]` 直操作实体 T，DTO/Entity 分离层从未落地。可整体删 `mapper.go`(~87 行)+测试；若计划引入 DTO 分离再引入。**D11 已提及 `Marshal`；此处 mapper 同理属「可选能力零采用」**。

### E10 [需决策] `pkg/store` 其他零调用符号
- `store.WithPageSize`/`WithMaxPageSize`/`WithLogger`(`store.go:67/72/77`)、`Store.Provider()`(`store.go:94`)、`store.FromLogger`/`logAdapter`(`logger.go:22/38`)、`contextx.UsernameFromContext`(`context.go:47`)。全仓零真实调用（`NewStore` 直接用字面量设默认，三 Option 从不被传入；`FromLogger` 为「桥接 pkg/log」准备但无调用方；`UsernameFromContext` 写入端 `WithUsername` 被 `authn` 调用但读取端从未用——与 `WithUserID`/`UserIDFromContext` 成对使用的范式不对称）。
- 建议：若确认不保留这些可配置入口/适配入口，删除即可；否则至少补一例调用，避免沦为「看似可用实则死」的 API。

### E11 [可合并] appkit 内部重复
- `mount.go:166` `auditComponent` 与 `reconcile.go:200` `auditReconcile` 构造 `AuditEvent` 近乎逐行重复（缺 `TenantID`，对比 gin/grpc 审计中间件会填 `TenantID: tenant`）；`component.go:77` 与 `mount.go:102` 出现完全相同错误包裹串 `fmt.Errorf("appkit: component %q start: %w", ...)`；`store/tenant.go` 与 `scope.go` 的「遍历全局 func 注册表 → append 进 Where」同构。
- 建议：抽内部 helper（如 `auditReorg(ctx, action, object, name, err)`、`wrapComponentStartErr(name, err)`、通用 `mergeRegisteredConditions`）消除重复并统一审计纪律（顺带补 `claims.TenantID`）。

### 精简优先级建议

| 优先级 | 项 | 类型 | 动作 |
|---|---|---|---|
| 高 | E1 | 直接可删 | 删 15 便捷函数 + 独占 setter，清理文档表 |
| 高 | E2 | 直接可删 | 删 `ValidateAddress`/`MustTLSConfig`/`cfgToLogOptions` inline |
| 高 | E3 | 直接可删 | 删 `config.Marshal` + `encoder.go` |
| 中 | E4 | 直接可删 | 去 `_ = d`、删重复 doc |
| 中 | E5 | 可合并 | 抽 `shared` 子包收敛 gin/grpc 平行实现 |
| 中 | E6 | 直接可删 | 合并 `RequestID` 别名 |
| 中 | E7 | 需决策 | 删未接线的 registry 脚手架，或补示例 |
| 中 | E8 | 需决策 | 7 个死 Option 删或补调用 |
| 中 | E9 | 需决策 | 删未采用的 mapper 整包 |
| 低 | E10 | 需决策 | store/contextx 零散零调用符号清理 |
| 低 | E11 | 可合并 | appkit/store 内部重复 helper 化 |

---

## 六、正确性核验记录（2026-09-02 二次核验）

> 方法：对 D1-D12 / E1-E11 逐条独立复核——重读源码原文 + 全仓调用点搜索（覆盖 `pkg/**`、`internal/**`、`_example/**`、`examples/**`、`*_test.go`）。核验中发现并**当场修正 1 处笔误、精确化 2 处边界**（见下表"备注"）。结论：**23 项全部证实，无虚构项**，可进入执行。

| 编号 | 结论 | 备注（证据/修正） |
|---|---|---|
| D1 | ✅ 证实 | `bindflags.go:348-356` 原文确认 fail-open 返回 `InsecureSkipVerify:true` |
| D2 | ✅ 证实（范围精确化） | 绕过路径确认：`slog.go:189` `slogLogger.With` → `slog.Logger.With` → `handler.WithAttrs`（filterHandler 未过滤）。**精确化**：仅 `With`/`WithAttrs` 固化路径绕过；每次日志调用的直接参数（`Info(ctx,msg,"password",x)`）仍被 `Handle` 过滤 |
| D3 | ✅ 证实（笔误已修正） | 原文误把 gRPC 链序标为 "gin 链序"，已修正为双链各自正确；结论不变。**补充**：gRPC 侧 AuthnInterceptor 返回 Unauthenticated 同样跳过内侧 Audit——双传输同病。**边界精确化**：无 Authn 的路由（如 /v1/login）业务层 401 仍被全局链 Audit 记录，缺口为受保护路由 authn abort 路径 |
| D4 | ✅ 证实 | bundle.go:186-190（nil→Nop 不吃全局）、mount.go:176（全局）、appkit 无 Auditor Option，三点均复核 |
| D5 | ✅ 证实 | 全 `pkg` 仅 `gin/recovery.go` 存在 Recovery，gRPC 侧无；`grpc/audit.go:116` `rec.Record` 在 recordSafely 保护外 |
| D6 | ✅ 证实（加重） | **新证据**：`grpc/audit_test.go:150-164` `TestAuditInterceptor_PanickingAuditorSafe` 把 `panicAuditor` 传给被丢弃的首参，opts 未注入 → 实际执行全局 nop auditor → 测试对 recover 逻辑**零覆盖而通过**，是死参危害的实证 |
| D7 | ✅ 证实 | bundle.go:85 `normalized` 默认 false |
| D8 | ✅ 证实 | gin/grpc observability 构造期 `Logger: log.GetLogger()`；示例时间线 `main.go:63(setLogger)→:128(Gin())→:262(重建)` 逐行核对 |
| D9 | ✅ 证实 | `web.ErrorBody{error:{code,message}}` vs `authn.go:29` `gin.H{reason,message}` |
| D10 | ✅ 证实 | `bindflags.go:303-316` CA 非空即 `RequireAndVerifyClientCert` |
| D11 | ✅ 证实 | `config.Marshal` 全仓零调用 |
| D12 | ✅ 证实 | conf.go:27 import pkg/log；store/logger.go 注释自证去耦意图 |
| E1 | ✅ 证实 | 15 个便捷函数全仓（含 examples/_example）仅定义 + 框架契约总览.md:270 文档表 |
| E2 | ✅ 证实 | 三符号零调用/唯一内联点确认 |
| E3 | ✅ 证实 | Marshal+encoder.go 零调用 |
| E4 | ✅ 证实 | `_ = d` 死赋值 + doc 逐字重复 |
| E5 | ✅ 证实 | gin/grpc 平行实现逐行对照成立 |
| E6 | ✅ 证实 | `RequestID()` 一行转发，调用方 bundle.go:111 + _example |
| E7 | ✅ 证实 | ServerRegistry/MiddlewareRegistry 全仓 0 引用；ProviderRegistry/NewRegistry/RegisterStoreProvider 仅测试/注释 |
| E8 | ✅ 证实 | 7 个 Option 全仓 0 真实调用（Env 仅被注释） |
| E9 | ✅ 证实 | mapper.go 仅自身 + 测试 |
| E10 | ✅ 证实 | store 零散符号 0 调用；UsernameFromContext 写读不对称 |
| E11 | ✅ 证实 | auditComponent/auditReconcile、错误包裹串、tenant/scope 同构均确认 |

---

## 七、执行计划

> **总原则**：安全红线先行 → 行为正确性 → API 破坏性修正**集中一批**（减少版本噪音）→ 卫生删除。每阶段验证门：`go build ./...` + `go test -race ./pkg/...` 全绿；涉及 contrib/示例的阶段加跑对应 module。破坏性变更集中在 v0.2.0 发布（当前 v0.1.0 个人框架可接受 breaking，见文末清单）。

### 阶段 0：基线（规模 S）
- 记录当前 `go build ./...` + `go test -race ./pkg/...` 结果为基线，后续每阶段对照。

### 阶段 1：P0 安全修复（D1 + D2 + D3，规模 M）
1. **D1**：删除 `MustTLSConfig`（零调用，删比修好——不做"改 panic"的无谓维护）。与 E2 合并执行，同步清理 `log/slog` import。
2. **D2**：`filterHandler.WithAttrs` 先 `h.filter(a)` 过滤再下沉 `next.WithAttrs`；新增测试：`logger.With("password",x).Info(...)` 与构造期 `WithAttrs` Option 两条路径均断言 `"***"`。
3. **D3**：Authn 失败分支**显式发审计事件**（`ResultDeny`，Subject 留空、Meta 带 reason；旁路不阻断 abort 本身）——gin 与 gRPC 拦截器各一处；bundle 把 `b.auditor` 注入 Authn 层（新增 option），裸用路径走 `audit.GetAuditor()`。补测试：受保护路由 401 必产生审计事件（双传输）。
   - *不采用*「Audit 移到 Authn 外侧」方案：gRPC 拦截器 ctx 不外传，外侧 Audit 读不到 subject，会牺牲认证成功请求的审计质量。

### 阶段 2：P1 行为正确性（D4 + D5 + D7 + D8，规模 M）
4. **D4**：新增 `appkit.Auditor(a)` Option；appkit 内部统一 `resolveAuditor()`（显式注入→全局→nop），`mount.go`/`reconcile.go` 审计改走它。bundle 语义不变（显式注入），包文档写明单一真相源约定。
5. **D5**：抽 `safeEmit` 同时保护 `auditor.Record` 与 `rec.Record`（两处 transport 各一份，或下沉到 shared）；自写 ~20 行 gRPC recovery 拦截器（berrors.Internal 收口，不引外部依赖，符合防漂移清单），bundle 的 `GRPCInterceptors` 插在 Error 之后第二外层。
6. **D7**：`normalized` 默认 `true`，新增 `Raw()` opt-out Option（零破坏入口）；更新 bundle 契约测试（原"未开归一化语义兼容"测试改走 `Raw()`）。
7. **D8**：observability/audit 中间件去掉构造期默认 `Logger: log.GetLogger()`，改为 `config.Logger == nil` 时**请求期惰性**取全局；`WithLogger` 显式注入优先级不变。示例无需改动即恢复一致。

### 阶段 3：P2 API 破坏性修正（D6 + D9 + D10，规模 M，集中 breaking）
8. **D6**：签名改 `AuditMiddleware(opts ...AuditOption)` / `AuditInterceptor(opts ...AuditInterceptor)` 同构；同步改 bundle.go:194/226、测试 9 处、doc 注释。**顺带修复 D6 加重项**：`TestAuditInterceptor_PanickingAuditorSafe` 改为 `AuditWithAuditor(panicAuditor{})` 真实验证 recover。
9. **D9**：authn 错误响应改用 `web.ErrorBody` 形状（`{error:{code,message}}`）；核对示例/测试对旧形状的断言。
10. **D10**：proto `Tls` 拆 `root_ca`（服务端验证对端链）/ `client_ca`（mTLS 强制客户端证书）；`ca` 字段保留并映射 `root_ca`（标注弃用）；跑 `task proto-config` 重新生成并提交生成物。

### 阶段 4：P3 卫生删除（E1-E4 + E6，规模 S，纯删不加）
11. 按清单删：15 个便捷函数 + 独占 setter（保留 `gin.WithSkipMetrics` 与核心函数）、`config.Marshal`+`encoder.go`、`ValidateAddress`/`cfgToLogOptions`(inline)、`_ = d`、重复 doc、`RequestID` 别名合一。
12. 同步清理 `框架契约总览.md:270` 便捷变体表。每删一批跑验证门。

### 阶段 5：决策项（E5 / E7-E11，默认不动，规模 —）
等拍板后独立执行，每项独立 PR 便于回退：
- E5 gin/grpc shared 合并（收益：修一处两处生效；成本：中，建议在 E1 清理后做）
- E7 registry 脚手架：删（我的建议）或补真实接线示例
- E8 七个死 Option：删或补示例调用
- E9 mapper 整包：删（建议，DTO 需求出现再引入）
- E10 store/contextx 零散符号：删或补调用
- E11 内部重复 helper 化（顺带补 appkit 审计事件的 `TenantID`）

### 阶段 6：收尾验证与文档同步（规模 S）
13. 全量验证：核心 `go build ./... && go test -race ./pkg/...`；三个 contrib module 与 `_example`（`GOFLAGS=-mod=mod`）build/test；`examples/go-bald-admin` build（bundle 签名变更影响面核对）。
14. 本文勾选已完成项；`框架契约总览.md`/`架构优化路线.md` 同步契约变更；打 tag `v0.2.0`。

### 破坏性变更清单（v0.1.0 → v0.2.0）
| 变更 | 影响 | 迁移 |
|---|---|---|
| D6 签名 | `AuditMiddleware/AuditInterceptor` 少一个参数 | 删首参 `nil` |
| D7 默认归一化 | 未显式 `Normalized()` 的 bundle 行为变化 | 需旧语义时加 `Raw()` |
| D9 响应体 | authn 401 JSON 形状变化 | 客户端解析改 `error.code` |
| D10 proto | `Tls.ca` 弃用 → `root_ca`/`client_ca` | 改配置键 |
| E1-E4/E6 删除 | 死 API 不可用（全仓无调用方） | 无需迁移 |

### 工作量与顺序依赖
- 阶段 1 → 2 → 3 → 4 严格顺序（阶段 3 的 D6 测试改动依赖阶段 2 的 D5 safeEmit 落点）；阶段 4 与 3 可互换；阶段 5 永不阻塞 1-4。
- 预估：阶段 1 约 3 文件 + 4 测试；阶段 2 约 6 文件 + 6 测试；阶段 3 约 8 文件（含 proto 重新生成）；阶段 4 约 5 文件纯删除。

### 执行记录（2026-09-02，阶段 0-4 + 6 已完成，阶段 5 决策项未动）

| 阶段 | 状态 | 关键改动 |
|---|---|---|
| 0 基线 | ✅ | `go build ./...` + `go test -race ./pkg/...` 全绿 |
| 1 P0（D1/D2/D3） | ✅ | 删 `MustTLSConfig`（含 E2 合并：`ValidateAddress`、`cfgToLogOptions` inline）；`filterHandler.WithAttrs` 先过滤再下沉 + 2 条固化路径测试；gin/gRPC `Authn` 失败显式发 `ResultDeny` 审计事件（`AuthnWithAuditor` 注入口，bundle 把 `b.auditor` 注入 Authn 层）+ 双传输契约测试 |
| 2 P1（D4/D5/D7/D8） | ✅ | `appkit.Auditor` Option + `resolveAuditor()`（mount/reconcile 审计同源，顺带补 `TenantID`）+ 契约测试；`grpc.RecoveryInterceptor`（panic→`berrors.Internal`，bundle 链第二外层）+ `emitMetricsSafely`（gin/grpc metrics 纳入 recover）+ `TestGRPCRecovery_PanicContained`；`normalized` 默认 true + `Raw()` opt-out + `TestDefaultNormalized`；observability Logger 请求期惰性解析（gin/grpc 各加 `resolveLogger()`，`WithLogger` 保留为显式隔离入口） |
| 3 P2（D6/D9/D10） | ✅ | `AuditMiddleware/AuditInterceptor` 签名去死参（bundle/测试/示例同步）；**假通过测试已修**（`TestAuditInterceptor_PanickingAuditorSafe` 改 `AuditWithAuditor(bad)` 真实覆盖 recover）；authn 401 响应统一 `web.ErrorBody` 形状；proto `Tls` 增 `root_ca`/`client_ca`（`ca` 弃用别名），`ResolveTLS` 拆分链校验与 mTLS 语义，`task proto-config` 重新生成 |
| 4 P3（E1-E4/E6） | ✅ | 删 15 个 `ObservabilityWithXxx` 便捷函数 + 独占 setter（**保留 `WithLogger`**——D8 显式注入入口，保留 gin `WithSkipMetrics`——`Logging()` 依赖）；删 `config.Marshal` + `encoder.go`；`bindflags.go` 死变量 `_ = d` 与重复 doc 合并；`RequestID` 别名删除（保留 `RequestIDMiddleware`，bundle 与 `_example` 调用点同步） |
| 5 决策项 | ⏸ | E5/E7-E11 未动（按计划独立 PR） |
| 6 收尾验证 | ✅ | 核心 build+vet+race 全绿；`_example/bald`、`examples/go-bald-admin`、5 个 contrib module build+race 全绿；改动文件已 gofmt |

**执行中发现并修复的额外问题**：
1. `examples/go-bald-admin/internal/apiserver/grpc_audit_e2e_test.go:49` 漏改旧签名 `AuditInterceptor(nil, ...)`——D6 后 `nil` 被解析为首个 option，调用即 nil deref panic。**教训**：`examples/**/go-bald-admin` 在 `.gitignore` 中（`examples/**/go-bald-admin`），ripgrep 尊重 gitignore 导致此前「全仓搜索」对该目录盲区——影响面排查必须 `grep -r` 兜底（已验证全仓仅此一处漏网）。
2. E1 删除时曾把 `WithLogger` 一并列入删除清单，执行时修正保留：它是 D8「显式注入优先于全局惰性」语义的唯一公共入口，删除会使该语义不可达。

**遗留跟进**（后续 PR）：阶段 5 决策项；`框架契约总览.md` 已同步 D3/D4/D5/D6/D7/D8 契约；打 tag `v0.2.0` 待用户确认后执行。
